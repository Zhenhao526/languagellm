# 完整消息重编码基准：探索性扩展

决定时间：2026-09-15 16:06:14（北京时间）。此时A阶段27组已完成，全部原始片段分数和初版三条件重编码结果已经查看，C阶段正在运行。

本次把同一重编码基准扩展到全部六类五选一、长度二的条件：已知目标课程、隐藏目标课程、组合留出、全程阻断通道、直接训练和混合呈现。新增的是后三项；其余三项使用原种子重新精确复核。每个训练种子、每个通信方向仍为100次25码随机双射，每次重新在校准照片上选择两个符号位置的分配，再于验证照片固定评分。组合留出的选择仍只用8张训练地图。

动机是原始严格迁移分数在课程、直接和混合训练间存在差异，需相对各自保持任务表现的重编码分布比较，不能只比较原始比例。此项属于**看到结果后的探索性评价扩展**，不声称最初计划或最初评价范围包含全部条件；不修改模型、训练预算、种子或已有结果。

原始 `protocol_analysis.json/.md` 保留。扩展另存 `protocol_analysis_extended_null.json/.md`。程序核对初版全部54个方向的既有字段及18个方向的原重编码结果完全复现，并记录原JSON哈希不变。全部病例、筛选子集及零分母均保留。

```sh
redesign_v0.3/deployment/.venv/bin/python redesign_v0.5/analyze_protocols.py --extended-null
```

每个协议的重编码范围是条件于该协议的参考分布，不是独立训练种子的置信区间。高于重编码均值或范围不能单独证明语法，也不能消除不同协议原生成功率和可处理情境数量不同带来的解释限制。
