# 固定消息展示

本规则只生成辅助展示，不添加主指标或改变训练。写于正式私人阶段进行中、社会阶段结果尚未读取时。

取第一个预定来源34101、划分1、发送方向0、测试池按ID最前的食物和唯一水图，地图ID0–5，各两种末帧mask。A/B/C使用相同12个世界，共展示36条终点消息，完整保留成功和失败，不按结果挑样例。每枚整数0–6依次显示为`@ # ¥ % & * +`，这些字符只用于报告，模型实际收到的仍是整数token。

展示包含真实地点、两符号原生贪心消息、接收者两种需求的实际贪心地点和双目标是否正确；不人工解释任何单个符号的含义，不从这组例子推断句法。
