# 多任务段回放的临时验证

本目录是运行中的静态快照，不是最终实验结果。快照生成于 2026-09-14 22:12:33（UTC+8），状态为 `running`，包含 38 个已经结算的动作步。后续日志不会自动进入本页面。

| 条件 | 群体 | 任务段 | 快照动作步数 | 是否有任务段结束记录 |
| --- | --- | --- | ---: | --- |
| 延迟符号共享 | 17 | 1 | 12 | 是 |
| 延迟符号共享 | 17 | 2 | 12 | 是 |
| 即时符号交流 | 17 | 1 | 12 | 是 |
| 即时符号交流 | 17 | 2 | 2 | 否；页面明确不把当前得分当作最终结果 |

使用独立的新 headless Chrome 上下文，未访问用户浏览器 profile，未运行模型推理。通用验证命令为：

```sh
node qwen_language_v2/replay_browser_qa.cjs qwen_language_v2/review_outputs/symbolic_interim_multi_episode/replay.html --out qwen_language_v2/review_outputs/symbolic_interim_multi_episode --expected-cases 4
```

实际浏览器在 736 和 320 像素两种宽度下遍历了全部 4 个条件／群体／任务段组合。每种宽度遍历 38 步及其动作前、动作后状态，合计 152 次状态检查。逐步核对了三人的实际观察原文与日志、四窗消息与日志、前后导航边界、任务段选项及完成标记。没有页面错误或横向溢出。更换研究者全局状态前后不会改变主体当步观察。延迟和即时条件的投递时序提示分别核对通过。

截图按条件和宽度采集，每组取最后一个可用任务段的最后一个已记录动作步，包含页面顶部、广播区及主体区。人工查看了 `browser-320-immediate-g17-ep2-top.png`、`browser-320-delayed-g17-ep2-messages.png` 和 `browser-736-immediate-g17-ep2-agents.png`：中文及符号可读，任务段 2 选项清楚，即时段的未完成提示明确，研究者记录与主体观察的说明没有混淆。

完整机器记录见 `browser_verification.json`。本次 HTML SHA-256 为：

```text
5d98fcbc36acc8e185cb174595533b674d4a403eaaf013fabe8fc27dc5600708
```

最终运行结束后应另建 `review_outputs/symbolic_final`，重新生成页面并使用 `--require-completed --expected-cases 4` 验证。该参数要求快照状态已完成，且四个组合均有任务段结束记录，避免把本临时验证当成最终验证。当前没有修改冻结的六个实验核心文件、模型或既有实验日志。
