# 完整符号实验回放验证

本目录回放对应 `results/pilot_immediate_delayed_20260914` 的完整主试跑。页面是静态快照，生成于 2026-09-14 22:41:38（UTC+8）；生成时运行状态为 `completed`。共包含 48 个动作步，即时与延迟各一个群体（群体 17）、两个任务段。回放不会自动更新，也不包含后续冻结状态探查结果。

| 条件 | 任务段 | 结算动作步 | 最终完成度 | 是否全部完成目标 |
| --- | --- | ---: | ---: | --- |
| 即时符号交流 | 1 | 12 | 1/2（50.0%） | 否 |
| 即时符号交流 | 2 | 12 | 2/3（66.7%） | 否 |
| 延迟符号共享 | 1 | 12 | 1/2（50.0%） | 否 |
| 延迟符号共享 | 2 | 12 | 2/3（66.7%） | 否 |

“运行已结束”和“有任务段结束记录”表示日志已结算，不表示达成全部目标。表中的完成度直接来自任务段结束记录；本次回放验证不判断符号是否具有语义，也不将完成度、消息重复或长度作为语言形成证据。

生成及验证命令：

```sh
python3 -m qwen_language_v2.replay_snapshot qwen_language_v2/results/pilot_immediate_delayed_20260914 --out qwen_language_v2/review_outputs/symbolic_final/replay.html
node qwen_language_v2/replay_browser_qa.cjs qwen_language_v2/review_outputs/symbolic_final/replay.html --out qwen_language_v2/review_outputs/symbolic_final --require-completed --expected-cases 4
```

验证使用已安装 Chrome 的全新 headless 浏览器及独立上下文，Playwright 1.62.1，未访问用户浏览器 profile；GPU 渲染关闭，验证结束已关闭浏览器。没有模型推理、后台服务或实验日志修改，六个冻结实验核心文件未改。

## 实际浏览器检查

736 和 320 像素两种宽度分别遍历全部四个条件／群体／任务段组合、每段全部 12 步：共 96 次动作步显示、192 次动作前后状态检查。

- 完成状态守卫通过：快照为 `completed`，四个任务段均有结束记录；各段最终完成度与记录一致。
- 逐步核对了四个通信窗口的 12 条正式消息显示，包括空消息表示；显示文本与该步真实记录一致。
- 每次切换前后状态均核对了三主体实际观察原文。切换研究者全局状态不会改变各主体当时收到的观察。
- 全局物理时钟、已交付单位与总目标单位随前后状态正确切换。
- 条件、群体、任务段及动作步选项与快照逐项对应；前一步／后一步及首尾禁用边界正确，无跨任务段混入。
- 即时组提示同窗互不可见，窗口收齐后公开；延迟组提示四窗结束后公开他人本步消息，自己此前输出可见。广播表明确标作研究者完整记录。
- 无页面错误；所有检查状态均无横向溢出。

机器记录见 `browser_verification.json`。它验证的是页面如何呈现已嵌入的记录；实验信息隔离及计分规则的验证仍由实验代码审查和测试承担。

## 截图检查与文件哈希

共保存 12 张截图：每种宽度、每种条件各取任务段 2 第 12 步的顶部、广播区和主体区。截图是代表画面，全部 48 步均已做实际浏览器交互检查。人工查看以下五张：

- `browser-320-immediate-g17-ep2-top.png`
- `browser-320-immediate-g17-ep2-messages.png`
- `browser-320-delayed-g17-ep2-messages.png`
- `browser-736-delayed-g17-ep2-agents.png`
- `browser-736-immediate-g17-ep2-top.png`

中文选项、完成度、广播符号及投递时序说明均可读，窄屏自动换行。主体观察区与研究者全局状态分开，等待动作显示为“等待已执行”。私有分析没有当作通信内容呈现。

HTML SHA-256：

```text
75a350f9366b63fefbd9d2c91ae7a3f51e1108c7baf6f537f8a2064e9a0c7fa2
```

`sha256_manifest.json` 另外记录了 HTML、验证 JSON、截图、生成及验证脚本和三个源日志文件的哈希。本目录为新建的最终回放验证输出，未覆盖此前 `symbolic_interim_multi_episode` 或任何控制组快照。
