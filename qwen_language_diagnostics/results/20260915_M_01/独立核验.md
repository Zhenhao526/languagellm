# M 动作语义诊断独立核验

核验对象为已完成的 `20260915_M_01`，覆盖全部 16 次调用和 8 次正式判断。逐项结果与审计时间见 [verification.json](verification.json)。只读取记录、重新构造题目、重放环境状态和渲染 tokenizer 文本；没有加载模型权重、追加推理或修改冻结源码与实验记录。

**执行与冻结设计一致，正式结果为 6/8。M1 两种选项顺序均错，M2、M3、M4 两种顺序均对。**错误没有因换序消失；正确答案也随编号变化保持相同含义。这里只报告四个陈述各两种呈现的结果，不将其作为八个独立合作样本或一般语言能力分数。

## 输入、真值与执行核对

- 共 16 个连续调用、8 次分析及 8 次正式输出；调用顺序、种子 `20260916`、温度 0、每次新缓存与关闭原生 thinking 均符合预设。结果、逐次决定、状态和后端统计一致，没有重试或追加题目，也没有启动符号实验。
- 16 份实际消息均与各自冻结题目和两阶段构造一致。每个正式输入仅接上同题刚生成的分析；没有前题答案或分析进入下一题。全部提示用本地 tokenizer 重渲染后的 SHA-256 与日志一致。
- 八个基础提示只含 C 当前完整信息观察、对应直读、空历史、空广播、题目说明、待判断陈述及答案选项。原规则和完整信息例外均实际出现在系统提示中。没有研究者真值、正确编号、原始全局状态、见证计划或未来结算字段进入输入。同题两序只改变答案选项。
- 从 v3 首场第 9 步真实 `state_before` 独立生成两人共同搬运后的 S1、非法单人拿取后的状态及共同交付后的状态，均与预备记录相等。真值独立核对为 **T、F、F、T**：M1 到营地且建立共同负载；M2 单人拿取失败且物理状态未变；M3 当前木材交付量仍为 0；M4 增加 1 个交付单位并释放双方携带位。
- 12 份源码当前文件及快照全部匹配冻结摘要，其中包含 v3 的 7 份核心。预备案例、原 v3 四份来源记录及旧 R/S 四份来源文件摘要均未变。模型 revision 元数据与此前一致；本审计没有重新全量计算权重文件摘要。

8 次分析和 8 次正式生成全部正常 stop，无 token 上限截断。记录推理耗时约 24.7 秒，模型加载约 1.9 秒；本轮最大提示为 1679 token。

## 正式答案与证据边界

|陈述|环境真值|肯定选项在前／在后的正式编号|两序实际含义|结果|
|---|---|---|---|---|
|M1：匹配共同搬运后到营地且共同占位|真|1／0|不符合规则|两序均错|
|M2：长木可先单人拿取、下步再共同搬运|假|1／0|不符合规则|两序均对|
|M3：已搬到营地，所以已完成交付|假|1／0|不符合规则|两序均对|
|M4：匹配共同交付后计入一件并双释放|真|0／1|符合规则|两序均对|

**M1 的明确偏离发生在生成的规则解释中。**call 1、3 认可双方同地、同一目标及共同搬运前提，却将林地到营地的一步移动写为违反相邻地点规则。实际提示的原规则明确三个地点两两有道路，每次一步到达；环境重放也成功。随后 call 2、4 的正式否定与这段错误判断一致，没有“分析已给正确判断却被编号约束翻转”的证据。分析文本显示了可定位的矛盾，但不能单凭它认定内部致错机制或推定模型完全不会共同搬运。

**M2 的正式正确不能等同于解释正确。**call 5、7 都讨论“单人拿取后”占用携带位、无法立即转成共同搬运，而没有明确识别本题单人拿取本身就不合法。研究者真实结算是在第一步即拒绝该动作。题干包含“先拿再搬”的复合陈述，否定整句不能区分模型是否正确识别了第一步禁令。正式评分仍应保留为正确，同时记录这一解释限制。

M3 的 call 9、11 明确引用当前交付量 0，并指出搬到目的地不自动交付。这里的 `delivered=0` 和共同搬运反馈属于 S1 的合法当前观察，不是未来答案泄漏；因此 M3 包含状态读取，不能单独当成未来状态预测。M4 的 call 13、15 与真实共同交付后果相符，但给定双方动作的判断也不等于主体会自主协商出这些动作。

四题在本次换序中均保持答案含义，不代表不存在其他顺序效应。S0 来自已知失败，S1 是研究者生成的环境副本；这些结果不构成新的成功合作回合，也不能推翻 v3 第二场已经实际完成共同搬运和交付的成功记录。
