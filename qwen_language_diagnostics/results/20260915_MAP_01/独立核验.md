# G/J 地图呈现诊断独立核验

本核验覆盖已完成的 `20260915_MAP_01` 全部 16 次调用、8 次正式判断。逐项布尔结果、摘要及审计时间见 [verification.json](verification.json)。仅只读记录、重构输入、结算环境副本，并用本地 tokenizer 重渲染文本；没有加载模型权重、生成新输出或修改冻结文件。

**执行符合冻结设计。原地图与显式列边各为 3/4，四个配对的正式答案全部相同；J1 在两版中均错，其余三题均对。**J1 原版还完整重现了此前 M1，不能作为一个新的独立能力样本。

## 来源与实际输入

- 14 份源码的当前文件及运行快照均符合 manifest 摘要，其中包含 v3 的 7 份核心。预备案例摘要、旧 v3 四份来源记录及旧 R/S、M 各四份来源文件摘要全部匹配；从冻结 M 案例重新构建本轮输入也完全一致。
- 16 个调用连续、顺序正确，没有重试或额外题目。所有调用使用种子 `20260916`、温度 0、每次新缓存及关闭原生 thinking。8 次分析和 8 次正式输出均正常 stop，无 token 上限截断。状态、逐次决定、结果及后端计数一致，符号实验未启动。
- 每个实际分析输入等于其冻结基础提示加原分析指令；每个正式输入只加入本题刚生成的分析及答案编号指令，没有跨题历史。16 份实际提示重渲染后的 SHA-256 全部匹配。
- 每对基础提示只替换系统中的一个地图句。用户消息逐字相同：C 的完整当前观察、直读、原三地 `map` 列表、空历史、空广播、题干和肯定在前的答案选项均未改变。显式列边没有改变物理环境；分析提示均比原版多 8 token。
- 模型没有收到研究者真值、正确编号、未来结算、见证动作字典或原始全局状态。题干给出的假设动作属于待判断问题，并非事先执行或伙伴实际通信。

记录推理耗时约 21.9 秒，加载约 2.2 秒，最大提示 1639 token。本审计检查了模型版本元数据及来源链，没有重新全量计算模型权重文件摘要。

## 环境真值与正式评分

从 v3 首场第 9 步的真实 S0 独立重放，得到 G1/G2/J1/J2 的真值 **T/F/T/F**。G1 实际一步移动给出了反驳 G2 必经河滩的合法反例；J1 双方互配同一木材和目的地，成功移动并建立共同负载；J2 两个共同动作各自都在合法菜单中，但目的地不同，双方均失败且物理位置、物品及携带状态未变。预备结算与独立重放全等。

|题目|真值|原地图正式答案|显式列边正式答案|结果|
|---|---|---|---|---|
|G1：A 可由林地一步到营地|真|0，符合|0，符合|两版均对|
|G2：必须经河滩且至少两步|假|1，不符合|1，不符合|两版均对|
|J1：匹配共同搬运到营地并共同占位|真|1，不符合|1，不符合|两版均错|
|J2：目的地不一致仍共同搬到营地|假|1，不符合|1，不符合|两版均对|

J1 原版的新 call 9/10 对照旧 M1 call 1/2，分析消息、分析提示 SHA、分析全文、正式消息、正式提示 SHA、正式输出及两阶段种子全部相同，与报告的重现标记一致。

## 不同分析文本不能替代正式结果

G1 和 G2 在原地图下已正确判断一步直达，无需经河滩。因此不能概括为模型在所有题型中都无法读取原地图。J1 原版 call 9 仍将林地一步到营地误写为违反相邻移动规则；其正式否定完整重现旧记录。

J1 显式列边版 call 11 承认 A/C 共同搬到营地符合规则，随后称 B 位于河滩，无法在林地执行等待，因此否定陈述。题干没有要求 B 到林地等待；冻结菜单和真实 J1 结算均确认 B 可在河滩等待，且不会阻止 A/C 合法共同搬运。该文本还同时写到 B 的状态不影响 A/C 搬运，包含自相矛盾之处。call 12 正式输出仍为否定，**不能将分析中某一处正确陈述记成答题改善，也没有正确判断被动作约束翻转的证据。**

J2 两版的分析全文相同，均正确指出目的地不匹配会使共同动作失败。合法菜单表示各人可以尝试该动作，不代表两人的组合能成功，这一结算区别得到保留。

本次没有观察到列边对四题正式答案的改善；不等于证明地图呈现一般无影响。列边同时改变文字长度、重复程度及与题干的直接对应，而本轮只使用一个已知失败场景。分析文本变化表明输出表述变了，不能据此认定内部机制或已修复某项能力。结果也不能推出一般共同动作能力缺失，更不能抹去 v3 第二场实际合作成功；本轮没有重新检验自主分工、群体协商或语言形成。
