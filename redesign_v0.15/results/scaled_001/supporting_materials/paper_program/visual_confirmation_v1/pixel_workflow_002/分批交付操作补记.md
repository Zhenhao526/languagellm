# 第二批流程图：分批交付操作补记

记录时间：2026-09-15T16:35:05.342960+00:00

主代理在下载进行中的服务端冷却期授权先交付固定顺序前 10 条已下载图，后续剩余图片另包。原第二批方案采用终止后统一制包；本补记只调整交付时间和分包，不改变 19 条名单、请求顺序、阈值、预处理、许可或视觉标准，也不增加请求。原方案和运行中 collector 字节保持不变。

前 10 条完整下载中 1 条按已冻结单帧格式规则隔离，首包因此含其余 9 张 ordinary 图片、3 张 sheet；不追加第 11 条补满首包。首包脚本初版错误地断言 10 条均为 ordinary，执行在创建目录和包之前失败。该源码与失败说明保存在 `partial_delivery_development`；修订仅对固定前 10 条使用已有格式过滤，不读取图片作视觉筛选。

评审者只读 [review_early_round1/packet.json](review_early_round1/packet.json)，随后分别封存判断。包 SHA256：`2c584d2dfa24e1b496a78410b396388d6fb68d2e172b62254e8196107c14ed9e`。收集者未查看 sheet 或判断内容，不向两位评审提供来源 / 类别 / 作者 / 许可 / 隔离项的 ID 映射。输入下载清单的当时原字节另存 `partial_delivery_001`，不是要求评审者读取的材料。

采用 `review_early_round1` 是为避免占用冻结 collector 终止时必建的 `review_round1` 目录。后者将保留为全量技术包，实际第二轮视觉包另写 `review_round2`，仅含不在首包的成功 ordinary 图，不能重复评审或扩张候选。完整 19 条处理和跨图近重复检测完成前，不宣布任何本批最终验收；双视觉结果也不代替来源许可核验。

第 11 条限流的已记 Retry-After 双时钟期限和唯一重试保持不变，后台原进程持续运行，未重启。
