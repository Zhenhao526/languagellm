# 本地同步与清理策略

本目录的 Git 归档保留版本快照、研究代码、设计文档、报告和小型可重建元数据。运行时环境、第三方依赖、模型权重、图片与音视频、全量数值张量、逐步 JSON/JSONL 日志以及文献全文缓存不进入 GitHub。它们会先在 STORAGE_MANIFEST.json 中登记容量，再在远端提交可验证后删除。

本次清单生成于 2026-09-17 15:28:33 +0800。扫描本地文件 **559,110** 个；候选归档文件 **8,281** 个，约 **0.30 GiB**；排除并计划清理的本地文件 **550,829** 个，约 **598.18 GiB**。

## 远端保留

- research_program/ 与 redesign_v0.* 的源代码、版本说明、研究日志和结果解释文档。
- 当前 Qwen 先导实验的部署代码、任务协议、模型校验记录和依赖锁定文件；模型权重本身不上传。
- 论文计划、文献元数据和可追溯的结果摘要；下载的全文与大体量缓存不上传。
- 本清单及路径规则，便于之后按需要重新生成或下载数据。

## 本地清理

远端 main 分支成功推送后，删除清单中标为排除的虚拟环境、缓存、第三方运行时、模型权重和生成结果原始文件；保留候选归档文件与必要的 README/报告。后续实验从依赖锁定文件重建环境，从脚本重新生成运行产物。

GitHub 普通 Git 对单个 blob 有 100 MiB 限制，本次没有把数百 GiB 的 .npz、.pt 和分片模型权重伪装成可上传的历史；它们的路径、数量和容量已记录在清单中。
