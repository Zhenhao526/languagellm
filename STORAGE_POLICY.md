# 本地同步与清理策略

本目录当前只保留可继续实验所需的源代码、冻结方案、紧凑结果、审计收据、研究报告和本地运行环境。历史实验的完整日志、checkpoint、模型/视觉资产、依赖缓存和文献全文已从工作树移除；它们仍可从 Git 历史中的相应版本恢复，必要时按冻结方案重新生成。

## 远端保留

- `research_program/tabular_signaling_study/` 与 `research_program/population_signaling_study/` 的源代码、测试、冻结 JSON、紧凑结果和独立审计；`results/formal_20260917/` 只包含小型可追溯档案。
- `研究报告_共同符号形成机制_2026-09-17.md`、同步记录和清理收据。
- Git 历史中的旧版研究代码、报告和先导结果。当前工作树不再展开这些旧版本。

## 本地保留

- `.exp_venv/`：当前 NumPy 实验环境；不提交到 GitHub。
- 当前两个实验包的源代码和紧凑结果。
- 需要重新运行时，从冻结方案和源码重新生成原始过程文件；过程文件默认位于被 `.gitignore` 忽略的 `results/` 或 `execution/` 目录。

## 清理规则

大体积原始运行必须先完成独立审计并写入紧凑收据，再在远端提交可验证后删除。普通 Git 不保存模型权重、数组、JSONL、图像、视频或全量 checkpoint；清理后的文件不会影响当前报告中的数字、哈希和审计状态。
