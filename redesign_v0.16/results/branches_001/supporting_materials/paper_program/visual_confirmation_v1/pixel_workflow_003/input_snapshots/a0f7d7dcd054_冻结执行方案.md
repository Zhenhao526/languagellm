# 流程图第三批：独立冻结执行方案

承接原144作者簇预约、像素方案和已封存002整理状态。前置有界作者核查仅请求原用户页一次、官方redirects API一次：重定向目标已确认，但与旧101作者的别名关系仍未消歧，因此不把待核图增加到可用数，原002文件不修改。当前有限流程图7/24：苹果1、香蕉2、橙子2、水2。新补记与两份官方原响应写入本批 `author_resolution_001`，全部纳入首次新原图请求前的输入冻结。

本批仅使用原同层 reserve 队列中尚未消费的前项：苹果3、香蕉2、橙子2、水10，共17个不同簇。先排除002已消费的19个reserve记录，无论那些记录后来下载/视觉/身份结果如何；保留原层内序号，再按原全局cluster_rank规定请求顺序。001流程预约、确认48预约、未分配候选均不进入本批；不访问确认像素，不改元数据框、不换排序盐、不自动用第18张替代失败项。本批最终可用数可能低于17，不能按命中率扩张候选。

源码从002已验证收集器迁移，只调整本批配额、已消费记录过滤、累计参照范围和输出目录。绑定原参考：DINO4数据处理代码、旧90张可用派生图、001全部24张和002全部19张的原/派生字节及清单哈希，原101仍有11张像素不可用，不能声称全覆盖。每条新图与这133个已有像素记录及本批先前图比较；全部17图成功时应有17×133+17×16/2=2397对。本批MPO和已有MPO只使用既定首帧派生作技术召回，同时保持格式隔离，不视为单帧合格图。

网络规则沿002：只GET冻结metadata的唯一original_url，不自动跟随HTTP重定向或换入口。每次请求结束后到下一次开始，UTC与monotonic都至少10秒；timeout60秒；单次读取最多50MiB，完整/截断状态与每次成功或失败原字节均保存。每候选最多两次尝试。429/503大小写无关解析Retry-After秒数或HTTP-date，在期限外再加1秒，两个时钟均满足才对同URL重试一次；无头用30秒加1，非法头或再次429/503停止。401/403、明确拒绝、验证或重定向立即停止，不换入口。其他5xx/临时网络错误最多一次至少5秒后重试，同时满足至少10秒串行间隔。后台等待以不超过15秒的小段执行，live_status只向主任务公开计数、错误种类与期限，不暴露候选映射；不因工具等待超时重启进程。

像素算法、格式/视觉/许可标准不变：Python3.12.14、Pillow12.3.0、缓存NumPy1.26.4；通过PYTHONPATH引用现有缓存，不修改环境。原SHA1和尺寸校验后EXIF校正/RGB，最长边640 LANCZOS不放大、JPEG95；从落盘640 JPEG依旧函数短边256 BICUBIC、整数中心224，逐像素及张量等同性核验。只有实际单帧JPEG/PNG进入匿名包，MPO及其他不符格式保留原字节但隔离。pHash/dHash定义及≤8/≤6召回阈值沿原冻结方案，不改阈值挑图。

匿名ID仍由原盐与candidate_id固定产生，只在packet中给匿名ID、EXIF校正全图/实际224及哈希；按匿名ID排序，每sheet最多4图。评审者不读选择、来源/类别、作者/许可、请求日志或格式隔离映射；收集者不看图或作视觉判定。全部下载终止后生成统一包；若主任务要求提前按已下载固定顺序分包，另记时间安排，不能改变候选或标准、不能重复评审已封存图。root和plan_scope各自保存五项判断并封存后才机械合并，不把AI评审称为人类验证。

技术完整性、双视觉、作者/许可和有限流程使用四种状态分列；0近重复召回不证明来源完全独立。旧未解析作者、旧缺失11像素和DINO可能预训练暴露仍保留。固定框现阶段产出低，会影响在既定配额内完成确认数据的可行性，必须如实报告，不能把下载原图数量当作独立模型确认。没有模型调用、特征提取或确认48图揭盲；本批结束即停，不自动追加。
