# 已知作者的有界解析补记

两次请求均成功：原 [GTSPACE 用户页](https://commons.wikimedia.org/wiki/User:GTSPACE) 与官方 MediaWiki redirects API 一致给出 `User:GTSPACE → User:SarKaLay`，目标 page ID145637534、revision1229254541。请求及原响应已保留；没有重试、媒体请求或后续第三个作者请求。

但旧101缓存中《Banana 10.jpg》的作者链接为 `User:SarKaLay စာကလေး`，新元数据框已有4条这一旧账号的作品被排除。它与重定向目标共享明显名称部分，但当前确切账号键不同；本次两个请求没有解析旧带后缀账号，既不能断定同人，也不能确认新图独立。不能仅凭目标解析成功就把待核转为通过。

因此原 `1e0495e5e2cf` 保持身份未决，原因由“尚未解析重定向”补充为“目标已解析，但与旧作者的别名关系未消歧”。这是新证据补记，不改002原报告/状态或两份视觉判断，也不作版权指控。当前有限流程可用数仍7，第三批按实际缺口固定17张；不为继续解析追加请求。完整证据和输入哈希见 resolution_supplement.json。
