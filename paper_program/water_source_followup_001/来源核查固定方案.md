# 普通饮水候选：来源核查与有限像素阶段

2026-09-16。在本次新网络请求及像素读取前固定。上一轮v27完成冻结协议材料探针，属于实质进展；现在转向独立形成实验的数据准备。原24流程/48确认数据未齐，不能继续把模型探针记作独立确认。

本阶段范围为v2原79代表中，后续元数据注释E且D的全部8项，原序号8、19、22、42、46、51、62、70。这是看过文字元数据后另定的普通饮水场景范围，不倒写成原v2的自动资格，也不改79及E28/U43/X8的历史计数。原80和E28全体来源核查的建议均未执行，不是本阶段必须凑齐的门槛。

在新的来源结果出现前，按原cluster_rank固定前4项（8/19/22/42）为workflow候选，其余4项（46/51/62/70）保留像素。全8核查官方来源文字；前4如来源待核/冲突，不用后4补位。最多下载4张，不依据下载或视觉产出扩张。已用v1确认48的身份仍全部排除，像素不访问；v26/v27暴露的11图只在重复来源排除中出现。本批普通图像验收沿原标准，不查看任何实验模型得分，不重新调用DINO或行动/通信模型。

固定3个逻辑文本请求：1次Commons官方API读取8个文件pageid的当前版本、wikitext、imageinfo/extmetadata；1次Commons官方API解析7个已记录作者用户页的重定向和页面存在性（不请求个人履历内容）；1次已记录Flickr原作1081916080的HTML用于核Dave Matos/dmatos稳定账号及既存许可链。只请求这些文本入口，不加载HTML引用的图片、脚本、样式或其他子资源，不扩展搜索。字段存在不自动算摄影者真实身份已证实；有署名/自作/许可的一致支持可判有限来源支持，机构账号或上传者不能自动当作者。7项Commons自作与1项Flickr的证据分列，原图SHA1不一致则原版本待核，不用新文件静默替换。

请求串行，每次结束到下次开始在UTC和monotonic均至少10秒；60秒超时，正文最多8MiB，保留所有响应头和成功/失败原字节。最多每逻辑请求2次，总HTTP不超过6。429/503解析Retry-After秒数或HTTP日期，等待期限再加1秒；缺失头等待601秒，非法头或重试再限流即停。401/403、明确访问拒绝/验证、HTTP重定向立即停止当前收集，不换入口。其他5xx/临时网络错误最多同URL重试一次，至少10秒；4xx非拒绝记该请求失败后保留，结束时来源不全项记pending。运行中等待小段≤15秒，工具观察超时不能作为重启理由。

来源输出逐项列creator/work/license/已知旧簇交集及结论，另存新增账号别名关系。使用旧1180键、209pageid、309title排除闭包；新增作者别名需沿其链接传播，含Flickr dmatos与稳定NSID、Commons已知重定向目标。既有8簇或其他原79簇若通过新增键关联到旧/预约来源，整关联簇标记本阶段排除。别名核查的有限支持不证明任意未知转载或DINO预训练暴露已排除。

像素阶段在来源结果封存后另固定实际可执行子集，只能来自原定前4。水和冰是同一物料，冰不作为混合食物排除；仍必须有可辨液态水和透明容器，实际224裁剪保持资源，无明显文字/水印，无混合食物/不明液体，普通单帧JPEG/PNG真实照片。来源判定和像素判定不能互相代替。匿名视觉包只给随机ID、全图与实际224；评审者不接触来源映射或模型成绩，评审身份标为AI，不称人类验收。来源/技术/双视觉/有限使用四种状态分别保留。本阶段完成即停，不自动用保留图补缺。
