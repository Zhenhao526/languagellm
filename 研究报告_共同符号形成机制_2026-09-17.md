# 从私有需求到共同符号：阶段性研究报告

日期：2026-09-17

本报告把此前的文献审查、视觉主体诊断、表格正对照和伙伴生态实验放在同一条证据链中。研究对象被严格限定为：**在没有语言先验的主体之间，哪些非语言能力和环境条件足以使离散信号成为可复用的共同符号**。这里的“共同符号”是行为定义，不等同于自然语言、词义系统或语法。

## 研究问题

单次任务成功不能说明语言已经产生。一个信号只有在满足下面三个条件时，才进入本研究的“共同符号”候选集合：

1. 发送者拥有接收者没有的任务信息；
2. 使用自然信号比从头关闭信道的匹配组获得更高任务收益；
3. 在同一世界和同一动作抽样下打乱 token—需求配对会消除收益，同时不同接收者对同一 token 的行为响应保持一致。

这个定义把信息作用、训练中的通道必要性和跨伙伴一致性分开。token 与目标有互信息只能作为描述统计，不能替代干预。

主要假设是：

- **H1 私有信息假设**：接收者不知道目标、且任务后果依赖目标匹配时，通信才有可识别的边际价值；
- **H2 时间记忆假设**：一次性信号要影响后续多步行动，接收者需要保留信号；
- **H3 资源压力假设**：当正确行动有足够多的可执行机会时，通信收益能累积；资源很快耗尽时，信号可能没有可识别的行为边际；
- **H4 伙伴生态假设**：轮换伙伴会对发送者施加跨接收者的协议约束；若发送者可见伙伴身份，则伙伴专用编码可能取代共同编码；
- **H5 组合泛化假设**：在训练中见过原子属性，不代表主体能在未见组合上形成可复用的表达。

## 实验阶梯与控制

### 视觉和神经策略先导

此前的 DINO/Qwen 线路完成了接口能力、动作后果、接收者冻结、组合留出和消息干预诊断。它们说明视觉表征可以支持有限的资源定位和动作选择，但也显示神经策略容易等待、依赖固定伙伴或把反馈中的目标信息当作捷径。先导结果因此只用于定位可行性和失败机制，不作为“语言已形成”的证据。

已有视觉组合留出实验的最窄结论是：固定接收函数上，直接条件的自然双目标正确率为 4.08%，同一函数的全码表只有 22.92% 的地图单元存在可同时满足两个目标的 token；人工从训练供体拼接为 15.40%。这说明接收覆盖和自然产出都有限，不能把失败简单归因于发送者不会发。详细审计和外部文献定位保存在 `research_program/论文证据与创新性审计_2026-09-15.md`。

### 表格低方差正对照

为区分环境不可解和神经优化失败，使用完全没有语言或教师先验的 tabular softmax sender/worker。侦察者在 round 0 看到私有二值需求，只发送一个 token；worker 在 round 1–5 选择两个地点之一。地点类型、库存和需求在同一 seed 内严格配对，动作和消息抽样固定，另有有限时域集中式 oracle。

冻结方案定义了 `recurrent/stateless × scarce/abundant × PI/FI × live/silent × persistent/switching` 的因子空间；本轮按预先选定的 18 个关键 cell 执行 144 个 run（8 个 seed，3000 次更新），switching 先只在 recurrent-PI 的容量边界上确认。每个条件同时报告 training-support 与 held-out；live 运行再做 closed 和 within-stream token permutation。PI 隐藏需求，FI 把需求直接给 worker。**主通道对照是 live 与从头训练的 silent，而不是只看 live 终点上的 closed 反事实**；后者在 FI 中可能是分布外输入。

### 固定与轮换伙伴

第二个表格实验固定侦察者，设置四个彼此独立的 worker。`fixed` 每次只和 worker 0 交互；`rotating` 每个 episode 均匀抽取一个 worker。`hidden` 不让 sender 看伙伴身份；`visible` 允许 sender 看到身份，作为伙伴专用约定的控制。地点改为一个二值私有排列：worker 观察地点 0 的类型，地点 1 类型由其互补确定，因此“目标类型”和“选择哪一个地点”不会被不可观测的远端类型混淆。矩阵为 16 条件 × 8 seeds，同样使用 live/silent、scarce/abundant、closed/permuted 和 oracle。

## 结果

### 私有需求、记忆和资源容量

以下是 8 seed 的 held-out 汇总；差值均在 seed 内配对后计算，区间为双侧 Student-t 95% 区间。

| 条件 | natural | 从头 silent | live−silent | closed | permuted | natural−permuted |
|---|---:|---:|---:|---:|---:|---:|
| recurrent, abundant, PI, persistent | 0.574 | 0.315 | **+0.260** [+.249,+.270] | 0.223 | 0.312 | **+0.263** [+.248,+.277] |
| recurrent, abundant, FI, persistent | 0.573 | 0.575 | −0.003 [−.011,+.005] | 0.220 | 0.567 | +0.006 [−.001,+.013] |
| recurrent, scarce, PI, persistent | 0.210 | 0.210 | 0.000 [0,0] | 0.113 | 0.210 | 0.000 |
| stateless, abundant, PI, persistent | 0.314 | 0.315 | −0.001 [−.002,+.001] | 0.299 | 0.314 | 0.000 |
| recurrent, scarce, PI, switching | 0.202 | 0.209 | −0.007 [−.034,+.019] | 0.153 | 0.209 | −0.007 [−.032,+.018] |

这组结果给出三个较清楚的边界：

- abundant PI persistent 是唯一同时通过 live−silent 和 natural−permuted 的主单元，说明在本任务中，私有需求、持续记忆和足够的行动窗口共同提供了可识别的通信收益；
- FI 的 live−silent 接近 0，说明通道本身不自动提高表现；recurrent FI 的 live−closed 差异来自 live 训练后对 token 的依赖，不能解释为信息传递必要性；
- stateless PI 和 scarce PI 的从头 silent 对照接近 live，closed 的正差不能单独当作通信证据。switching 的训练支持增益不能迁移到未见上下文，组合泛化没有自动出现。

### 轮换伙伴与共同 token

轮换伙伴实验由 `population_signaling_study/aggregate.py` 从最终 checkpoint 重新读取。核心指标是每个 worker 的 natural−silent、natural−permuted，以及 sender 选择的 token 在四个 worker 间的一致率和对两个私有地点排列的语义成功率。数值是 8 个 seed 的 held-out 均值；差值在 seed 内配对，区间为双侧 Student-t 95% 区间。

预先规定的解释方式是：

- rotating-hidden 若在每个 worker 上都有 live 边际收益，并且同一 sender token 对四个 worker 都得到高语义成功率，才称为当前任务中的 population-level common code；
- rotating-visible 若收益仍在但 sender token 一致率下降，说明伙伴身份可以支持多个伙伴专用约定，不能把任务成功直接叫作共同语言；
- scarce 若置换效应消失，只保留为容量导致的可执行性边界。

本实验不是把 fixed 的 token 复制到四个 worker，而是让独立 worker 表从不同初始化出发，在轮换交互中共同适应同一个 sender。结果如下：

| 条件 | natural | 从头 silent | live−silent | natural−permuted | sender token 一致率 | 语义成功率 |
|---|---:|---:|---:|---:|---:|---:|
| fixed-hidden, abundant | 0.800 | 0.300 | **+0.500** [+.499,+.500] | **+0.502** [+.500,+.505] | 1.000* | 0.531* |
| rotating-hidden, abundant | 0.800 | 0.299 | **+0.501** [+.499,+.502] | **+0.500** [+.496,+.505] | **1.000** | **1.000** |
| rotating-visible, abundant | 0.755 | 0.299 | **+0.456** [+.447,+.465] | **+0.460** [+.447,+.472] | 0.516 [.412,.619] | **1.000** |
| rotating-hidden, scarce | 0.307 | 0.300 | +0.007 [−.005,+.020] | +0.095 [+.046,+.144] | 1.000 | 1.000 |
| rotating-visible, scarce | 0.300 | 0.300 | 0.000 [0,0] | +0.004 [−.001,+.009] | 0.484 [.417,.551] | 0.969 |

`fixed` 每次只训练 worker 0，表中的星号表示其余 worker 的 checkpoint 读数不是训练后的跨伙伴证据，因此只作固定伙伴校准。`rotating-hidden` 是本轮最强的 population-level common code：同一 sender 的两个目标 token 在四个独立 worker 上均得到正确的地点选择。`rotating-visible` 仍能形成每个伙伴都可用的局部编码，但 sender token 一致率显著降低，且整体回报低于 hidden。scarce 的自然−silent 区间包含零，说明容量上限让通道的额外行为收益难以识别；其中 semantic readout 是终点策略的描述指标，不能替代置换干预。

完整数值、每 seed 记录、冻结方案和审计收据在 `research_program/population_signaling_study/results/formal_20260917/`。

### 消息长度与组合性筛查

为检验“增加消息长度是否会产生可重组结构”，正式实验在同一低方差环境中比较两种等容量消息：`mono4` 用一个四值 token，`dual2` 用两个二值 token。两者都能表示 4 种目标组合，只有 `dual2` 有可检验的槽位结构。发送者看到两个私有二元目标，worker 只看到每个子任务的局部资源状态；`factorized` 任务让两个目标位分别控制两个子任务，`entangled` 任务把第二个子任务改为两个目标位的 XOR。评估同时包含从头训练的 silent 对照、整段消息置换，以及按目标相关位从不同供体拼接两个槽位的 held-out 重组读数。

下表给出 8 个 seed 的 held-out 均值。差值在 seed 内配对计算，区间为双侧 Student-t 95% 区间；`sequence agreement` 是发送者在四个轮换 worker 上选择相同完整序列的比例。

| 条件 | natural | silent | natural−silent | natural−permuted | dual2 重组−natural | sequence agreement |
|---|---:|---:|---:|---:|---:|---:|
| `mono4`、轮换、隐藏、factorized | 0.498 | 0.249 | **+0.249** [+.205,+.294] | **+0.247** [+.202,+.292] | — | 1.000 |
| `dual2`、轮换、隐藏、factorized | 0.499 | 0.249 | **+0.250** [+.186,+.314] | **+0.247** [+.184,+.311] | **−0.117** [−.224,−.011] | 1.000 |
| `dual2`、轮换、隐藏、entangled | 0.484 | 0.250 | **+0.234** [+.172,+.296] | **+0.233** [+.172,+.294] | **−0.146** [−.268,−.025] | 1.000 |
| `dual2`、轮换、可见、factorized | 0.488 | 0.249 | **+0.239** [+.215,+.263] | **+0.238** [+.215,+.261] | **−0.136** [−.196,−.077] | 0.484 |
| `dual2`、固定、隐藏、factorized | 0.484 | 0.252 | **+0.231** [+.171,+.292] | **+0.232** [+.169,+.295] | **−0.153** [−.275,−.031] | 1.000* |

`natural−silent` 和 `natural−permuted` 在主要 live 条件均为正，说明双 token 确实被用作共享协议。可是槽位重组在四个 dual2 live 条件都不恢复 natural 表现；轮换隐藏 factorized 的区间仍排除零。这是本轮最清楚的负边界：**形成共享码本不等于形成可组合词法**。`sequence agreement=1` 只说明四个 worker 使用同一完整序列，不能说明两个位置分别表示两个因素；负的重组效应正好表明两个位置被整体绑定。伙伴身份可见时，任务收益仍在，但完整序列一致率降至 0.484，支持伙伴专用协议与总体通信可以同时存在。

固定条件每次只训练 worker 0，星号仅作固定伙伴校准，不作为跨伙伴证据。正式批次包含 80 个 run、每个 run 3000 次更新，共 240,000 条训练日志、80 个终点 checkpoint 和 120,000 对 live/silent 轨迹；独立审计的最大终点评估回放误差为 0。正式结果、冻结方案和审计收据统一保存在 `research_program/compositional_signaling_study/results/formal_20260917/`；完整日志和 checkpoint 已在远端提交核验后删除。

### 行动依赖与组合性平衡

消息长度实验显示整体码本可以形成，却不能保证槽位可重组。为检验行动后果是否改变这个吸引域，新增 `action_dependent_signaling_study`：发送者仍发送两个二值 token，但 `simultaneous` 在第一个行动前暴露完整序列，`staged` 则在两个子任务开始前分别暴露 slot 0 和 slot 1。其余世界、目标、伙伴和随机流保持配对。预先固定判据为 held-out natural ≥ 0.60 且 `|recombined−natural|≤0.02`；正式矩阵覆盖 32 seeds × 8 conditions = 256 runs。

| 任务 | simultaneous 可组合平衡 | staged 可组合平衡 | staged−sim natural | McNemar staged-only / simultaneous-only |
|---|---:|---:|---:|---:|
| factorized | 3/32 (9.4%) | 9/32 (28.1%) | +0.022 [−.016,+.060] | 7 / 1, p=.0703 |
| entangled | 0/32 (0%) | 10/32 (31.2%) | +0.052 [+.016,+.088] | 10 / 0, p=.0020 |

所有 256 runs 的独立审计通过，包含 768,000 条训练日志、256 个终点 checkpoint、384,000 对 live/silent 轨迹，最大回放误差为 0。行动依赖提高了进入“高回报且重组无损”平衡的概率；entangled 任务的配对证据更强，而 factorized 的回报差异区间仍跨零。这个结果把机制命题收窄为：**可分解的行动后果可能是组合性协议形成的必要条件之一；它不是消息长度本身的结果。** 当前所有四种目标组合都在训练中出现，因此该批次测量的是平衡选择，不是零样本新组合泛化。压缩汇总、seed-level 分析、冻结方案和审计收据位于 `research_program/action_dependent_signaling_study/results/formal_20260917/`。

### 新成员加入与协议传递

组合性筛查之后，正式测试一个已形成的协议是否能被新主体恢复。每个 seed 先训练一个 `rotating-hidden` 的四 worker parent population；parent 终点在 held-out 上达到 0.800 的理论上限，sender token agreement 和语义成功率均为 1。随后只替换一个组件：`worker` 条件把 worker 0 换成随机初始化主体并冻结 sender 与 worker 1–3，`sender` 条件把 sender 换成随机初始化主体并冻结四个 worker。新主体只通过 token、行动后果和奖励适应。

`live`、`silent` 和 `scrambled` 三个 child channel 使用相同的世界、目标、伙伴和行动／消息随机流。`silent` 把 token 变为 NULL，保留行动奖励；`scrambled` 对每个 episode 的 token 施加独立随机循环偏移，保留符号事件但破坏稳定映射。下表是 8 seed 的 held-out 均值；差值在 seed 内配对，区间为双侧 Student-t 95% 区间。`learning AUC` 是五个 checkpoint 的新主体 natural-return 曲线积分除以 3000 次更新。

| 替换方向 | live natural | silent natural | scrambled natural | live−silent | live−scrambled | live−silent AUC | live semantic |
|---|---:|---:|---:|---:|---:|---:|---:|
| 新 worker | 0.800 | 0.202 | 0.284 | **+0.598** [+.540,+.656] | **+0.516** [+.479,+.552] | **+0.548** [+.495,+.602] | 1.000 |
| 新 sender | 0.800 | 0.316 | 0.347 | **+0.484** [+.329,+.639] | **+0.453** [+.167,+.738] | **+0.443** [+.301,+.586] | 1.000 |

两个方向的 live 子代都在终点恢复 parent 的任务回报；silent 与 scrambled 没有稳定恢复。新 worker 的 greedy action 表与原 worker 完全一致率平均只有 0.405，但其对两个目标和两种局部排列的语义成功率为 1.000，说明功能协议可以被恢复，而无关状态的表项不必逐项复制。新 sender 的 live token 表与 parent 一致率为 1.000。scrambled 条件个别 seed 会因随机初始化偶然命中部分旧码本，因此这里同时报告配对差值、学习曲线和逐 seed fidelity，不能把单个高终点叫作文化传递。

这组结果把“共同符号能否形成”推进为一个可检验的传递问题：当互补组件冻结且奖励仍在时，稳定 token 能显著缩短新成员恢复时间；只保留符号事件或只保留奖励不足以产生同样的恢复。它仍然只证明离散任务协议的跨主体传递，没有涉及新词项、组合语法或开放语义。

正式批次包含 8 个 parent run、48 个 child run、168,000 条训练日志、56 个终点 checkpoint 和 48,000 行三通道配对轨迹；独立审计回放终点与学习曲线的最大误差为 0。压缩结果、冻结方案和审计收据位于 `research_program/generation_transmission_study/results/formal_20260917/`。

### 组合协议的跨主体传递

上一节的一代传递使用稳定的单整体 token。为检验槽位结构能否一起传递，新增 `generation_compositional_transmission_study`：父代固定为行动依赖实验中的 staged、dual2、factorized 协议，分别替换 worker 0 或 hidden sender；子代比较 live、silent 和 partner-permuted 三个通道，并沿用 held-out natural ≥ 0.60 且 `|recombined−natural|≤0.02` 的组合平衡判据。32 个父代中有 9 个先满足该判据。

在这 9 个可组合父代上，新 worker 和新 sender 均为 9/9 个子代恢复组合平衡，live natural 均为 0.667、重组−natural 均为 0；另外 23 个非可组合父代的两个替换方向均为 0/23，worker/sender 的 live natural 分别为 0.468/0.458，重组损失分别为 −0.199/−0.188。全部 192 个 child run 的独立审计通过，包含 576,000 条训练日志、192 个终点 checkpoint、384,000 行配对轨迹，最大回放误差为 0。

这是一项按父代状态分层的条件化传递：9/9 不能当作总体形成概率。它说明已进入组合平衡的父代可以把槽位协议交给新主体，但没有证明组合性会从零自发产生；所有目标组合仍在父代训练中出现，严格的未见组合留出见下一节。结果和冻结文件位于 `research_program/generation_compositional_transmission_study/results/formal_20260917/`。

### 未见目标组合与从零组合泛化

前面的 `recombined−natural` 只是在已经见过四种目标组合之后，把两个槽位从不同供体拼接起来；它不能回答新主体是否能处理训练中从未出现的组合。为此，固定 staged、factorized、dual2 的 parent sender，分别替换 worker 0，从四种目标组合中为每个 seed 留出一个组合。`full` child 看到全部四种组合，`leave_one_out` child 只看到另外三种；两种支持都配有成对的 live 与 silent 通道。留出的组合在 32 个 seed 间均衡，parent 是否先前进入可组合平衡在 child 训练前冻结。

| parent 分层 | n | full live 留出组合 | leave-one-out live 留出组合 | leave-one-out 达到 0.60 |
|---|---:|---:|---:|---:|
| parent composable | 9 | 0.667 | 0.263 | 0/9 |
| parent non-composable | 23 | 0.499 | 0.250 | 0/23 |

全部 32 个 seed 的总体 full live 留出表现为 0.546，leave-one-out 为 0.254，配对差值 −0.293 [−0.344,−0.241]。在 leave-one-out 支持内，live−silent 的留出差值仍为 +0.319 [+0.268,+0.371]，说明新 worker 继续利用消息来完成已见目标，但这条消息并没有让它恢复未见的目标组合。正式批次包含 128 个 child run、384,000 条训练日志、512 个终点 checkpoint 和 192,000 行 live/silent 配对轨迹；独立回放审计的最大误差为 0。

这个负结果比整体码本的重组读数更严格：即使 parent 已满足“自然表现高且槽位重组无损”的平衡，新 worker 在没有见过的联合消息状态上仍不能零样本恢复。当前 tabular worker 以完整消息历史作为状态，缺少把两个 slot 的行为映射分解并共享到新组合的压力；因此，组合协议的跨主体传递和组合泛化应当作为两个独立问题报告。结果、parent 分层、冻结配置和审计收据位于 `research_program/heldout_composition_study/results/formal_20260917/`。

### 接收者状态表示与组合留出

上一节的 leave-one-out 失败可能有两个来源：协议本身没有形成可分解结构，或者接收者把两个 token 合并成一个整体历史状态，因而没有把已见槽位映射迁移到新组合。为区分这两种解释，固定同一批 staged、dual2、factorized parent sender 和同一留出目标，替换 worker 0 后只改变接收者状态表示。`joint_history` 对照读取完整的 staged 消息历史；`slot_local` 在第一个子任务只读取 slot 0，在第二个子任务只读取 slot 1。两种表示共享 parent、初始化 seed、训练世界、消息／动作随机流、更新数和 live/silent 配对；slot-local 是显式的接收者架构干预，不提供语义字典或教师标签。为区分表示效应和“任务本身不可解”，正式矩阵还加入了看到全部四种组合的 `full` 支持对照。

| 接收者表示 | child 支持 | channel | all | seen | 留出 held-out |
|---|---|---|---:|---:|---:|
| `joint_history` | `full` | `live` | 0.524 | 0.516 | 0.546 |
| `joint_history` | `full` | `silent` | 0.250 | 0.262 | 0.214 |
| `joint_history` | `leave-one-out` | `live` | 0.491 | 0.571 | 0.251 |
| `joint_history` | `leave-one-out` | `silent` | 0.251 | 0.352 | −0.056 |
| `slot_local` | `full` | `live` | 0.498 | 0.496 | 0.504 |
| `slot_local` | `full` | `silent` | 0.250 | 0.262 | 0.214 |
| `slot_local` | `leave-one-out` | `live` | 0.497 | 0.553 | 0.328 |
| `slot_local` | `leave-one-out` | `silent` | 0.251 | 0.352 | −0.056 |

在 leave-one-out、live、held-out 读数上，slot-local 相对 joint-history 的 seed 配对差值为 +0.077，95% CI [−0.012,+0.165]；在 full 支持上差值为 −0.043 [−0.080,−0.006]。leave-one-out 相对 full 的下降由 joint-history 的 −0.295 [−0.345,−0.245] 降到 slot-local 的 −0.176 [−0.251,−0.100]。这说明 slot-local 的优势主要出现在缺失组合迁移，而不是一般性的性能提升；在 full 支持上它反而略低。

parent 分层进一步限定了这个结果。9 个在 parent 阶段已进入可组合平衡的 seed 中，joint-history 的 leave-one-out held-out live 均值为 0.255，0/9 达到 0.60；slot-local 为 0.667，9/9 达到。23 个非可组合 parent 中，joint-history 为 0.250、0/23 达到，slot-local 为 0.196、0/23 达到。因而，显式槽位读取不能从非可组合 parent 中凭空产生协议；它更像是把已经存在的因子化行动结构暴露给接收者，使可组合 parent 更容易跨越联合状态的留出边界。

两种表示的 live−silent 在 leave-one-out held-out 上分别为 +0.308 和 +0.384，均保持正值；因此 slot-local 的提升不能被解释为单纯的奖励或信道效应。正式批次包含 256 个 child run（32 seeds × 2 表示 × 2 支持 × 2 channel）、768,000 条训练日志、1,024 个终点 checkpoint 和 384,000 行 live/silent 配对轨迹；独立审计通过，最大 replay 误差为 0。紧凑结果、full-support 对照、parent 分层、冻结配置和审计收据位于 `research_program/receiver_representation_study/results/formal_20260917/`。该扩展批次已经把 leave-one-out 结果与 full-support 控制放在同一冻结矩阵中，后续统计均以这个统一批次为准。

### 两代替换链：组合协议能否继续传递

接着把一次新成员替换扩展为两代链，只保留前面 32 个 parent 中已经满足组合判据的 9 个 seed。`worker_then_sender` 先替换 worker 0，再替换 sender；`sender_then_worker` 采用相反顺序。每一代都各有 live 和 silent 分支，第二代从对应第一代的最终 checkpoint 继续。第一代替换出的 worker 使用 slot-local 接收状态；每个阶段的世界、目标、伙伴和随机流在 live/silent 之间配对。

结果显示明显的路径依赖：第一代 live 后再进行第二代 live，两种替换顺序都保持 `9/9` 组合端点，natural return 为 `0.667`，槽位重组损失为 `0`；第一代 live 但第二代 silent 时，两种顺序均为 `0/9`，因为第二代替换没有利用符号。第一代 silent 后，即使第二代重新开放 live，worker-then-sender 为 `0.177`、sender-then-worker 为 `0.314`，也没有恢复到组合阈值。第一代 live 分支在第二代的配对 live−silent 分别为 `+0.511`（worker-then-sender）和 `+0.506`（sender-then-worker），说明第二次替换仍然依赖可用的稳定协议。

这项结果支持一个较窄的“文化传递”命题：已有的组合协议可以连续交给两次新主体，但传递依赖前一代已经建立了可用约定；它没有证明协议能够从 silent 链中自行产生，也没有把离散任务码提升为人类语言。正式链包含 108 个阶段、324,000 条训练日志、108 个终点 checkpoint、72 个阶段二父链接和 108,000 行配对轨迹，独立审计最大 replay 误差为 0。分支展开的结果见 `research_program/multi_generation_transmission_study/results/formal_20260917/branch_stratified_analysis.md`。

### 三代链与严格组合留出

在两代链之后，进一步把每个事件都放在 leave-one-out 支持上：每个 seed 的一个目标组合在子代训练中始终缺席。正式矩阵覆盖 32 个 parent、两种接收者表示（`joint_history` 与 `slot_local`）以及成对的 live/silent 分支。generation 1 替换 worker 0，generation 2 替换 hidden sender，generation 3 再替换 worker 0；上一代的最终参数作为下一代的起点。组合平衡仍定义为 held-out live natural ≥ 0.60 且 `|recombined−natural|≤0.02`。

| generation | role | representation | live held-out | silent held-out | live−silent | composable |
|---:|---|---|---:|---:|---:|---:|
| 1 | worker | `joint_history` | 0.250 | 0.169 | +0.081 [+.043,+.119] | 0/32 |
| 1 | worker | `slot_local` | 0.322 | 0.168 | +0.153 [+.064,+.243] | 9/32 |
| 2 | sender | `joint_history` | 0.172 | 0.159 | +0.013 [−.060,+.086] | 0/32 |
| 2 | sender | `slot_local` | 0.185 | 0.157 | +0.028 [−.088,+.143] | 6/32 |
| 3 | worker | `joint_history` | 0.179 | 0.163 | +0.016 [−.037,+.070] | 0/32 |
| 3 | worker | `slot_local` | 0.198 | 0.144 | +0.054 [−.047,+.155] | 6/32 |

在前面已经满足组合判据的 9 个 parent 上，`slot_local` 的第一代为 9/9；经过 sender 替换后降为 2/9，第三代 worker 替换仍为 2/9。`joint_history` 在这三代均为 0/9。23 个原本不可组合的 parent 上，`slot_local` 各代分别为 0/23、4/23 和 4/23，说明少数 seed 可以在训练中越过阈值，但没有形成稳定的总体恢复。sender 的离散码本相对 parent 在 generation 1 的逐槽一致率为 1.000，在 generation 2 和 3 为 0.871；因此功能衰减不能简单等同于 sender 表面字符串完全消失。

这条链比两代 live 链更严格，因为每一代都要在未见目标组合上评估。结果支持一个有边界的结论：按行动阶段读取对应 slot 可以帮助新 worker 第一次恢复已有组合协议，但 sender 被替换后，严格的零样本组合恢复明显衰减，连续替换没有维持原有的 9/9。它测到的是“组合泛化与代际传递的联合稳定性”，不能单独解释为开放式文化演化。

正式链包含 128 条 chain、384 个 replacement event、1,152,000 条训练日志和 576,000 行 live/silent 配对轨迹；四个 seed 分片的独立回放审计均通过，最大误差为 0。紧凑结果、分层统计、冻结方案和收据位于 `research_program/multi_generation_chain_study/results/formal_20260917/`。

### 信道噪声下的协议维持与修复

在 9 个已组合 parent 上，对发送后的两个 slot 独立施加 0%、10%、25% 和 40% 的二值翻转。交叉 `worker_only`（sender 与 incumbent workers 冻结）和 `coadapt`（同时更新 hidden sender 与新 worker），并比较 `joint_history`、`slot_local` 两种接收表示。每个 seed 的世界、目标、伙伴、消息/动作随机流和噪声流在条件间成对共享。

| 翻转率 | 新 worker natural（train noise） | clean natural | silent | 重组−natural | 达到组合阈值 |
|---:|---:|---:|---:|---:|---:|
| 0.00 | 0.667 | 0.667 | 0.149 | 0.000 | 9/9 |
| 0.10 | 0.583 | 0.667 | 0.149 | 0.000 | 0/9 |
| 0.25 | 0.458 | 0.667 | 0.149 | 0.000 | 0/9 |
| 0.40 | 0.334 | 0.667 | 0.149 | 0.000 | 0/9 |

两种接收表示的数值完全一致；`coadapt−worker_only` 的新 worker natural、incumbent natural 和重组损失在四个噪声水平上也都为 0。也就是说，在这个固定二值码和当前更新规则下，coadapt 没有形成可观测的重协商；回报随翻转率下降，说明已有协议能被使用，却没有冗余来抵抗符号损坏。该实验只检验已形成协议的条件稳定性，不能推出从零形成概率。

正式矩阵包含 144 个 run、432,000 条训练日志和 432,000 行跨噪声配对轨迹；审计最大回放误差为 0。结果、冻结方案和收据位于 `research_program/noise_repair_study/results/formal_20260917/`。

### 三值属性与容量扩展

为检验当前结果是否只适用于二值任务，新增三值世界：每个子任务的三个地点各出现一次三种资源，位置随机排列；目标由两个三值私有属性组成。`mono9` 使用一个九值 token，`tri3` 使用两个三值 slot，二者容量相同；同时比较 factorized 与 modular-sum entangled 目标，以及 simultaneous/staged 时序和 live/silent 通道。

| form | task | protocol | live natural | silent natural | live−silent | recombined−natural | composable |
|---|---|---|---:|---:|---:|---:|---:|
| `mono9` | factorized | simultaneous | 0.148 | 0.110 | +0.038 [+.031,+.046] | — | 0/9 |
| `mono9` | factorized | staged | 0.143 | 0.110 | +0.033 [+.026,+.039] | — | 0/9 |
| `mono9` | entangled | simultaneous | 0.153 | 0.111 | +0.042 [+.027,+.058] | — | 0/9 |
| `mono9` | entangled | staged | 0.146 | 0.111 | +0.035 [+.021,+.049] | — | 0/9 |
| `tri3` | factorized | simultaneous | 0.182 | 0.111 | +0.071 [+.055,+.087] | −0.026 | 0/9 |
| `tri3` | factorized | staged | 0.193 | 0.111 | +0.081 [+.071,+.091] | −0.014 | 0/9 |
| `tri3` | entangled | simultaneous | 0.180 | 0.111 | +0.069 [+.054,+.085] | −0.034 | 0/9 |
| `tri3` | entangled | staged | 0.177 | 0.111 | +0.066 [+.047,+.085] | −0.016 | 0/9 |

`tri3` 的两个 slot 确实带来比 silent 更高的任务回报，但在 9 个 seed 上没有一个达到 0.60 的组合平衡；staged−simultaneous 的 factorized 差值为 +0.010 [−.003,+.023]，entangled 为 −0.003 [−.027,+.021]。因此，扩大属性和消息容量没有自动产生更强的组合协议。由于三值实验同时扩大了 worker 状态和动作空间，这个负结果只能说明在当前训练预算和表格主体下可学习性下降，不能把失败归因于“组合性在三值世界不可能”。

正式矩阵包含 144 个 run、432,000 条训练日志和 216,000 行 live/silent 配对轨迹；三份 seed 分片审计均通过，最大回放误差为 0。结果、冻结方案和收据位于 `research_program/ternary_composition_study/results/formal_20260917/`。

### 容量匹配的冗余、信道噪声与协议修复

前面的噪声批次只在一个已经形成的二值协议上施加扰动，无法区分“通道本身更抗噪”与“主体在训练中形成了冗余”。这一轮把消息空间作为因果干预：`dual2` 是四个原始状态的基线；`triple2` 是三个二值 slot，共八个原始状态，允许通过 slot 间的 Hamming 距离形成冗余；`atomic8` 是一个八值 token，原始容量相同但没有可分解的坐标结构。三种形式同时使用、在第一个行动前到达的消息，因而不让到达时序成为形式差异的解释。对二值 slot 使用 0、10%、25% 独立翻转；`atomic8` 的替换率设为 (1-(1-p)^3)，匹配三个二值坐标中至少一个被翻转的概率。每个 seed 先得到干净 parent，再比较 `scratch`、只替换 worker 的 `worker_only` 和 sender–新 worker 同时更新的 `coadapt`。

第一次执行使用了七步 horizon、四个有效行动步，却保留了 0.60 的功能阈值。理论最大回报只有 (4/7=0.5714)，因此所有端点都不可能达到预设阈值。该批次在分析前拒绝，失效原因和参数保存在 `research_program/error_correcting_signaling_study/results/invalid_20260917/`，不进入任何统计。修正版把 horizon 改为 6、行动从第 2 步开始，最大可达回报为 (4/6=0.6667)。

修正版正式矩阵包含 9 个 seed、3 种消息形式、3 种适应方式和 3 个噪声水平，共 27 个 parent 与 243 个 child run。独立审计通过：parent 训练日志 81,000 行、child 训练日志 729,000 行、跨噪声配对轨迹 486,000 行，终点 checkpoint 回放最大误差为 0。

| form | adaptation | p=0 natural | p=0.10 natural | p=0.25 natural | p=0.25 functional | p=0.25 最小 Hamming 距离均值 |
|---|---|---:|---:|---:|---:|---:|
| `dual2` | `scratch` | 0.457 | 0.414 | 0.357 | 0/9 | 0.00 |
| `dual2` | `worker_only` | 0.458 | 0.415 | 0.352 | 0/9 | 0.00 |
| `dual2` | `coadapt` | 0.457 | 0.416 | 0.352 | 0/9 | 0.00 |
| `triple2` | `scratch` | 0.504 | 0.462 | 0.395 | 0/9 | 0.11 |
| `triple2` | `worker_only` | 0.481 | 0.454 | 0.391 | 0/9 | 0.11 |
| `triple2` | `coadapt` | 0.481 | 0.455 | 0.392 | 0/9 | 0.11 |
| `atomic8` | `scratch` | 0.503 | 0.442 | 0.305 | 0/9 | 0.00 |
| `atomic8` | `worker_only` | 0.551 | 0.456 | 0.350 | 0/9 | 0.11 |
| `atomic8` | `coadapt` | 0.505 | 0.426 | 0.337 | 0/9 | 0.11 |

在 25% 二值 slot 翻转下，`triple2` 相对容量匹配的 `atomic8` 自然回报差值分别为 `scratch` +0.090 [0.056, 0.125]、`worker_only` +0.042 [0.012, 0.072] 和 `coadapt` +0.055 [0.024, 0.086]；这些是同一 seed 配对的 95% t 区间。相同趋势在 0% 和 10% 噪声下并不稳定，说明优势依赖受扰信道，而不是简单的容量或整体成功率差异。`triple2` 在无噪声时有 1/9 个 seed 达到 0.60 的功能阈值；在 25% 噪声下没有任何形式或适应方式达到该阈值。

这项结果只支持一个有边界的机制判断。三个二值 slot 的坐标结构足以改变噪声造成的性能损失，但没有在当前任务、更新规则和训练预算下形成预注册的“错误校正候选”：候选要求自然回报至少 0.60 且 sender codebook 的最小 pairwise Hamming 距离至少为 2，243 个 child endpoint 中候选数为 0。最佳端点是一个 `triple2` seed，在无噪声下回报 0.667、最小 Hamming 距离 1；这属于可用协议，不是纠错码。`coadapt` 与 `worker_only` 在 `dual2`、`triple2` 上几乎完全重合，表明本设置没有产生可观测的重新协商；`atomic8` 中 coadapt 的自然回报反而低于冻结 incumbent，提示适应本身可能扰动已有协议。因而，冗余形式影响了信道响应，但“噪声压力必然催生错误校正语言”没有得到支持。紧凑结果、种子级配对效应、冻结方案和审计收据位于 `research_program/error_correcting_signaling_study/results/formal_20260917/`。

### 重复意义下的冗余压力

上一轮的四个目标组合各自对应不同的完整消息，三位二值码没有理由把额外 slot 用来重复同一个意义。为检验“同一意义在多个后果窗口中反复有用”是否是冗余出现的条件，下一轮只改变任务语义：两个行动阶段都要求同一个隐藏 parity（两个私有二值属性的异或），而每个阶段的资源位置仍独立随机。消息形式、噪声配对、适应方式、训练预算和功能阈值保持一致。因此，`triple2` 的三个 slot 可以学习跨阶段共享的码，而 `atomic8` 仍是相同原始容量的单 token 对照。

| form | p=0 natural | p=0.10 natural | p=0.25 natural | p=0.10 功能/候选 | p=0.25 功能/候选 |
|---|---:|---:|---:|---:|---:|
| `dual2` | 0.667 | 0.583 | 0.460 | 0/9 / 0/9 | 0/9 / 0/9 |
| `triple2` | 0.667 | 0.633 | 0.523 | 7–8/9 / 7–8/9 | 0/9 / 0/9 |
| `atomic8` | 0.667 | 0.539 | 0.368 | 0/9 / 0/9 | 0/9 / 0/9 |

表中自然回报是三种适应方式的 seed 均值；候选数按适应方式分别报告为范围。`triple2` 在无噪声下三种适应方式均为 9/9 个错误校正候选，10% 噪声下为 7/9、8/9、8/9，25% 噪声下降为 0/9。其候选 codebook 的跨 parity 最小 Hamming 距离均值约为 2.78–3.00，within-parity 距离为 0；这表示两个目标共享同一个 parity 时，三个二值 slot 自发形成了两类重复码。容量匹配的 `atomic8` 在 10% 和 25% 噪声下显著更差；`triple2−atomic8` 的配对差值在 10% 噪声下为 +0.088 至 +0.098，在 25% 噪声下为 +0.137 至 +0.188，所有报告的 95% 区间均不跨 0。`coadapt` 与 `worker_only` 的差异接近 0，说明这个正结果来自任务中重复意义的压力，而不是可观测的在线重新协商。

这组结果把前一轮的负结果变成了可检验的边界：冗余不是由“有三个 slot”单独产生，而是在同一符号需要跨多个行动后果保持稳定时出现。它仍然是有限的二类纠错任务，不能直接等同于自然语言中的音系冗余或语法；需要在新对象、新组合和持续代际替换中检验这种码是否能够被重新解释和扩展。正式矩阵包含 27 个 parent、243 个 child run、729,000 条 child 训练日志，独立审计最大回放误差为 0；紧凑结果位于 `research_program/shared_redundancy_study/results/formal_20260917/`，前置设计阈值失效记录统一保留在 `research_program/error_correcting_signaling_study/results/invalid_20260917/`。

### 重复意义压力曲线

前两轮只比较了“不同目标各自需要一个完整消息”和“同一个 parity 反复有效”两个端点。为检验冗余压力是否随意义复用连续变化，本轮保持私有输入、四个行动阶段、伙伴轮换、消息容量、噪声配对、适应方式和训练预算完全相同，只改变四个阶段的目标函数：`unique4=[a,b,a xor b,not(a xor b)]`，`repeat2=[a,a,b,b]`，`shared4=[a xor b,a xor b,a xor b,a xor b]`。`unique4` 的四个布尔函数都在四个目标上保持 50/50 平衡，避免阶段目标分布成为混淆。三种消息形式仍是等容量对照：`dual2` 有四个原始状态，`triple2` 有三个二值 slot 和八个原始状态，`atomic8` 有一个八值 token 和八个原始状态。

正式矩阵包含 9 个 seed、3 个任务、3 种消息形式、3 种适应方式和 3 个噪声水平，共 81 个干净 parent 和 729 个 child run。三片执行各自通过独立回放审计，合并后的 parent/child 训练日志行数为 243,000/2,187,000，配对噪声轨迹为 1,458,000，最大回放误差为 0。完整的冻结方案、紧凑终点、聚合表和审计收据位于 `research_program/repetition_pressure_study/results/formal_20260918/`。

下表给出 `coadapt` 的九个 seed 均值；括号内是达到自然回报阈值 `≥0.60` 的 seed 数。候选数还要求最小 Hamming 距离至少为 2，并在 `shared4` 中要求同 parity 的目标使用完全相同的 codeword。

| task | form | p=0 | p=0.10 | p=0.25 | 候选数（p=0 / 0.10 / 0.25） |
|---|---|---:|---:|---:|---:|
| `unique4` | `dual2` | 0.481 | 0.435 | 0.367 | 0 / 0 / 0 |
| `unique4` | `triple2` | 0.493 | 0.460 | 0.392 | 0 / 0 / 0 |
| `unique4` | `atomic8` | 0.481 | 0.409 | 0.328 | 0 / 0 / 0 |
| `repeat2` | `dual2` | 0.458 | 0.417 | 0.355 | 0 / 0 / 0 |
| `repeat2` | `triple2` | 0.482 | 0.462 | 0.401 | 0 / 0 / 0 |
| `repeat2` | `atomic8` | 0.492 | 0.418 | 0.333 | 0 / 0 / 0 |
| `shared4` | `dual2` | 0.667 (9) | 0.582 | 0.458 | 0 / 0 / 0 |
| `shared4` | `triple2` | 0.667 (9) | 0.637 (8) | 0.528 | 9 / 8 / 0 |
| `shared4` | `atomic8` | 0.667 (9) | 0.538 | 0.393 | 0 / 0 / 0 |

同一 seed、form、adaptation 和 noise 下的配对比较表明，`repeat2−unique4` 的范围为 p=0 时 −0.024 至 +0.056、p=0.10 时 −0.018 至 +0.042、p=0.25 时 −0.015 至 +0.020；局部重复没有形成跨形式、跨适应方式的稳定增益。`shared4−unique4` 的对应范围为 +0.138 至 +0.209、+0.096 至 +0.181 和 +0.007 至 +0.136，说明全局重复在多数匹配条件下更容易形成稳定协议，但优势随噪声减弱。该效应必须结合任务等价类解释：`shared4` 只有两个 parity 类，而 `unique4` 有四个平衡且不同的目标，因此前者的高回报是低意义熵边界，不是开放词汇或组合语法的证据。

这轮结果把“重复后果是否足以诱导冗余”改写成一个更精确的结论：重复同一意义能在受限二类任务中支持跨阶段复用和 `triple2` 的重复码，但仅有局部复用 `repeat2` 不足以稳定改变协议几何。早期使用 `a&b` 作为 `unique4` 第四阶段的部分 pilot 因目标比例不平衡而停止；它只保留失效收据，不进入任何统计，见 `results/invalid_20260917/unbalanced_unique4_partial_20260917/`。

### 八意义容量压力

前一轮 `shared4` 的正结果可能来自意义空间只有两个 parity 类，而不是来自一般的重复压力。为区分这两种解释，本轮把任务扩展到八个三位二值目标：`unique8` 在八个阶段使用八个不同的平衡布尔函数，`repeat4` 使用四个平衡意义各两次，`shared8` 在八个阶段重复同一个三位 parity。`triple2` 有 8 个原始消息状态，`quad2` 与 `atomic16` 都有 16 个原始状态，后两者构成分布式二值坐标与原子 token 的容量匹配对照。伙伴仍轮换且身份隐藏，消息、行动、噪声流在匹配条件内共享，训练预算和候选规则与重复压力曲线相同。

正式矩阵包含 9 个 seed、3 个任务、3 种消息形式、3 种适应方式和 3 个噪声水平，共 81 个 parent 与 729 个 child。三片独立 replay 审计均通过，合计 243,000 条 parent 训练日志、2,187,000 条 child 训练日志、1,458,000 条配对噪声轨迹，最大回放误差为 0。冻结方案、完整聚合表、紧凑终点和审计收据位于 `research_program/meaning_capacity_study/results/formal_20260918/`，结果说明见 `meaning_capacity_formal_结果与下一步.md`。

| task | form | p=0 | p=0.10 | p=0.25 | coadapt 候选数（p=0 / 0.10 / 0.25） |
|---|---|---:|---:|---:|---:|
| `unique8` | `triple2` | 0.486 | 0.461 | 0.408 | 0 / 0 / 0 |
| `unique8` | `quad2` | 0.530 | 0.505 | 0.434 | 0 / 0 / 0 |
| `unique8` | `atomic16` | 0.558 | 0.481 | 0.377 | 0 / 0 / 0 |
| `repeat4` | `triple2` | 0.599 | 0.537 | 0.446 | 0 / 0 / 0 |
| `repeat4` | `quad2` | 0.602 | 0.545 | 0.453 | 0 / 0 / 0 |
| `repeat4` | `atomic16` | 0.585 | 0.488 | 0.385 | 0 / 0 / 0 |
| `shared8` | `triple2` | 0.800 | 0.770 | 0.645 | 9 / 9 / 9 |
| `shared8` | `quad2` | 0.800 | 0.762 | 0.635 | 9 / 9 / 8 |
| `shared8` | `atomic16` | 0.800 | 0.612 | 0.432 | 0 / 0 / 0 |

八个不同意义的 `unique8` 没有形成满足“自然回报 ≥0.60 且相关 codeword 最小 Hamming 距离 ≥2”的候选；`repeat4` 在部分 cell 的回报略高，但同样没有候选。相反，`shared8` 的重复 parity 使 `triple2` 在三个噪声水平都达到 9/9 候选，`quad2` 在 p=0、0.10 达到 9/9、在 p=0.25 达到 8/9。`atomic16` 在无噪声时可以有较高功能回报，却没有满足类内一致与跨类距离的候选。按全部适应方式和噪声条件计，`shared8` 的候选端点在 `triple2` 为 78/81、`quad2` 为 75/81、`atomic16` 为 0/81；`unique8` 与 `repeat4` 均为 0/81。容量匹配的 `quad2−atomic16` 在 `shared8` 的 coadapt 条件下从 p=0 的 0.000 增至 p=0.10 的 +0.149、p=0.25 的 +0.203，配对 95% 区间均不跨 0；这提示分布式坐标在受扰信道下更容易保留可复用结构。

本轮把“重复同一意义”与“意义空间低熵”区分得更清楚：局部重复 `repeat4` 不足以复制 `shared4/shared8` 的结构，稳定的重复码只出现在同一 parity 跨八个阶段反复有效的低熵任务中。因此目前更谨慎的机制结论是，重复后果可以诱导有限任务中的鲁棒共同码，但尚不能说明主体获得了开放词汇、组合语法或自然语言。下一步应在八个或更多独立意义中加入新对象和留出组合，并把同义重复、组合复用、伙伴变化和信道噪声正交化；否则无法判断 `unique8` 的失败来自任务熵、主体表示还是优化预算。

### 生态因子化与新组合传递

前面的八意义实验显示，低熵的重复意义可以稳定诱导有限冗余码，但 `unique8` 没有跨过高意义熵边界。本轮把“因子化目标是否支持新组合”单独拿出来检验：每个 episode 有两个行动阶段，每阶段显示三个对象的一次随机排列；sender 观察两个私有三值属性，共九个等概率组合。`factorized` 任务让两个阶段分别对应两个属性，`holistic` 对同样九个组合施加冻结双射后再产生阶段目标。消息容量匹配为两个分阶段三值 slot（`tri3`）与一个九值原子 token（`mono9`）。parent 在九个组合上训练，随后冻结 sender、替换 worker 0；child 要么看到全部组合，要么留出一个组合。tri3 child 还比较完整历史与按阶段读取 slot 的接收表示。

正式矩阵包含 9 个 seed、36 个 parent 和 216 个 child。三片 replay audit 均通过，合计 36,000 条 parent 训练日志、216,000 条 child 训练日志、324,000 条配对 live/silent 轨迹和 1,008 个终点 checkpoint，最大回放误差为 0。冻结方案、紧凑结果和收据位于 `research_program/ecological_factorization_study/results/formal_20260918/`，设计与结果说明见该包的 `ecological_factorization_formal_结果与下一步.md`。

parent 的 all-goal natural return 只有中等水平：`mono9/factorized` 为 0.143，`mono9/holistic` 为 0.131，`tri3/factorized` 为 0.195，`tri3/holistic` 为 0.167。作为通信存在性的检查，full-support、live、tri3/factorized 的 all-goal natural−permuted 差值为 0.186（paired 95% CI [0.143, 0.229]，joint-history）和 0.186（[0.127, 0.245]，slot-local）；这说明消息在部分训练平衡中确实带来行为收益，但并不表示已经形成高性能协议。

预注册的零样本判据是 leave-one-out、live、held-out natural return ≥ 0.60，理论上限为 0.6667。六个三值／九值留出 cell 的 9 个 seed 均为 0/9 达标：`joint_history/mono9/factorized` −0.044，`joint_history/tri3/factorized` −0.023，`slot_local/tri3/factorized` −0.036，`joint_history/mono9/holistic` −0.074，`joint_history/tri3/holistic` −0.065，`slot_local/tri3/holistic` −0.063。作为支持量检查，`slot_local/tri3/factorized` 的 leave-one-out−full 配对差值为 −0.329，95% CI [−0.468, −0.190]；因此失败来自留出干预，而不是把 full-support 结果误读为零样本泛化。

三个机制对比都没有稳定的正效应：factorized−holistic 在 slot-local tri3 留出 held-out 上为 +0.027，95% CI [−0.109, 0.163]；slot-local−joint-history 为 −0.013 [−0.057, 0.031]；等完整容量的 tri3−mono9 为 +0.021 [−0.045, 0.086]。已见组合上，factorized slot-local 的 raw-factor 重组近似无损，但这没有使新 worker 在未见联合状态上恢复。因而本轮支持的较窄命题是：**把行动目标写成两个因子、把消息拆成等容量 slot，并不会自动产生可传递的新组合协议；当前 tabular 主体还需要显式参数共享或更强的接收者归纳偏置。** 这个负结果不能单独推出组合语法不可能，因为三值场景、较大的状态表和优化预算也构成可学习性瓶颈；下一批先在已验证可学习的二值生态中加入对象表面重映射，再逐步回到三值和视觉主体。

下一项已冻结为二元对象表面重映射实验：在可学习的 `dual2`/`mono4` 生态中固定 sender、替换 worker，并把 child 的可见对象标签保持为 `identity` 或整体 `swap`。该矩阵同时保留完整支持、留一目标、slot-local/joint-history、live/silent 和等容量控制，先测量重映射造成的适应代价，再判断二元槽位是否能在新组合上恢复。

### 二元对象表面重映射：首轮诊断与修正

首轮 9-seed 矩阵已经完成，包含 18 个 parent 和 216 个 child；三片独立 replay audit 均通过，最大回放误差为 0。结果保存在 `research_program/object_surface_remap_study/results/formal_20260918/`，但该目录明确标为 diagnostic archive。

审计后发现一个评估流设计错误：all-goal 评估让 `partner_id` 与 goal index 同步循环，导致每个 partner 只看到一个 goal；因此在 partner 内旋转消息的 `permuted` control 是恒等操作，所有 live child 的 `natural−permuted` 都为 0。固定 held-out endpoint 和 identity/swap 训练流仍然成对生成，但首轮不能支持消息因果效应的推断。原始日志和 checkpoint 已在哈希核对后删除，失效原因记录在该归档的 `validity.md`。

修正版本按四个连续 goal 安排同一 partner，使每个 partner 在 all-goal 评估中同时看到四种目标，并加入回归测试保证 permutation control 实际交换不同 goal 的消息。只有修正版本完成审计后，才把 object-surface remapping 的通信差异用于论文统计。

### 二元对象表面重映射：修正后的正式结果

修正后的九个 seed 已在同一冻结代码下完成：18 个 parent、216 个 child，三片独立 replay audit 均通过。审计重放了 54,000 条 parent 训练记录、648,000 条 child 训练记录、72 个 parent 与 864 个 child checkpoint，以及各 324,000 行 live/silent 和 identity/swap 配对流；最大绝对误差为 0。修正后的完整汇总和 `parent-shift` 分析位于 `research_program/object_surface_remap_study/results/formal_20260918_corrected/`。

这轮先测量冻结 parent 协议对表面标签置换的敏感性。parent 只在 identity 映射上训练，sender 参数在 identity 和 swap 评估之间完全不变；因此该比较把 latent goal、语义场景和容量固定，只改变 child 可见的 object surface。all-goal natural return 的 paired 均值如下：

| parent form | identity | swap | swap−identity | 95% CI |
|---|---:|---:|---:|---:|
| `mono4` | 0.481 | 0.018 | −0.463 | [−0.534, −0.392] |
| `dual2` | 0.482 | 0.019 | −0.463 | [−0.570, −0.356] |

只有一个 `dual2` parent 在 identity 上达到理论上限 0.667；它的 codebook 四个目标均分离，但 swap 后降到 −0.167。其余多数 parent 学到的是两类 parity/粗粒度码，`pairwise_min_hamming=0`，仍然能在 identity 下取得约 0.46 的行动收益，却同样无法抵抗 surface swap。这区分了两个层次：**协议是否影响行为**与**协议是否把语义绑定到可迁移的表面形式**。

child 结果把“局部修复”和“新组合恢复”分开。下表是 primary endpoint（leave-one-out、live、held-out natural return）的初始值、训练后值和 seed 均值学习增益；理论上限为 0.667，预注册功能阈值为 0.60。

| 接收表示 | form | mapping | 初始 | 训练后 | 增益 | 达到 0.60 |
|---|---|---|---:|---:|---:|---:|
| `joint_history` | `mono4` | identity | 0.104 | 0.234 | +0.130 | 0/9 |
| `joint_history` | `mono4` | swap | 0.243 | 0.245 | +0.002 | 0/9 |
| `joint_history` | `dual2` | identity | 0.136 | 0.250 | +0.114 | 0/9 |
| `joint_history` | `dual2` | swap | 0.228 | 0.250 | +0.022 | 0/9 |
| `slot_local` | `dual2` | identity | 0.218 | 0.250 | +0.032 | 1/9 |
| `slot_local` | `dual2` | swap | 0.161 | 0.250 | +0.089 | 1/9 |

在 full support 下，child 可以从奖励中重新建立可用的行为映射：live held-out natural 均值落在 0.424–0.551，且各条件均有明显的初始到最终增益；但这不是零样本组合泛化。leave-one-out 的六个 live cell 中只有两个 `slot_local` cell 各有 1/9 个 seed 达到阈值，其他 cell 为 0/9，最终均值约 0.234–0.250。也就是说，稳定的 surface swap 可以被新 worker 局部修复，但缺失的联合 goal 仍需在训练中直接观察。

修正后的 all-goal 流让消息因果检查有效：所有 live 条件的 `natural−permuted` 均为正，cell 均值约 0.392–0.439；live 相对 silent 的 all-goal 配对增益约为 0.207–0.231。由于 permuted 控制现在把不同 goal 的消息交给同一隐藏伙伴，这些差异说明 child 确实使用了 sender 消息来选择资源，而不是只学到固定动作策略。该结论不依赖 held-out cell 的置换读数，后者因目标固定而只作端点指标。

这轮实验把当前机制边界说得更具体：已有共同码把意义绑定在可观察的表面标签上，标签规则改变会使 incumbent 行为崩溃；奖励和稳定的重映射足以让新 worker 在见过的目标上修复局部映射，但没有产生未见联合意义的零样本恢复。它支持“可识别的共同符号需要语义后果、接收记忆和可学习的重映射线索”这一窄命题，尚不能支持开放词汇、语法或人类语言起源的结论。

### 已形成协议的 incumbent worker：迁移崩溃与奖励修复

上一轮的 child identity/swap 比较使用了 fresh worker，因此它不能区分“旧接收器的迁移代价”和“新接收器从随机参数学习”。本轮把这个混淆直接拆开：parent 在 identity 表面上训练 `dual2` sender 和四个 worker；冻结 sender 后，`incumbent` child 复制 worker 0，`fresh` child 使用独立初始化。两类 child 都分别进入 identity 或稳定的 0↔1 surface swap，并保留 live/silent 对照。正式矩阵为 9 个 parent、72 个 child，每个 3,000 次更新；所有三片分片的独立 replay audit 均通过，最大绝对误差为 0。结果归档于 `research_program/object_surface_transfer_study/results/formal_20260918/`。

主要 endpoint 是 full-support、held-out goal 的 natural return。理论上限为 0.667，功能阈值仍为 0.60：

| 初始化 | mapping | channel | 初始 | 训练后 | 修复增益 | 达到 0.60 |
|---|---|---|---:|---:|---:|---:|
| `fresh` | identity | live | 0.146 | 0.597 | +0.451 | 7/9 |
| `fresh` | swap | live | 0.216 | 0.609 | +0.393 | 7/9 |
| `incumbent` | identity | live | 0.586 | 0.574 | −0.012 | 7/9 |
| `incumbent` | swap | live | −0.086 | 0.667 | +0.752 | 9/9 |

incumbent 的 swap 初始值相对 identity 的 paired 差为 −0.671，95% CI [−0.922, −0.421]；经过奖励更新后差为 +0.093，CI [−0.049, 0.234]，swap repair gain 相对 identity 多 +0.764，CI [+0.651, +0.877]。这说明旧 worker 的行为确实把约定绑定到了可见标签，但在标签变换稳定且后果可观察时，梯度更新可以把同一个接收器重新对齐。fresh 的 swap−identity 终点差为 +0.012，CI [−0.051, 0.074]，说明 fresh 对两种表面的学习难度接近；因此 incumbent 的初始崩溃来自迁移冲突，而不是 swap 世界本身更难。

消息的因果检查也通过：在 all-goal 流中，四个目标按完整 block 分配给同一隐藏 partner，live 条件的 `natural−permuted` 均值为 0.579；identity 条件 live 相对 silent 的 all-goal 增益为 0.372（incumbent 为 0.371）。stable swap 几乎不改变这个通信差异，说明干预改变的是接收器对 object surface 的 grounding，而不是 sender 消息是否影响行为。学习曲线的分段均值保存在 `learning_curves.json`，可以直接比较崩溃后的修复速度。

这轮把“协议传递”拆成三个可检验量：旧接收器在新表面的零样本损失、奖励下的局部修复，以及 fresh control 的普通学习增益。结果支持更窄的机制命题：**共同符号的功能依赖于发送消息、接收记忆和可观察对象表面之间的联合约定；表面规则改变会暴露迁移成本，但稳定的反馈可以重写接收器映射。** 这里仍然没有新对象、新联合意义、开放词汇或语法的零样本产生，因此不能把修复称为新语言的诞生。

### 群体感知异质性与共同协议

前面的 surface-swap 只改变一个新 worker 的可见标签。本轮把干预提升到 parent population：均质群体的四个 worker 使用同一 object-surface 约定；异质群体中 worker 1、3 稳定交换两个表面标签，worker 0、2 保持 identity。sender 看不到 partner identity，只通过有限消息协调轮换伙伴。parent 训练后冻结 sender 和其他 worker，再替换 worker 0；child 同时比较 identity/swap、full/leave-one-out、live/silent、`joint_history`/`slot_local` 与等容量 `mono4`/`dual2`。正式矩阵为 9 个 seed、36 个 parent 和 432 个 child，48 个条件组；三片 replay audit 合计重放 36,000 条 parent 与 432,000 条 child 训练记录、48/576 个 parent/child checkpoint，以及各 216,000 行 live/silent 和 identity/swap 配对流，最大绝对回放误差为 0。完整结果、片级审计和冻结方案位于 `research_program/heterogeneous_grounding_study/results/formal_20260918/`。

父代的全体伙伴 endpoint 显示，异质性没有造成稳定的总体性能损失：`mono4` 的 heterogeneous−homogeneous all-goal natural 差为 −0.023，95% CI [−0.077, 0.030]；`dual2` 为 +0.023 [−0.030, 0.077]。两种形式的 message gap 也没有稳定差异。与此同时，消息本身仍然有因果作用：各 full/live cell 的 all-goal `natural−permuted` 约为 0.417–0.463，live 相对 silent 的 paired 增益约为 0.221–0.255。

child 的主要留出结果为 `joint_history/dual2`、leave-one-out、live held-out natural：homogeneous identity 从 0.141 增至 0.250，homogeneous swap 从 0.164 增至 0.273，heterogeneous identity 从 0.167 增至 0.262，heterogeneous swap 从 0.167 增至 0.273；四个 cell 均没有达到 0.60 功能阈值。按 seed 配对的 heterogeneous−homogeneous 差在 identity 为 +0.012，95% CI [−0.015, 0.038]，在 swap 为 0.000；`slot_local/dual2` 的四个 cell 均值约为 0.296，功能阈值计数为 homogeneous 1/9、heterogeneous 2/9。

这组 child 结果需要按干预的暴露路径解释：child 只训练和评估被替换的 worker 0，且 `child_mapping` 覆盖该 worker 的表面映射，所以 worker 1、3 的异质约定没有直接进入目标 child 流。因而本轮支持的结论是“稳定的群体 surface 异质性不会自动破坏 parent 的共同协议，也不会在当前单目标替换中自动产生新组合泛化”；它不能被解读为非目标伙伴异质性已经被充分暴露后的零效应。下一版应让 child 实际服务非目标异质伙伴，或迁移已形成的 incumbent worker 表面约定，再估计跨伙伴群体效应。

### 多伙伴暴露与发送者协议对齐

上一轮群体异质性 child 只训练 worker 0，非目标伙伴的约定没有进入 child 流。本轮把替换角色改为 fresh sender：每个训练 batch 实际混合四个轮换伙伴，worker 的表面约定保持均质或让 worker 1、3 使用 swap。sender 的伙伴身份可以隐藏或可见；`sender_only` 冻结四个 incumbent worker，`coadapt` 同时更新 sender 与全部 worker；`leave_one_out` 在适应期间遮蔽一个联合目标。这样 partner exposure、身份可见性和适应方式都进入了可识别的因果矩阵。

正式矩阵包含 9 个 seed、18 个 parent 和 144 个 child，重放 54,000 条 parent 与 432,000 条 child 训练日志、72/576 个 checkpoint，以及 visibility、adaptation、population、support 配对流；独立 replay audit 的最大绝对误差为 0。完整结果、冻结方案和审计位于 `research_program/partner_exposure_study/results/formal_20260918/`，smoke 诊断位于同包的 `results/diagnostic_20260918/`。

parent 的均质与异质总体回报几乎相同（配对差 −0.000，95% CI [−0.113, 0.113]），all-goal `natural−permuted` 约为 0.486，说明两个群体都形成了有因果作用的消息协议。fresh sender 在 full support 下的 held-out natural 为 0.510–0.565，训练增益约 0.30–0.39；在 leave-one-out 下仅为 0.111–0.227，所有主要群体差异区间都跨过 0。

| population | visibility | adaptation | full support | leave-one-out | visible/hidden consistency（留出） |
|---|---|---:|---:|---:|---:|
| homogeneous | hidden | sender-only | 0.513 | 0.160 | 1.000 |
| heterogeneous | hidden | sender-only | 0.559 | 0.166 | 1.000 |
| homogeneous | visible | sender-only | 0.513 | 0.212 | 0.824 |
| heterogeneous | visible | sender-only | 0.559 | 0.227 | 0.824 |
| heterogeneous | hidden | coadapt | 0.557 | 0.157 | 1.000 |
| heterogeneous | visible | coadapt | 0.565 | 0.181 | 0.824 |

hidden sender 被迫使用一个跨伙伴码本；visible sender 在留出条件下的一致率降至 0.824，说明身份可见性确实打开了伙伴专用协议的自由度，但没有稳定提高未见联合目标恢复。coadapt 相对 sender-only 在 full support 上几乎没有增益，在 leave-one-out 中反而略有下降。由于 sender 的 joint-goal 参数彼此独立，leave-one-out 的失败不能解释为“多伙伴暴露没有形成语言”，它只说明共享码本的对齐压力还不足以产生因子复用或新组合。

这轮完成了上一轮诊断要求的可识别性修正，却给出一个更窄的负边界：**多伙伴实际暴露可以测量共同码本对齐和伙伴专用化，但不会自动产生组合语义。** 下一步应把独立社区的既有码本冲突、角色对称的 referential game、新对象与新组合留出正交化，并把协议一致性和零样本组合恢复作为两个主终点。

### 社区码本冲突与角色对称 referential game

上一轮的 fresh sender 只在发送方向适应，无法判断“新成员会使用旧码本”与“新成员能在两个角色之间共享约定”是否是同一机制。本轮把任务改成角色对称的 referential selection：sender 只看到目标对象的两个三值属性，receiver 看到四个无序候选对象，receiver 通过四符号、双槽位消息选择目标。parent 阶段让同一个 tabular policy 在两个角色之间轮换；child 的 fresh agent 每个 batch 实际接触四个伙伴，并在 `alternating` 中平衡 fresh-sender 与 fresh-receiver。`sender_only` 只作角色对照。

aligned 社区的两个副本共享同一冻结 canonical 初始化和 paired world stream；conflict 只把 community 1 的外部 token 做 `(0,1)`、`(2,3)` 双交换。这样 conflict−aligned 的差异不会混入独立随机码本。正式矩阵为 9 个 seed、36 个 parent、432 个 child，交叉 aligned/conflict、hidden/visible、fresh-only/coadapt、full/heldout-combination/heldout-value 和 alternating/sender-only。消息符号可以显示为 `@`、`#`、`￥`、`%`，但训练只使用离散 token id。

parent 的 all-goal natural return 为 0.584，`natural−permuted` 为 0.566，说明这套 referential game 能形成有因果作用的消息协议。主角色对称条件下，aligned/hidden/fresh-only 的 full-support all return 为 0.400，fresh-sender 与 fresh-receiver 分别为 0.443 和 0.358；加入表面冲突后降为 0.177、0.208 和 0.146，配对的 heldout-combination 差为 −0.278（95% CI [−0.536, −0.021]），message-gap 差为 −0.232（[−0.350, −0.113]）。hidden/conflict/coadapt 相对 fresh-only 把 full-support heldout-combination 提高约 0.060（[0.009, 0.111]），但仍不足以恢复未见联合意义。

伙伴身份可见性打开了专用码本：conflict/hidden/fresh-only 的 fresh-sender 跨伙伴一致率为 1.000，visible 为 0.481，visible−hidden 为 −0.519（95% CI [−0.599, −0.438]）。visible 并没有稳定提高 strict heldout-combination 或 heldout-value；在 alternating 的所有严格留出 cell 中，heldout-combination 的功能计数均为 0/9，heldout-value 支持遮蔽下也没有形成稳定的零样本原子值恢复。`sender_only` 的 full-support 回报明显高于 alternating（aligned/hidden/fresh-only 约 0.755 对 0.349），因此单向发送适应不能代替角色对称的共同协议。

这轮把下一层机制边界固定下来：**角色对称 referential pressure 可以让消息获得行为因果作用，并让社区表面冲突、伙伴身份和共同适应产生可识别差异；但有限 tabular receiver 没有因此获得新组合或新原子值的零样本语义。** 独立 replay audit 已通过（36 parent、432 child；最大绝对回放误差 0；训练日志 1,404,000 行；checkpoint 1,872 个）。完整冻结方案、聚合统计和清理收据已放在 `research_program/community_merge_study/results/formal_20260918/`。下一步需要把这个已验证可学的 referential game 移植到能力匹配的视觉主体，并单独加入结构化因子共享或可检验的文化传承机制；否则继续增加 token 数量只会扩大容量而不会解释组合语义。

### 结构化因子复用：环境压力与架构先验的因果分离

社区冲突实验说明，角色对称 referential pressure 可以让消息获得行为因果作用，但有限的 holistic tabular policy 仍不能把已见原子值重组为未见联合意义。下一轮保持世界、场景、消息容量、社区 surface permutation 和四伙伴轮换完全不变，只操纵策略结构：`holistic` 为每个完整意义和完整消息保留独立表格；`factorized` 让 slot 0/1 分别共享两个属性的值参数，receiver 以两个 slot 的属性证据相加。这个 factorized arm 是明确的结构性归纳偏置，不能被解释成无先验主体自行发现了组合语法。

正式矩阵包含 9 个 seed、72 个 parent 和 216 个 child，每个 run 3,000 updates。独立 replay audit 已通过，重放 216,000 条 parent 与 648,000 条 child 训练日志、288/864 个 checkpoint 以及 architecture、visibility、population、support 配对流，最大绝对误差为 0。完整冻结方案、聚合统计和清理收据位于 `research_program/structured_factorization_study/results/formal_20260918/`。

在 aligned/hidden 条件下，factorized child 的 held-out-combination natural return 为 0.974，9/9 个 seed 达到预注册的 0.60 功能阈值；holistic 对照为 −0.220，0/9 达到，配对差为 +1.194（95% CI [0.580, 1.808]）。factorized 的 held-out-value return 只有 0.122，0/9 达到，说明它复用的是已经学到的原子值，而不是从未见原子值中凭空建立词义。community conflict 在 hidden 条件下把 factorized 的 held-out-combination 降至 0.434；伙伴身份可见时回到 0.974，说明共享码本冲突与伙伴专用协议是可分离的机制。

这轮给出了目前最清楚的正结果和解释边界：**环境压力本身不足以产生组合复用；当主体具有可学习的 factor sharing 结构时，已见原子约定可以跨新组合传递。** 因而论文需要把“语言产生”分成两个问题：共同符号何时获得行为因果作用，以及组合结构由生态压力自发形成还是由主体归纳偏置带入。下一步应在视觉主体中做同一 architecture × community × partner visibility 矩阵，并加入不预先固定 slot—attribute 对应关系的可学习 routing 对照。

### 可学习 routing：槽位对齐与组合泛化的分离

为检验 factorized 正结果是否只依赖实验者预先固定的 slot—attribute 对应关系，新增 `routed` policy。它保留原子 factor table，但让 sender 和 receiver 各自学习一个两行 soft routing matrix；正式设定冻结为 routing-logit 初始标准差 0.5、softmax 温度 5。世界、消息容量、社区冲突、伙伴轮换、可见性和三种支持条件均与上一轮相同。正式矩阵包含 9 个 seed、108 个 parent 和 324 个 child，每个 run 3,000 updates；独立 replay audit 重放 324,000 条 parent 与 972,000 条 child 训练日志、432/1,296 个 checkpoint，architecture、visibility、population、support 配对流完整，最大误差为 0。完整冻结方案、结果和审计位于 `research_program/routed_factorization_study/results/formal_20260918_scaled/`；原始近均匀 routing 的低梯度敏感性基线位于 `formal_20260918_low_gradient/`。

在 aligned/hidden 的 held-out-combination 条件下，fixed `factorized` child 的 natural return 为 1.000（9/9 达到 0.60），`holistic` 为 −0.156（0/9），`routed` 为 0.245（1/9）。配对差值为 `factorized−holistic` +1.156（95% CI [1.087, 1.225]），`routed−holistic` +0.401（[+0.135, +0.667]），但 `factorized−routed` 仍为 +0.755（[+0.536, +0.974]）。routed 的 sender/receiver 最佳槽位置换对齐度均值分别为 0.717 和 0.728；它们明显高于随机的 0.5，却不能保证组合泛化，因为一些 seed 把两个 slot 都路由到同一属性，另一些 seed 的 sender 与 receiver 路由不一致。routed 的 held-out-value return 为 −0.020，继续支持“已见原子值可复用、未见原子值不能凭空恢复”的边界。

这轮把正结果的因果解释收窄了：**结构化 factor sharing 足以支持组合传递，但仅有可学习的 slot routing 并不能稳定从 payoff 中发现可复用的组合坐标。** 原始近均匀 routing 基线的 route 对齐度约 0.50、aligned/hidden held-out-combination return 约 −0.016，说明低梯度初始化会把 routed policy 留在无效盆地；提高 gate 的可学习性后，部分 seed 能形成近置换 routing，整体仍未达到 fixed factorized 的稳定转移。这是一个优化与机制交界的边界，不能把 routed 的失败直接解释成生态压力不足，也不能把少数成功 seed 解释成无先验主体已经发明了组合语法。

### 共享 routing：角色对称的组合坐标

独立 routing 的负结果有一个可识别的混淆：sender 和 receiver 可能各自形成槽位对齐，却没有形成同一个组合坐标。新增 `tied_routed` arm，只让 sender 与 receiver 共享一枚可学习的 slot—attribute routing matrix；两侧的 atomic factor table 仍然独立，因此没有直接共享 token 词义。holistic、fixed factorized 和 independent routed 保持为匹配对照，世界、社区冲突、伙伴轮换、身份可见性、支持遮蔽和 3,000-update 训练过程不变。正式矩阵为 9 个 seed、144 个 parent、432 个 child。三片独立 replay audit 的合并收据重放 432,000 条 parent 与 1,296,000 条 child 训练日志、576/1,728 个 checkpoint，architecture、visibility、population、support 配对流分别为 1,944,000、648,000、648,000、864,000 行，最大回放误差为 0。

在 aligned/hidden 的 held-out-combination 条件下，independent routed 的自然回报为 0.050（0/9 达到 0.60），tied_routed 为 0.552（4/9），fixed factorized 为 0.948（8/9），holistic 为 0.028（0/9）。配对差为 `tied_routed−routed` +0.502（95% CI [0.241, 0.762]），`tied_routed−holistic` +0.524（[0.214, 0.835]），`factorized−tied_routed` +0.396（[0.208, 0.584]）。independent routed 的 sender/receiver route 矩阵平均绝对差为 0.369；tied_routed 按定义为 0。tied_routed 的 held-out-value 回报为 −0.096，0/9 达到功能阈值，保留了“共享组合坐标不能恢复未见原子值”的阴性边界。

这轮把机制解释推进了一步：**独立角色各自学会槽位对齐仍不足以形成可传递组合；把组合坐标作为角色共享的可学习对象，会显著提高新组合恢复，但仍弱于实验者固定的 factor sharing。** 这个结果支持“角色间共享坐标”是组合语义形成的一个必要条件候选，而不是证明主体自主发明了人类语言。

### 路由初始化控制：共享更新与共同起点的区分

为排除“相同初始化本身带来收益”的混淆，新增 `sync_routed`：sender 与 receiver 的 routing 矩阵逐元素使用同一初值，但随后独立更新；`routed` 仍独立随机初始化，`tied_routed` 仍在训练中共享同一 routing 参数。三种 arm 的 atomic factor table 均保持独立，其他世界、训练、支持遮蔽和 9 个 seed 完全冻结。正式矩阵包含 54 个 parent、81 个 child，每个 run 3,000 updates。合并 replay audit 重放 162,000 条 parent 与 243,000 条 child 训练日志、216/324 个 checkpoint，architecture 与 support 配对流分别为 243,000 和 162,000 行，最大误差为 0。

在 aligned/hidden 的 held-out-combination 条件下，`routed` 的 natural return 为 0.050（0/9 达到 0.60），`sync_routed` 为 0.325（2/9），`tied_routed` 为 0.552（4/9）。配对差值为 `sync_routed−routed` +0.274（95% CI [0.070, 0.478]），`tied_routed−routed` +0.502（[0.241, 0.762]），而 `tied_routed−sync_routed` +0.227（[−0.036, 0.491]）。对应的 sender/receiver route 平均绝对差分别为 0.369、0.111 和 0。相同起点确实改善了独立更新 arm 的平均表现，但在 9 个 seed 下，共享更新相对相同起点的优势尚未达到稳定的配对显著性；因此现有结果支持“学习时保持角色耦合”是候选机制，却不能把它单独定为已确认的必要条件。三种 arm 的 held-out-value 功能计数均为 0/9，未见共享或相同初始化恢复未见原子值。

这项控制把解释边界收紧为：**共同起点可以部分修复角色路由错位，训练期间的共享更新仍有更高的点估计，但二者的差异需要更多 seed 或更强的任务压力来确认。** 后续应把初始化控制与新对象、新组合、伙伴变化和视觉主体正交化，避免把优化路径差异误读为语言结构本身。

### 开放世界扩展：新值与新组合的分离

前面的留出只问“已见原子值能否组成新联合意义”。为接近开放词汇的形成条件，本轮把父代限制在值 `0,1,2` 构成的 3×3 世界，消息仍是两个四值 slot；子代面对第 4 个值 `3`，因此完整 4×4 意义空间恰好等于消息容量。评估场景强制目标同时拥有共享 attr0 和共享 attr1 的 distractor，避免接收者只靠一个已知属性完成选择。除了 `old_combo` 和包含全部新意义的两个 support，还加入 `expanded_single_alternating/pair`：训练中只出现单新值意义，双新值 `(3,3)` 完全留出；`pair` 只在新值回合让 fresh sender 与 fresh receiver 同时承担角色，旧意义仍与 incumbent 交替互动。

正式矩阵包含 54 个 parent、135 个 child、9 个 seed。合并 replay audit 重放 162,000 条 parent 与 405,000 条 child 训练日志、216/540 个 checkpoint，architecture 与 support 配对流分别为 405,000 和 162,000 行，最大回放误差为 0。训练后修正了 role=2 重组评估器，并用同一 checkpoint 重新计算自然与重组终点；修正后的三片 audit 仍全部通过。

在严格的 `expanded_single_pair` 条件下，factorized 的 new-single natural return 为 0.996（9/9），double-new 为 1.000（9/9）；tied_routed 分别为 0.513（4/9）和 0.988（9/9）；holistic 为 0.616（4/9）和 −0.250（0/9）。factorized 与 tied_routed 的 double-new 自然消息和按两个 single-new donor 重组的消息逐 seed 完全一致，说明双新值回报不是直接训练 `(3,3)` 得到的整体码，而是由两个已学新值槽位组合出来的。holistic 的重组和自然 double-new 均失败。

fresh–fresh 的作用也有严格的配对对照。在同样排除 double-new 的训练流下，`expanded_single_pair−expanded_single_alternating` 的 double-new 差值为 factorized +0.887（95% CI [0.708, 1.066]）、tied_routed +1.068（[0.924, 1.212]），而 holistic −0.230（[−0.374, −0.085]）。交替条件下三种架构的 double-new 均未达到功能阈值。这支持一个比“新对象需要更多训练”更窄的机制命题：**结构化组合坐标必须同时获得新原子值的学习信号和双方共享的后果窗口，才能把未见的新联合意义恢复为可重组消息。** 这个结果仍依赖 factorized/tied_routed 的显式结构先验和预先指定的新增值；它不是 holistic 主体自主发明开放词汇的证据。

这轮把开放性边界推进了一步，但还没有完成文化传递检验。下一步应冻结已经形成的 single-new 扩展，替换其中一个 worker，测试新双值组合能否传给下一代；同时加入新对象表面置换和视觉主体，区分结构先验、共同适应和真正的代际文化保留。

## 形成过程和“语言”边界

当前最有价值的结果不是某个 token 字符串，而是一个可以反驳的条件图：

```text
私有需求 ──┐
           ├─ 充分行动窗口 + 接收记忆 ──> token 有因果收益
时间记忆 ──┘                                 │
                                             ├─ 伙伴身份隐藏 + 轮换 ──> 跨 worker 一致约定
                                             └─ 伙伴身份可见 ────────> 伙伴专用约定
旧协议 + 稳定 token + 奖励 ───────────────────> 新成员功能恢复
旧协议 + silent/scrambled ───────────────────> 无稳定协议传递
无冗余 token + 信道翻转 ────────────────────> 性能随噪声下降，未见自发修复
坐标冗余 + 容量匹配的原子 token ───────────> 受扰时性能差异，但未形成纠错码
同一意义跨两个行动阶段重复有效 ───────────> triple2 自发形成 parity 重复码
三值对象 + 等容量槽位 ──────────────────────> 弱通信收益，未见稳定组合平衡
冻结协议 + 稳定 surface swap ───────────────> incumbent 崩溃；奖励可修复已见映射，不能零样本补全联合意义
群体内部稳定 surface 异质性 ───────────────> 父代仍有消息因果收益；新 worker 未自动获得新组合泛化
fresh sender + 四个实际伙伴 ───────────────> 可测共同码本对齐；独立 joint goal 仍无新组合泛化
角色对称 referential game + 社区表面冲突 ─────> 消息因果作用与码本冲突可识别；严格留出仍失败
结构化 factor sharing + 同一生态 ───────────> 已见原子值跨新组合传递；holistic 对照失败
可学习 routing + 同一生态 ────────────────> 部分槽位对齐；组合泛化对 seed 和优化盆地敏感
相同 routing 初值 + 独立更新 ─────────────> 部分修复角色错位；仍弱于共享更新的点估计（差异需更多 seed）
```

这仍然是离散信号协议，不是人类语言的完整模型。实验没有让主体自主发现词类、组合语法、开放词汇、代际传承或文化演化；也没有证明分工是人类语言起源的唯一原因。更准确的论文命题是：**共同符号的可识别形成需要私有信息产生的任务价值、可持续的后果窗口和对接收者的记忆；伙伴轮换改变的是协议的共享范围，而非必然产生组合语法。**

## 可复现性和失效记录

- tabular 与 population 均使用本地 `.exp_venv` 的 Python/NumPy，未调用 LLM、视觉模型、外部 API 或网络服务；
- 每个正式 run 保存世界、目标、伙伴、消息和动作抽样的 SHA-256；live/silent 在同一其他因子下共享这些流；
- 独立 audit 从最终 checkpoint 重放训练日志和三种评估通道，要求最大回放误差小于 `1e−12`；
- 早期 population 运行因“两个地点独立抽样而 worker 只观察一个地点”被明确标为 invalid，并保留无数据失效收据；它不进入任何统计；
- 错误校正实验的第一次矩阵因理论最大回报低于功能阈值被拒绝；修正版重新冻结 horizon 后才执行正式批次，失效矩阵不进入统计；
- 紧凑轨迹、终点指标、参数／日志哈希和审计收据已上传到 GitHub；完整训练日志、checkpoint 和大体积合并结果因体积策略不上传，在远端提交核验后从本地删除。本地只保留源代码、冻结方案、紧凑汇总、审计收据和本报告。

## 论文级判断和下一步

现有证据可以支撑一篇机制性 workshop/短文，并为 ICLR 风格论文提供清晰的因果矩阵、正负对照和可复现实现；它还不足以声称“模拟出了语言起源”。当前最有价值的边界包括：轮换伙伴下可以稳定形成跨 worker 的整体码本，但等容量双 token 的槽位重组会失败；在已经形成可组合 parent 的条件下，给接收者提供按行动阶段读取对应 slot 的表示，可以把留出组合的恢复从 0/9 提高到 9/9，而在非可组合 parent 上仍然失败；两代链表明，已经建立的组合协议可以连续交给两次新主体，但前一代的 live 约定是后续传递的条件；在前一轮四目标任务中，容量匹配的坐标冗余优于原子 token 却没有达到错误校正候选标准，而把同一 parity 的意义放到两个行动阶段后，`triple2` 形成了跨 parity 距离至少 2 且类内一致的重复码；修正后的对象表面重映射进一步显示，旧协议对 surface 规则高度敏感，奖励可以修复已见映射但不能补全未见联合意义；群体感知异质性批次则暴露出一个新的可识别性要求：如果新成员只接触被覆盖的目标 worker，非目标伙伴的约定差异根本不会进入 child 流。噪声、冗余、三值扩展、表面重映射、群体异质性和结构化 factor sharing 给出了当前 tabular 主体的边界；下一阶段应把对象重映射放到新对象、新组合、伙伴变化和视觉主体中检验，并把结构先验与环境压力正交化。

下一轮优先固定三个检验：

1. 设计有显式冗余、确认或替代通道的信道干预，区分错误校正、sender–receiver 重新协商和单纯性能下降；
2. 在新对象和新组合同时出现的条件下，区分旧码本的传递、局部修复与真正的新符号约定，并把三值扩展的训练难度拆成可识别的对象复杂度和消息容量因素；
3. 将同一干预矩阵移植到能力匹配的视觉主体，把 tabular parent 结果作为可学性上界，而不是把 tabular 协议直接当作自然语言。
4. 在已完成多伙伴暴露修正的基础上，继续使用社区冲突与角色对称 referential game；把 holistic/factorized policy、独立初始化、社区混合、伙伴身份、incumbent/fresh、新对象与新组合留出正交化。
5. 将同一 architecture × community × partner-visibility 矩阵移植到能力匹配的视觉主体，并加入不固定 slot—attribute 对应关系的可学习 routing 对照。
6. 把 routing 的初始化尺度、温度和路由共享方式作为预注册优化敏感性因素；在视觉主体中检验“槽位对齐”与“held-out combination 恢复”是否仍然分离。

主要结论仍应以 `live−silent`、`permuted` 干预、跨 worker 一致性、槽位重组和新成员传递为主；互信息、单次成功和人工挑选的码表只能作描述统计。两代和三代链说明已有协议可以在部分条件下传递，但严格留出、噪声和三值扩展同时暴露了稳定性边界。要把当前机制结果提升为关于语言形成条件的更强主张，还需要在新组合、受扰信道和视觉主体上复现这一因果结构。
