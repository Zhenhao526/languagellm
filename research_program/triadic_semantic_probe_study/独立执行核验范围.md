# 独立执行核验范围（执行前准备）

只在根代理明确通知整批完成后运行一次审计；不会读取未完成成绩或额外训练。当前文件是审计范围，不能当作正式执行已经通过。首次只读干预审查见 `channel_readonly_review_001.json`：识别出 W2 的 logits argmax 与原始概率 argmax 在近并列时不同，已请作者在冻结前修正；审计者不改生产代码。

## 证据独立性

复用已通过历史独立执行核验的 `triadic_message_study/audit_execution.py` 中观察编码、纯矩阵 MLP、稳定 softmax、自然两窗路由、checkpoint 格式与原生结算；其文件及依赖 `triadic_learning_baseline/audit_execution.py` 必须先与 `partner_001/audit_execution_001/verification.json` 中记录的历史 SHA 相符。新的出站消息干预另写独立路径，不调用本轮 `channel_intervention.intervene/replace_outward`，也不把本轮 `metrics.py` 输出当作正确答案。

新静态 dataset 作为待核的索引载体：从旧完整状态表和全部原生成功方案重算每对世界、变化主体/需求轴、听者观察相等性、可接受行动集合及分类；不根据学到的动作重新选案例。单个主体的适切性与团队真实结算分开。

## 完整覆盖与所需清单

- 160 份新自然 NPZ：5 个前期检查点 × 16 个政策 × 2 分区；重建九个独立模块的全部消息、动作概率、greedy 动作与原生执行/满足/得分。
- 32 份末点自然引用：核对旧独立审计中的文件 SHA、checkpoint、真实状态索引及内容引用。不把原文件复制或统计当作本轮新增自然前向。
- 320 份新干预 NPZ：8 个 PI 政策 × 2 分区 × 10 模式 × 2 方向；逐行核对基础与供体世界、局部/远程映射、同/反需求、出站接收人、本人保留、可见位、W1 屏障、全部 W2 重算及三主体行动。自然 W1 与供体消息来自已经锚定的自然记录，独立重算后六个模块。
- 本轮源与数据快照 SHA、旧 checkpoint SHA、旧自然 SHA、各输出实际 SHA、运行状态、文件集合和总行数全部核对；不得只比较汇总得分。
- 独立按三轴、六种有序 sender/listener、案例及物理背景权重重算主要量与预定辅助差；完整保留所有四种子、两方向、零与负结果。

各文件清单需要 `kind / seed / condition / checkpoint / partition / mode / direction / path / sha256`；数据需要完整或可重建的基础 endpoint indices、case indices、供体 partition/indices、实际自生消息及供体消息。**自生消息与替换后出站副本必须能区分**，否则仅查看 `messages` 容易误解该网络真正接收了什么。

## 不变量与预算

sham、silent 的输出应和自然记录精确一致；local_both 的两个非发送者完整输入/输出应重现自然反端点，这只是结构核验，不计新的语义成绩。远程供体在全 24 布局的固定双射、owner 保持及跨分区流量须独立复核；留出身份只限定基础接收布局。

主实验预算独立算得：3,732,480 新自然世界 + 2,764,800 新干预世界 = 6,497,280 世界；网络样本分别 33,592,320 与 16,588,800，合计 50,181,120。另有 746,496 个旧末点世界引用。完整独立审计会产生额外 CPU 前向，预计相同的新记录重放规模；这属于核验计算，单独报告实际数量和时间，不混入主实验预算，也不称零模型前向。

不重新优化或重放训练，不检索最佳消息位置，不增加采样轨迹，不用准确率选择需要审计的文件。若中途发现不符，保存失败记录并报告；不悄悄跳过或改成容差通过。浮点概率的容差沿用历史审计，离散消息/动作/状态/得分必须精确相同。
