# v0.8 形成轨迹探针：执行前独立核验

被审脚本：`v08_formation_trajectory.py`，SHA `76b9b72d3cb921a4b71552f8f898aa33220c217b2b88d1d75909a4a4e5714a3c`。独立11项测试、12个参数化子案例通过，另穷举720个合成菜单。没有prepare、权重加载、神经前向或训练；未改作者代码。

计划为60运行×8检查点×2方向＝960方向记录，每方向固定30地图×16验证照片对＝480条自然消息。检查点恢复逐次严格装载，并核冻结私人投影和输入变换，保证预先计算的视觉投影仍对应当前检查点。2400检查点逐参数核对final，再锚定原协议结果。

菜单简化要求每个码／目标恰好一个有限最大值。并列最大值全局停止并保留原logits，不挑选方向或自动改用另一评价口径。已核AST“菜单仅为最终gather”的逻辑，并用全部720个排列独立验证唯一最大值的物理动作不变。

自然输入索引为每地图重复16次、16照片索引重复30次；每检查点共用相同验证照片。合成输入能复现480条全分母，单独改变一张照片对应码只改变其所在地图的一条计数。终点锚核49解码行、720菜单物理动作摘要及60兼容sender_goal行；原发送与实际投递频数均逐行匹配。旧协议没有逐照片对应表，所以锚点只称逐地图频数一致。

更新0跨条件核对原receiver、emitted及照片对，关闭通道组的delivered可不同；112次比较对应120份初始记录减8个种子／方向参照。blocked只能使用零码的可用覆盖仍与全49码反事实覆盖分开。

实现不支持已生成记录的resume：已有execution目录会在导入Torch前拒绝。这里的检查点恢复指装载历史权重，与断点续跑不同。源文件和快照在运行前后均核SHA，失败保留现有证据，不自动重试。

这些检查支持由主任务按固定预算运行探针，尚未验证真实网络是否无并列或所有终点锚是否通过。U/N轨迹属于事后探索；8个稀疏检查点不能给出精确的首次语言形成时刻，也不能单凭趋势认定生态因果或语法机制。数据只在四个种子层面保留独立重复。
