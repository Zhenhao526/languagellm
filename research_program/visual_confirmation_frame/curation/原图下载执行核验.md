# 原图下载阶段独立执行核验

2026-09-15。**核验通过：本轮按停止规则结束，成功取得 10 个与冻结元数据一致的原文件；第 11 次请求返回 HTTP 429，随后 95 个候选未请求。** 本核验只读元数据、收据及二进制哈希，未联网、解码或查看任何图片，未调用模型。

核验对象为 `water_download_001`。[逐请求核验 JSON](原图下载执行核验.json)；[独立脚本](audit_original_download_execution.py)。

| 检查项 | 独立结果 |
|---|---:|
| 冻结 water 请求计划 | 106 个 |
| 实际请求／不同 URL／不同 pageid | 11／11／11 |
| 成功原文件 | 10 个 |
| 逐个匹配冻结原 SHA1、收据 SHA256、元数据及响应字节数 | 10／10 |
| 成功原文件总字节 | 22400456 |
| 429 错误正文 | 2256 字节 |
| 实际响应体总字节 | 22402712 |
| 未请求候选 | 95 个 |
| 相邻请求的最小完成→启动间隔，按 UTC 独立计算 | 5.004243 秒 |
| 收据单调时钟记录的最小间隔 | 5.004259125 秒 |

20 个冻结来源文件 SHA、计划 SHA、执行快照和启动记录均匹配。编号 0–10 的请求前记录和完成收据连续且一一对应；全部 10 个相邻间隔不少于 5 秒。编号 0–9 均为 HTTP 200，返回地址与原请求相同；原文件未超出单文件 16 MiB，声明 Content-Length 存在时与实际字节数一致。逐收据累计字节与所有成功原文件及错误正文之和完全一致，未超过 2 GiB。

编号 10 的收据为 HTTP 429，`Retry-After: 600` 原样保留；2256 字节错误正文的 SHA256 与收据一致。该项没有原图或部分原图文件。执行结果为 `stopped_on_error`，没有编号 11 的请求前记录、完成收据或额外原文件；未请求名单精确等于冻结计划的第 12–106 项，未跳过、替换或重排。本轮没有自动重试，也没有改用缩略图。600 秒是记录到的服务端等待要求，不构成到时自动重试的授权。

**取得 10 个文件不等于通过视觉、作者来源或许可验收。** 当前也没有证据将此停止解释为“106 个元数据候选无法提供 84 张合格水照片”：元数据数量初筛已完成，本轮受限的是原图获取阶段，95 张尚未请求。最终 48／12／24 材料分配仍未进行。

证据范围是本地冻结代码、计划、执行收据和文件字节，未进行独立网络流量抓包，因此“不再请求”限定于该已归档执行的记录及停止路径。程序正常保存停止结果与研究材料获取完成是两回事；本轮不重试、不更换原图版本或缩略图。
