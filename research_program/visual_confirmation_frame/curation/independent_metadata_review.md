# 元数据候选独立核查

2026-09-15。**元数据候选重算通过；尚未验收任何照片，也未执行材料分配。** 本核查仅读取 JSON、元数据文本和代码，未查看像素、下载原图或调用模型。独立脚本不导入候选生成脚本，重新提取全部作者／来源键、重算旧库命中、用并查集重建来源图并模拟固定候选排序。[逐项结果](independent_metadata_review.json)；[独立脚本](independent_metadata_review.py)。

## 核查结果

| 项目 | 独立重算 |
|---|---:|
| 完整元数据文件 | 1876 |
| 原共享键规则的全局分量 | 1217 |
| 补入规范化 Commons 源标题关系后的分量 | 1215 |
| 命中旧材料／作者／来源的分量 | 99 |
| 被旧来源分量排除的文件 | 147 |
| 多类别交叠文件 | 18 |
| 最终提名文件／不同全局分量 | 232／232 |
| 提名原文件声明总字节 | 618432302 |

| 类别 | 元数据合格文件 | 合格全局分量 | 水优先后可用分量 | 主候选 | 备用 |
|---|---:|---:|---:|---:|---:|
| apple | 379 | 272 | 265 | 28 | 14 |
| banana | 407 | 303 | 297 | 28 | 14 |
| orange | 502 | 335 | 328 | 28 | 14 |
| water | 164 | 106 | 106 | 84 | 22 |

全部 1876 个文件先入图，再按分量传播旧来源排除；没有先删除不合格桥节点。18 个交叠文件中，1 个同时属 food／water，17 个属多个 food 子类，均退出单文件候选。含合格 water 文件的 106 个分量全部保留给 water；food 不占用这些分量。其余 food 严格按 apple→banana→orange 循环，使用已固定的分量／文件哈希排序，每分量只提名一个文件。

逐条核对了全部元数据排除理由、生成／非照片标记命中的文本、类别池、232 张提名的顺序／主备身份／pageid／原 SHA1／URL／大小，以及匿名评审清单。源文件及冻结快照 SHA、候选结果输出 SHA 全部对应。匿名评审清单只含匿名 ID 和预览路径，评审模板的类别和结论均未预填，不含作者、预期类别、主备名次或未来用途。

不互斥的排除数：旧来源 147、短边不足 145、文件大小不合格 44、MIME 不合格 36、许可模式不支持 64、作者键缺失 17、类别交叠 18、显式生成／非照片标记 2。不能把这些数字直接相加当作排除文件总数。

## 审查中补全的来源关系

审查发现原共享来源键只能连接“都引用同一来源”的文件，未必将衍生文件与框内被引用的原文件本身连接。根任务在像素与排序前批准补入源链接→标题节点的保守关系，并保存了原始链接、匹配标题及确定性标记；旧详情元数据未修改。分量 1217→1215 属于分组规则完善，不意味着上一轮按旧规则完成的元数据审计失败。

- `9367965 → 8945613`：`Petrina.orangepiano-2.jpg` 的 Credit 直接链接 `Petrina.orangepiano.jpg`，与框内标题一致。即使较小衍生图因尺寸退出，也应在筛选前保留该连接。
- `5948709 → 45067683`：Credit 实际链接 `Bananas.JPG`，框内标题为 `BANANAS.jpg`，作者字段也不同。二者仅在既定忽略大小写的规范化规则下相同；当前保守合并，**不是已证实同一原图**。最初仅看规范化键时将两对均称作明确原图关系，已在读取原始 Credit 后更正并通知实现者。

全框另有指向框外的 Commons 来源链接，仍需后续来源验收。没有新增“指向旧标题却未被排除”的本框案例。作者别名、上传者与摄影者混淆、系列及未披露衍生关系，也不能靠当前自动图全部解决。

## 分配与剩余门槛

方案的 48／12／24 分配规则与候选数自洽：food 每子类最终取 28，再分 16／4／8；water 最终取 84，再分 48／12／24。分配使用独立的 `partition` 哈希，发生在全部评审／来源核验锁定之后。**当前脚本只生成候选，未执行该分配，也未模拟一份实际照片用途名单。** 备用递补、后续分量合并和最终集合间来源隔离仍需实际验收阶段审计。

water 只有 22 张备用，需 106 张中至少 84 张完成全部验收；这只是算术要求，不是预计通过率。各 food 子类需 42 张中至少 28 张通过，不能跨子类补额。全体声明字节低于 2 GiB、每文件不超过 16 MiB，不证明实际响应一定符合预算。候选不足或后续来源合并导致不足时，应按方案停止，不能临时换盐、补源、放宽标准。

审核源：`prepare_candidates.py` SHA256 `65a38d757fd0ba4b6a1ac6dd6ba1134c8ac3b44db1ffb92602645195b86ece5f`；`plan.md` SHA256 `704e91200a71d1b6b663f147543a69b5932d031620e7a4965e1331875b8defdb`。完整源哈希见 JSON。

## 原图下载入口附查

初读确认：当前执行入口只允许 water；每任务一个 GET，拒绝重定向，无自动重试；HTTP／网络错误结束整轮并保留未请求清单；原始与错误响应体分别留档且计入字节，执行目录拒绝覆盖。SHA1／元数据不匹配与网络失败分开处理，前者按文件退出，不偷偷获取替代版本。food 下载需要另行冻结入口，当前候选清单没有授权提前下载 food。

另发现严格流读取边界待根任务修复：初版只验证 Content-Length 不超过总上限，却未把流读取上限同时降为 Content-Length，也未拒绝实际 EOF 短于声明长度。用无网络假响应验证了该脚本层缺口；常规 urllib HTTPResponse 自身限制 Content-Length，不能把假响应结果写成真实原图已超额。[修复前合成记录](download_boundary_before_fix.json)。本问题已在真实下载启动前修复：当前读取上限显式包含 Content-Length，提前 EOF 与声明长度不符时整轮停止。独立复跑全部 8 项断网合成测试通过，含两项新增边界测试。根任务可以按既定方案冻结并启动 water 阶段；该判断不包含 food、像素验收、许可验收或最终分配。[修复后预审记录](independent_download_precheck.json)。当前下载源 SHA256 `9dda34ad2bf01f3c8aada46dda0a8a2b23583d72fe03e7a6e028fed21d03c377`。
