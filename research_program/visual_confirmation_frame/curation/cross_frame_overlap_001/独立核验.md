# 跨框精确身份重合：独立复核

2026-09-15 14:46:40 UTC，纯本地元数据重算通过。脚本为 `../thumbnail_stage/audit_offline_metadata.py`，收据为 `independent_audit.json`。未导入根任务的重合实现；从本项目原 232 提名和另一任务 800 条元数据记录，独立逐对比较 pageid 与非空原文件 SHA1，重建的 108 条结果与 `overlap.json` 每个字段、顺序完全一致。

四个直接输入的 SHA256 全部匹配；另外核对另一状态链接的计划、采集源码、续跑记录、候选状态表、评阅顺序及历史状态收据共 6 个哈希，均一致。另一抽样框的 `(pageid,stratum)` 多重集也与 800 条实际元数据逐项对应。本核查不等于对另一任务全部请求与执行规则的复核。

| 本项目类别 | 精确重合 |
|---|---:|
| apple | 16 |
| banana | 20 |
| orange | 13 |
| water | 59 |
| 总计 | 108 |

108 对全部同时为相同 pageid、相同原 SHA1；没有同 pageid 异 SHA1，也没有异 pageid 同 SHA1。108 个本项目候选对应 108 对，没有一项被重复计数。

另一任务的临时用途计数：confirmation 9、workflow 6、reserve 10、未分配 83。9 个 confirmation pageid 为 `17360476、20854309、119900183、78764912、166335160、25582513、129384079、11053171、26528704`。这些是临时元数据名额，不是完成验收或已暴露的确认图片。

另一框为 800 条类别记录、799 个 pageid；40608211 的 orange／water 两条均未分配，本项目原 232 候选不含该项。保存状态为 `metadata_collection_complete`，完成时间 14:32:54.703765 UTC；本脚本未观察或控制进程。

仅核精确文件身份与保存字段。未读图片字节、未看像素、未联网、未调用模型；未核作者／系列／共同原图或感知近重复。来源重合提示后续需统一追踪用途，不能仅凭目录不同宣称独立，也不能由候选重合直接断言已发生训练泄露。所有原数据保持不变。
