# 第二轮元数据独立核验

2026-09-15，离线核验通过。未新增网络请求、图片下载或模型操作。[逐项JSON证据](独立核验.json)和[独立脚本](audit_metadata.py)保留。

- 全框1876个pageid唯一且全部有详情。第二轮仅请求原1551个未知ID；原325条记录与首轮逐字段完全一致。
- 63个唯一请求URL、63份请求前记录、63份HTTP200响应和完成回执全部对应。每批最多25个文件，末批1个；没有重复、遗漏或额外请求。
- 最小记录单调时钟间隔为5.000677458秒；独立按UTC时间戳计算的最小间隔为5.000649秒。所有62个间隔均不少于5秒，启动满足最后429后60秒及全部Retry-After截止时间。
- 322个来源、快照和响应／结果文件SHA核对通过。1876条记录均回查官方原始响应；许可模式、作者／源键、旧标题／SHA1／作者／源键排除标记重新提取一致。
- 独立使用图遍历重建分量，未调用原分组函数；五个类别／并集的文件数、初筛计数和分量成员全部一致。

water固定分组为121个元数据分量，再排除旧作者／源键后115；food对应978和963。water在本规则下**不再少于84**。这只说明数量门槛未被当前元数据盘点否定，不能称115个已确认独立摄影者或115张已验收照片。

将两资源合并后，排旧作者／源键的候选形成1061个元数据分量，其中可对应food的963个、water的115个；按每分量最多1个文件的两个资源计数约束，数量上允许各84个，但没有选择任何实际照片。全框有1个文件同时属于food与water类别：`File:Water Splash (3040411133).jpg`，pageid40608211；类别归属不能直接替代正式标签验收。

首轮partial、30次429、暂停／终止和失败记录保留，未回写为成功。完整的是当前四个直接类别的元数据覆盖；作者身份、系列、衍生作品、完整许可和视觉适用性仍待审查。
