# 配对执行设计独立预审

审阅源码 `runner.py` SHA256 `9aad4e8bbcb71f645227908ddb05559d103cd919e264dfa5b4bc4eae9aeaba87`。本次准备没有读取新训练成绩、政策文件或未完成输出，没有训练、前向或优化重放。根任务现已启动正式训练；须等其确认8组全部完成后，才运行独立执行核验。

未发现需要增加训练烟测的执行设计问题。两臂初始化均用 `[seed, actor, 100]`，训练批次均用 `[seed, 200]`，目标名称不进入随机数。三套actor分别构造参数和Adam状态，两个目标也从头分别构造，没有继承前一臂末点。每个种子先mean_J再mean_log_J，四个种子共8组；正常任务失败不触发早停，数值／程序错误保存失败并停止整批。

两臂使用同一冻结基线的54维完整信息编码、17动作头、原反事实收益和独立argmax部署。所有四格终点完整保存概率、动作、原奖励、执行和需求满足。固定检查点及每步批次／状态哈希均保存；最终比较仍使用更新6000，不选择最好检查点。执行器不会因99%候选筛查结果而解锁符号阶段。

独立脚本 [audit_paired_execution.py](audit_paired_execution.py) 不调用新runner的训练、评价或梯度函数。纯数值辅助函数由旧独立审计适配，重建划分与官方观察编码；只对真实保存的联合动作重算物理结算，不重新枚举4913个动作的全环境静态表。它将核对：

- 8组初值的每个参数与独立SeedSequence；每对初值逐值相同，48个检查点及其采样RNG状态。
- 48000个更新、12288000次状态抽样的完整哈希重建；两臂各6000批次及固定熵时间表逐项相同。
- 32份四格终点的全部状态、概率、argmax精确并列集合、执行／满足／原R、动作分布、失败计数、最终监测子集和原99%标记。
- 独立前向1147392个世界、3442176个actor样本；概率及随机策略解析量用atol=rtol=2e−12，动作、并列集合与物理结果要求完全相同。这将是实际终点前向核查，不能标为“零推断”。
- 四个预定双留出配对差及均值。全部种子与失败世界保留；参数更新与中间梯度不重放，数学有限差分另由对应审计负责。

准备期已做语法、序列化prepared划分、17动作和6个新增实际动作结算边界检查，通过。一次初始手动比较曾把make_prepared的内存tuple与JSON list直接比较而中止；改为审计实际读取的序列化形式后通过，未修改训练源码。该过程在 [paired_execution_preflight.json](paired_execution_preflight.json) 留痕。

根任务的原预审保留其绑定的旧审计器SHA。随后仅补充对已经保存的每格／全域argmax分布及主配对表的精确核对，未读成绩；[补记](paired_execution_preflight_addendum.json)保存前后SHA。实际核验版本为 `b72ed7a97c0fb5af9bc08b5c6ee194d5074e1fb7ea56c8e281223b149ec9d69f`。补记不覆盖原收据，也不改变主实验指标。

等待根任务的完整运行通知后，单次命令为：

```sh
qwen_collect_pilot/.venv/bin/python -m research_program.triadic_coordination_study.audit_paired_execution --run research_program/triadic_coordination_study/results/mean_vs_log_001
```

脚本拒绝未完成8组或覆盖已有审计目录；错误保留failure记录，不自动重试。本预审不预测训练结果、不检验目标替换的机制，也不构成语言形成证据。
