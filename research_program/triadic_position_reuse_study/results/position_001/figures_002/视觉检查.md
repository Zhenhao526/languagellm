两张实际 PNG 已逐图查看，修正版文字、矩阵数值、共用色阶、零线及脚注均可读。首版矩阵图的条件说明与纵轴标签重叠，已仅合并该标签并在本目录重新生成；首版保留在 `../figures/`。第二张 PNG 与首版字节完全相同。

全部输入 SHA 与绘图数值 SHA 未变。原实验源码和结果未改。PDF 已生成并记录 SHA，本次没有另行将 PDF 渲染为图片核验。检查为代理对实际 PNG 的视觉检查，不是独立人工双人审查。详见 `visual_review.json` 与 `figures.json`。
