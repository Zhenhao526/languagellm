# 多合法行动 × 随机重配确认实验

本包把上一轮发现的执行／选择混合拆开：在每世界恰有两个等价完整方案的任务上，比较固定需求—主体对应与每世界随机重配对应，且各自配对 `live/silent` 通信。预注册主量是重配×通信差中差的条件 `Q`（物理执行后选中任一合法方案）中心化时间 AUC。

这是新的 16 种子训练批次，完整冻结方案见 [`plan.md`](plan.md)。结果生成后应先看条件 `Q`，再看未条件化 `Q` 和物理执行率；任何结果都只支持任务协调层面的解释，不能称词义或语言起源证据。
