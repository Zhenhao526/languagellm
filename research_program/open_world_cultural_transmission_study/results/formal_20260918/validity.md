# Validity

The archived rerun uses a single frozen source/config pair. The expansion support excludes `(3,3)` from training. The transfer supports use identical scene, target, partner, message-uniform and action-uniform streams; only the fresh-role schedule differs. `expanded_single_none` has zero optimization updates and is retained as a random replacement baseline. All expansion and transfer shard audits report maximum absolute replay error 0.0; transfer audits verify constant incumbent hashes.
