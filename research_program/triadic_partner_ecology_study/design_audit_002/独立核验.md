# 设计函数独立核验

修正审核器自身的容器比较后，核验通过。第一次核验把旧JSON布局列表与源码中的等值元组直接比较，产生误报；原始失败收据和审核器保留在 design_audit_001 与 audit_design.py，第二版只将这一比较归一化，并写入新目录，没有修改 design.py。

完整21层支持由独立集合交集公式重建，unique324、多伙伴996，六个非共同目的地层被明确排除。三主体完整12需求边际两生态精确相同：每个资源类的L／R／任一目的地分别1/14、1/14、3/28。原18／6布局顺序与冻结旧准备一致，全部需求用于两种布局分区，六种归属完整覆盖。

对全部190080个世界，以各条件抽样区间中点覆盖每个世界一次；sample_indices与独立笛卡尔索引一致。另核17748个人工U边界行、6种非法U拒绝。边界按实际float64的floor(U×类别数)定义，没有声称离散浮点U是数学连续分布。纯构造未实例化任何正式seed的网络。

完整权重逐项符合1/(21×本层需求数×布局数×6)，各D质量1/21，未把原始世界等权当目标分布。unique训练34992／留出11664世界，多伙伴训练107568／留出35856世界；两生态全量共190080。每个监测分区672世界、每D32个，无重复，权重均1/672；这是固定抽样子集，不是全量人口重权重。

同U人工输入核对：八条件只要求D／布局／归属相同，同生态四条件的完整ids相同；跨生态需求和ids可以不同。监测抽样同样使用共享32个不重复物理位置及共享资源U，按D＋布局索引＋归属索引连接，672个键全部匹配。**各生态monitor_indices分别排序后，按相同行号匹配仅训练338/672、留出334/672正确；后续跨生态个体配对不得使用行号。** 同生态不同条件的排序相同，可以逐行核对。

本轮核查的是设计与准备函数；尚未验证后续runner实际是否调用正确抽样器、携带正确权重和保存配对字段。0网络请求、0模型构造、0神经前向、0训练。完整数组与来源SHA见 verification.json、audit_arrays.npz 和 prepared_inspected.json。
