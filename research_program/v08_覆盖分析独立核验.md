# v0.8 覆盖分析独立核验

状态：**脚本与合成输入核验通过，真实60组重算尚待协议结果完成。** 时间：2026-09-15T11:55:03.348379+00:00。这是事后分析审查，不是新增训练或模型实验。

被审脚本 `v08_receiver_coverage.py` SHA：`a9d744656bad7a6986a5ed722244d038a53f08fd141ad9683196a31dfa40eaa8`。独立参照与测试保存于 `v08_覆盖分析独立核验.py`。13项测试、8个参数化子案例通过；新模型调用0，原任务分析调用0。

已核对以下边界：

- **覆盖与自然使用分开。** U=0必有N=0；存在正确共同码也允许自然消息不使用它。食物和水分别能由不同码正确，不代表一条共同码可同时正确。五种覆盖类别互斥完备。
- **菜单等变须有证据。** 要求49码×2需求×720菜单的保存物理动作计数一致；即使摘要声明等价，只要逐码计数出现719/1分裂，分析也会拒绝。没有重跑神经网络菜单枚举。
- **兼容轴先核再去重。** 同地图sender_goal两行的实际投递频数、原发送频数与共同成功计数须相同。已建议并加入关闭通道时也核原发送频数，负例测试通过。
- **终点以真实世界配对。** 由实际私人菜单把编号映射为物理地点，按当次目标顺序检查同世界两次选择。反转目标顺序的合成世界重算一致，不把两目标边缘成功率相乘作为实际共同成功。
- **blocked合法域只有[0,0]。** 全49码的覆盖仅是反事实接收能力；主通道允许覆盖另算。出现非零实际投递会被拒绝，不能用从未投递的正确码夸大可用通道能力。

每个λ在留出支持上的分母分别为 **144地图／方向单元、2304个去重后的协议照片／地图／方向情境、23040个真实终点世界**。旧兼容轴的4608行只用于检查生产者旧汇总，不增加样本量。成功与覆盖率先在种子内汇总后等权平均四种子；合并失败占比是按失败数加权的描述，不能冒充四种子平均或显著性。

U与失败分解为新增事后口径。协议N来自另16验证照片对；终点J来自实际保存世界，两者不能逐照片相减。真实协议产物完成后，独立 `independent_real_recount(batch)` 将直接用保存表的集合判断和NPZ动作重算，不调用被审脚本的 `analyze()`。当前没有把未完成数据记为通过，也不据这些检查推断语言形成或唯一内部机制。
