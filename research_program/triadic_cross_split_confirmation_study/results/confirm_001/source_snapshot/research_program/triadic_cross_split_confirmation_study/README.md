# 双向跨分区语义边确认

本包在新的 16 个配对初始化上冻结 seen↔heldout 双向对象×属性边，把上一批追加的跨分区 probe 改为训练时的预先测量。任务、收益操纵和通信条件沿用收益生态×因素语义留出；每个方向有六个非空 changed-person×`{kind,length}` 层。

完整方案见 [`plan.md`](plan.md)。结果目录会保存冻结输入、六检查点的紧凑跨分区指标、训练 checkpoint、配对日志和独立重算收据。本包的行为 Q 仍是研究者定义的任务指标，不能单独宣称语言形成。
