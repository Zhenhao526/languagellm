# 随机重配策略的方向性消息探针

本包对多合法行动 × 随机重配确认批次的 64 个最终策略做后验单发送者首窗替换，检验方向性计划响应能否在随机重配训练后保持。它不重新训练，但对 live 臂重新计算第二窗口和动作，并保留 silent 闭通道参照、sham 和独立重放审计。

冻结规则见 plan.md，结果见 结果与下一步.md。该探针只测任务内方向性响应，不把整包符号对应称作词义或语言形成。
