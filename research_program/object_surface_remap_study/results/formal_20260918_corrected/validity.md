# Validity note

This is the corrected formal archive. The first diagnostic run is retained separately in `formal_20260918/`; its all-goal permutation control was a no-op because partner and goal cycled in lockstep.

The corrected stream assigns four consecutive goal values to each partner before rotating partners. A regression test verifies that the partner-wise permutation changes the goal sequence. The three shards were replay-audited against the final frozen source; all replay errors were zero.

The endpoint remains a finite tabular protocol-transfer measure. Identity versus swap tests local remapping of a frozen protocol; leave-one-out tests zero-shot recovery of a missing joint goal. Neither endpoint is a claim of human language or open-ended cultural evolution.
