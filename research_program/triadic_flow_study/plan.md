# 同背景下的首轮入向与出向消息：功能路径定位

上一轮完成首轮跨布局定向干预及独立重算，唯一主量L为负。本轮落实此前已经提出的同背景入向／出向四格对照，定位具体消息路径对合作和动作选择的作用。此问题不要求对方必然减少不兼容提议；它也不以任务受损证明消息语义。旧L及其反向结果保持原解释，不换指标重写旧结论。

## 固定支持与政策

沿用私人需求实验的四个PL_live社会59101–59104，均只取最终6000检查点。每个社会是三人、九个独立MLP；两轮同步消息，每轮4个8进制符号位置。保留对象／材料、长短、搭档、目的地、17动作、原观察和互选执行规则。0新增训练、0新增Qwen调用，不读取或改动另一任务的实验。

使用原四分区及上一轮静态定义的四世界组。组内焦点s有两种需求r=0/1，另外两人u/v按身份升序，其需求交换构成c=0/1。四世界的唯一正确搭档均含s，且改变r或改变c都使其兼容伙伴反转。所有原组和背景均保留，不按原成功、消息变化或干预结果过滤。

从此前独立核验的16份自然最终银行读取消息与行动概率，绑定原计划、结果、审计、检查点和数据哈希。不新增自然银行或新种子。

## 四格投递

对每个组×背景×情境c×焦点自身需求r，固定原世界观察、身份、公共布局、所有权及各人自身消息，设置两个二元因素：

- **I，入向替换：**将另外两人向s投递的W1，替换为同背景、另一情境`1-c`、相同r中各自生成的W1。其自身通道和互相之间的通道保留原值。
- **O，出向替换：**将s向另外两人投递的W1，替换为同背景、相同c、另一需求`1-r`中s生成的W1。s自己的通道保留原值。

I/O两组投递边不重叠，各两条；四格00、01、10、11全部计算。每格之后同步重算三人的W2，自然广播新W2，再重算三人的动作分布，共6模块样本／格。不是用供体W2覆盖接收方，也不改真实物理世界。双替换形成的是混合投递，不能称为某个完整反事实世界的自然轨迹。

完整行序为`group/background/context/self_endpoint/incoming/outgoing`。00必须复现原自然消息、贪心动作，概率容差绝对1e−13；该检查只确认实现，不是语义正对照。PL首轮消息不读取他人私人需求，入向供体对焦点的合法输入不变，出向供体对其他两人的合法输入不变；这些不变性须在静态支持与自然包中核对。

## 唯一主量与完整辅助量

令Sio为给定该格重新生成的贪心消息后，三人独立行动分布在**原接收世界**下的精确满分概率。原世界只有一个满分的执行对、地点和目的地，故S等于该对两人各自选择正确互选动作的概率之积；第三人所有动作都被边缘化，不能加上其等待概率。这里的“精确”仅针对行动分布，没有积分所有可能消息。

**唯一主量：完整双留出分区的S10−S00。** 先在每个相同组、背景、c、r内作差，再对c/r等权平均、背景与组等权平均、九个焦点×需求轴层等权平均，最后四社会等权。保留正负；负值表示这个具体入向消息替换降低原任务的满分概率。四社会原读数是独立重复，不把所有格当独立种子。

预定辅助包括：四格S，I在O=0/1下的效应，O在I=0/1下的效应，以及`S11−S10−S01+S00`；四分区和九层全部保留。交互是两项具体干预的效应，不是协同或语义机制的因果分解。本轮案例加权下的S00无需等于原均匀全世界自然成绩。

保存每格原身份下的全部17动作概率及贪心物理结算；另按主体角色`[s,u,v]`汇总伙伴、地点、物资、种类、长短、目的地的概率响应。伙伴列为`[wait,s,u,v]`，自身伙伴概率为0；地点和物资为`[wait,四个值]`，其余为`[wait,两个值]`。等待单列，不条件化删除。每个响应保留同样的四格、条件效应及交互，完整细节按实现快照记录。原世界真值、反事实标签与组索引仅用于研究者分析，不进入主体输入。

另外保留给定贪心消息的精确原生期望收益、执行对概率`[none,AB,AC,BC]`。24个合法互选事件互不相交，每个事件概率为该对两人对应动作概率之积；按对聚合、以原世界奖励加权即可得到这些量，none为1减所有事件概率。记录I/O实际改变的首轮投递符号数，各最多8个；零开关计0。符号距离只作描述，不筛例、不以损失除以字符数声称剂量可比。

## 预算、冻结与独立核验

| 分区 | 四世界组 | 背景 | 每社会全部格 | 其中00回填格 |
|---|---:|---:|---:|---:|
| 训练 | 708 | 108 | 1,223,424 | 305,856 |
| 新需求关系 | 168 | 108 | 290,304 | 72,576 |
| 新布局 | 708 | 36 | 407,808 | 101,952 |
| 双留出 | 168 | 36 | 96,768 | 24,192 |

每社会2,018,304格，四社会8,073,216格；00回填包含其中，共2,018,304格。每格6模块，正式新增48,439,296模块样本，无随机采样、无优化更新。全部16个旧自然银行只读复用，不能计为新前向。四个进程按社会并行，固定分区顺序与分块大小见机器计划。

每社会×分区保存一份完整flow数据及一份逐组指标NPZ，共32份新增NPZ。各格的语义响应可由已保存原始17动作概率、世界和主体顺序精确重建；逐组四格、差与交互另存数值数组并绑定哈希，JSON保留总体和九层。

运行前冻结方案、案例、路由、统计、必要测试及来源文件；独立审计在主运行前完成源码冻结，再对全部新增格重新生成W2与动作、重算精确S与完整响应。预计独立前向另为48,439,296模块样本。保留失败、重试和修订历史，不覆盖既有结果；此轮不根据结果追加检查点、供体或新训练。

## 可解释范围及本轮之后

I与O虽然各改两条投递边，但改变的发送者数、消息内容与下游传播不同。不能把I效应大于O直接称为信息集中化，也不能把转向唯一另一搭档解释为理解了需求。消息冲突、一般分布失配和W2补偿仍可能起作用；须结合完整动作响应才可提出下一步机制假说。

此轮结束后停止追加同类末点干预来追求正值，将资源转回一个预先固定的环境因素与完整形成轨迹。候选因素应来自信息结构或行动依赖，保留同一任务本体、配对初始化和预算，并使用所有社会及预定检查点。形成、跨情境使用与符号结构分开检验，不能靠词频、奖励或单项终点达到ICLR目标。近邻比对和具体确认设计另记录；本计划不是论文创新性声明。
