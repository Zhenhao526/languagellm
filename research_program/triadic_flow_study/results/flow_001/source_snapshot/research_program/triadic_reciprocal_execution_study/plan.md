# 完整信息下的互选执行规则：新种子配对能力实验

本计划在新模型初始化、前向或训练之前固定。上一轮冻结转录的解码结果不支持伙伴联合解码普遍改善；本轮转向合作执行机制。保留对象、长短、搭档、目的地，仍无加工和道路事件。本轮无跨人通信，不作为语言形成实验或论文确认实验。

## 条件与观察

57101–57104四个新社会初始化，每个同时训练strict和reciprocal，共8组。种子选择前对旧triadic Python源码作精确数字边界扫描，无此前使用；不读取新成绩挑种子。每组三人参数独立，两规则共享初始化、世界与符号外生抽样随机流。唯一改变为联合提议如何结算：strict要求第三人等待；reciprocal让互选且地点和目的地一致的两人执行，忽略未匹配第三人。每人仍有17行动，三人最多一对互选，不按正确答案仲裁。

所有主体使用原54维FI观察，可见全部需求和物资布局，跨人通道关闭；保留原两窗个人内部离散计算及自身消息，不清空来改变架构。每窗4位置、每位置8符号，两窗消息在本轮只参与本人的内部计算，不能当作主体间语言。模型仍为原9个MLP，参数量、初始化、优化器和路由不变。监督正确动作不进入模型或损失，所有效用仍为实际满足人数/2，取0/0.5/1。

原完整四分区固定：训练419904、新需求160704、新布局139968、双留出53568，共774144世界；需求分区3888/1488，布局18/6、归属6。训练分区抽样6000更新×batch256，另外三个分区不更新。更新0/100/500/1500/3000/6000固定监测全部需求关系×两固定背景，四分区共21504世界。完整末点只在6000步评价，不早停、不挑检查点、不追加种子、无自动延训。

## 训练目标与物理核

继续优化逐世界精确期望原生收益的log均值，加原行动熵系数β(t)=0.001×max(0,1−(t−1)/1000)，从第1步0.001线性降到第1001步0；Adam学习率0.001、梯度裁剪5。发送模块保留两个独立完整符号轨迹及留一基线，虽然跨人通信关闭，个人内部离散节点仍要训练。世界抽样与符号随机流在规则间配对，实际抽样符号可因政策改变而不同。

strict的24结构方案包含第三人等待；reciprocal每个结构事件的概率是两名执行者对应提议的概率乘积，第三人所有17行动积分为1。24事件互斥，仍包含所有4913联合提议的零收益项。唯一正确执行对/地点/目的地不变，但正确联合提议从1种增为17种。使用稳定log和正确的logit导数，不剪裁概率、加收益底数或删失败行动。纯合成穷举及有限差分在正式训练前检查，新旧strict核与原alpha=.5实现兼容。

## 指标、对照和解释

唯一主量是完整双留出6000步满分率reciprocal−strict，四个社会等权，保留所有配对差。无阈值驱动的种子删除或只报正组。能力参照为：固定实际搭档的满分上限1/3；逐世界拥有完整事实且可执行的理想策略满分1。FI的可解性不保证当前网络能学成。PL静默的19/62或1/2界不适用于FI本轮。

预定关键辅助为**2×2交叉结算**：每个政策生成的一组自然贪心提议同时按两种规则结算，所有monitor/final均保存摘要，不新增模型前向。令SS、SR、RS、RR依次表示训练规则→结算规则，则主差RR−SS等于(RR−SR)+(SR−SS)，也等于(RS−SS)+(RR−RS)。RR−SR是共同新规则下的策略差，SR−SS是旧政策的机械放行；此代数分解不是中性路径机制的因果分解。两个机械放行差对相同动作应非负。

分别记录完整成功、实际执行搭档正确、三人提议角色全对、原始联合动作/提议角色/执行角色频数、真实AB/AC/BC分层、被忽略第三人提议及全部未执行提议。候选满分可以伴随第三人仍提议，因此不能把旧三人角色全对指标换名为执行角色。

辅助内容轴为实际有一对执行且种类/长短/目的地等于唯一正确执行方案的对应属性，不额外要求执行搭档正确；未执行记0、全世界分母，另存完整物件身份正确率。它们是动作内容指标，不是词义、符号适切或组合性测量。天然期望收益、满分概率和执行概率均按本训练规则记录，不与贪心成功混用。

新规则同时改变行动等价类、第三人阻断和正反馈支持（17倍），主效应只能归于整体结算规则。静态非降路径是联合动作配置之间的单人偏离，不增加回合内顺序观察、协商或反馈。即使搭档变化，也需检验是否按需求、而非只变化身份。

## 固定预算与审计

8组×6000=48000更新，12288000训练世界样本、24576000完整符号轨迹、589824000符号抽样、221184000训练前向模块样本。48检查点、192监测NPZ、32完整末点NPZ；监测1032192世界、9289728模块，末点6193152世界、55738368模块，评价合计65028096模块。交叉结算0新前向，不增加文件数或独立样本数，所有组均无closed重复评价或逻辑别名。

prepare固定源码、预检、静态证书、原分区哈希和快照；execute拒绝覆盖。逐更新记录世界uniform/索引/状态、符号uniform和已抽样消息哈希、梯度裁剪及目标项。四个worker各跑一配对，严格配对检查后输出完整结果。失败保留，不自动新建目录重试。

独立审计另行冻结：用旧已固定的独立环境/观察/路由/前向辅助，重新实现两规则结算、精确期望、指标和主量；重建48000世界/符号随机流记录，核48检查点与192monitor统计，再独立重放全部32final共55738368模块样本。不重放优化器、全部训练梯度、随机抽样消息或中间检查点前向；不能把日志随机流核对冒称训练过程完全重放。合成预检产生的临时网络样本和更新另记，不与正式预算混合。

完成后结合共同结算下策略差、固定搭档界和全域搭档分布决定私人信息通信实验；本轮不预设阳性，不自动启动后续，不宣称已达到论文目标。0新增Qwen调用，不改另一任务的paper_program/redesign文件。
