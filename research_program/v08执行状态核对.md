# v0.8 执行状态核对

核对时刻：2026-09-15T11:44:00.919505+00:00。这是读取期间的非原子状态快照，不是终态审计。未启动模型、训练或分析命令，未改运行目录与源码。

**已有其他任务负责。** Codex 任务「明确语言起源研究问题」（`01a09dbb-97ef-77f3-9f95-7c12c4fc46d6`）在本地记录中启动相同命令，持有执行 session `13626`，随后仍检查该日志与完成数；其代理已交首个三条件开发审计并等待正式60组。当前训练为 PID `91664`。不要在此根任务重复启动、发送接管消息或打断原任务。

本次看到 19/60 份 `result.json`，另有 1 个已建目录尚未写出结果；`COMPLETE` 标志未出现。个人诊断4组全通过，启动门槛记录与原摘要SHA一致。invocation 的8份源码/数据哈希、6份源码与方案快照、4份个人控制来源均核对一致。已有 `audit_development.json` 仅覆盖最早3组社会运行，状态 incomplete、0失败，不能当作60组正式审计。

训练脚本自动完成每组训练与五模式终点评价，写 `result.json`；全部循环结束后只打印 `COMPLETE`。它**不自动调用**执行审计、协议分析或总报告。确认训练结束应同时查看持有 session 的退出码0、完整60组结果及最终 COMPLETE；没有统一 `status.json`，检查点2400也不等于最后五模式结果已经写完。

## 接续规则

- 已有 `result.json` 的组仅在预算、条件和三个主源哈希符合时跳过；已有准备权重会复用。
- 部分目录没有 `result.json` 时，`train()` 的 `mkdir(exist_ok=False)` 会报错。脚本没有从检查点或优化器恢复部分组的实现，不能把重启同命令称为自动断点续训。
- 当前PID仍在运行，维持原执行。若异常退出，保留部分结果，交原任务处理；本核对不建议删除、改名或覆盖证据。独立重跑要新目录及相应个人控制前置。

60组结束后，由原任务分别运行已有接口：

```bash
redesign_v0.3/deployment/.venv/bin/python redesign_v0.8/audit_execution.py --root redesign_v0.8/results/complementarity_001
redesign_v0.3/deployment/.venv/bin/python redesign_v0.8/analyze_protocols.py redesign_v0.8/results/complementarity_001
```

正式审计默认写 `audit_execution.json`，若已存在会拒绝覆盖，复查需另取 `--out`。协议分析可按源码、配置和权重SHA复用逐组缓存；最终需 `status=complete`、60组且无缺项。协议分析会读取保存的社会权重做前向评价，本次没有执行。

本次读取的 `analyze_complementarity.py` 尚无 main/CLI，汇总程序仍在建设；等待原任务补齐和验证后按其实际接口生成报告与四张图，不预先编造接续命令。它对 `protocol_analysis.json` 要求 complete，所以总汇总应在协议分析完成后进行。

建议本根任务只观察同一PID和最终产物，避免重复计算；待原任务交付后，再开展单独限定范围的只读复核。详细证据见 [v08执行状态核对.json](v08执行状态核对.json)。
