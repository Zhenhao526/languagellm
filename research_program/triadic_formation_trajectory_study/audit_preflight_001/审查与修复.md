# 独立审计器合成预检

本收据在主计算完成后、正式独立审计启动前写入。未读取本批正式成绩、权重或消息；没有启动正式审计、训练或真实神经网络前向。

13 项测试及 15 个子例全部通过，pytest 报告 0.26 秒。先前 11 项及 11 子例通过的记录保留在 JSON 中；最后增加两项旧接口兼容回归后再次运行本文件。

## 唯一修复

独立 `audit_execution.py` 的 `numeric_tree` 原先把 Python 布尔值视为整数，比较 `selected_position_changed` 时会调用 NumPy 布尔数组减法并抛出 TypeError。现将 bool/np.bool_ 在数值分支前按类型与值精确比较；错误布尔值和数值 1 冒充 True 均拒绝。前后 AST 仅 `numeric_tree` 改变。原文件完整副本为 `audit_execution_before.py`。

- 修复前 SHA256：`df4a9ca18ce262f492e380cc11c95a72ab72236f6df299351be171bd39ddd1af`
- 修复后 SHA256：`496a416d29ad67b8f0a454396f82d71d945ccbfad401856a2fc33826b9ba0be2`
- 测试文件 SHA256：`e11658b4ddaacb840e06987a24754d38fc58997f4d1a8fb636e56192003e07cc`

## 覆盖与边界

手写回调验证自然的 6 个发送头及可选 3 个行动头、双窗同步、完整外发包只对伙伴替换、焦点自己的 W2 保留而最终动作可经伙伴 W2 改变、silent 的零调用路径。静态索引测试含重复、端点方向及全域/局部映射；时间积分核对精确 Fraction 权重；单点、六检查点及 6×6 矩阵的汇总字段和行对照公式与主程序一致。完整 419904 行漂移测试使用人工全零数组与一个预设符号变化，未读取正式消息。

新增兼容回归实际调用未修改的旧 `p.audit_cell`，验证 live 的 W1/W2 新记录与 silent6000 的 `local554_pool` 元数据、NPZ 字段、路由及输入 SHA、原生结算和样本计数；计数篡改会拒绝。该合成案例不声称属于正式内容案例，内容聚合函数在此项中明确被 mock，其统计公式另有纯数组测试。

6000 live 锚点使用合成旧记录核验：仅三项新前向计数归零，旧 `natural_source`、路径和 SHA 等保留；新 `receiver_natural_source` 等字段不覆盖旧键。全域索引没有误作局部索引。未发现第二个 schema 阻断。

本预检不能代替正式全批重现；主代理可在此修复后的独立审计器上执行一次完整审计。未改主程序、冻结通道/数据/指标、旧源码或结果。
