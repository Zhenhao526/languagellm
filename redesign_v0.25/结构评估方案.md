# v25 辅助结构评估：固定方法与来源

本方案写于 v25 正式结果之前。仅评价 joint 条件已保存的终点协议，不训练模型，不改 v24 源码或结果，也不改 v25 主要 old18 自然双目标 J 的定义。

正式输入为四来源 33101–33104 × 三分区 × 两个发送方向的 24 份 `protocol_2400_d*.npz`，共 12 个 joint 运行。开发输入固定为 99523/p1 的两份 `protocol_0040_d*.npz`。正式输出目录 `results/joint_001`，开发目录 `results/smoke_001`。

1. **沿用评价，不重新挑结构。** 直接调用已冻结 v24 `structural_assay.py` 的 `evaluate_protocol` 与 `average_scores`。两个槽位—资源分配 food_water / water_food 都保留，不根据成绩选择、合并成最优分配或校准方向。供体只从 old18 选择，与目标同 mask、同照片对；对每目标遍历全部合法供体对，先目标内均匀平均，再目标等权。自然 J 与实验者人工供体重组 J 分别呈现。
2. **同一重编码参考。** 使用 v24 已保存的 200 个完整 49 码双射（种子 2402401），核对完整列表与哈希。先重命名两个供体的完整码，再拆数字拼接、逆映射、查原接收器动作；不重训基线。参考索引跨新旧条件和来源配对。分位数在每个索引依次平均两个通信方向、三个分区、四来源之后计算，不平均单元分位数，也不解释为独立来源置信区间。
3. **旧基线不可变。** 正式 v24 mean/attention 结构 JSON 的 SHA-256 为 `fec2d85100cfbb7428fdd3199ed5982ad9a26a01f9b7bce1471dcafec9719fbb`，其结构审计 SHA-256 为 `cc1ec67e9187d22994111ab3a827c3c460cab8763d17968b3eb7463c2df43572`。新包装器核验后直接使用既有逐来源/汇总数值；不重新推理旧主体。开发只与开发旧基线比较，不混入正式来源。
4. **比较与层级。** joint−mean 是本轮主要辅助结构比较；joint−attention 仅描述。old18/new12/common30 和 pooled/food_only/water_only 全保留。列出四来源配对差；两个发送方向和三分区是嵌套单元，不是额外独立来源。源间异质性、负结果和两种分配方向相反的结果均保留。
5. **有界独立复核。** 复用 v24 已保存测试代码中的独立 target-first 朴素循环。开发固定检查 99523/p1/d0 一份协议；正式固定检查 33101/p1/d0 一份协议。每份均覆盖全部 960 个目标、两种分配、identity 与参考列表前 3 个双射，共 7,680 个逐目标结果。与实际生产评价函数的可选逐目标输出逐项精确比较，另外核对该份保存的完整 200 参考汇总。这个局部复核不能称为对所有正式参考评分的第二次独立重算。

复用算法文件 SHA-256：`bda5004eed00c66558233658c6ed7bba06443be8853946c53cbf5e647830b4bf`；朴素循环所在测试文件 SHA-256：`76cc6023d6c4fd48750b2888b961ec359f5f512032fea7b2c089689157f96824`。v24 的 `return_details` 仅是经过来源修订记录说明的诊断接口，默认计算未改变。

人工重组使用实验者知道的资源地点筛选供体，不能替代主体自主产生新组合的成功率；超过整体重命名参考不能直接证明句法或因果发现。joint 是保留预测概念差异的任务内读出对照，也不能把更高分直接称为新的语言学习方法。

包装器：[analyze_structure.py](analyze_structure.py)。命令为 `python analyze_structure.py --out results/joint_001 --step 2400`；开发加 `--step 40 --dev`。输出 `structural_assay.json`、`结构重组评估.md`、`structural_assay_audit.json`。来源哈希、旧基线只读状态和检查范围随结果保存。
