# 非语言能力与共同符号形成：本地先导方案

> 实施状态更新（2026年9月14日）：第一轮已完成，详见[实际实验报告](/Users/xia/Documents/ChatGPT/语言/pilot_round1/第一轮实验报告.md)和[代码说明](/Users/xia/Documents/ChatGPT/语言/pilot_round1/README.md)。下文保留实施前设计记录，其中“尚未运行”等表述反映制定方案时的状态；正式轮数、评估样本量、实际资源与结果以实验报告为准。

## 已确定的研究定位

核心问题是：**哪些非语言能力已经足以支持共同符号的诞生，哪些能力会改变诞生的过程和最终结构？**

第一阶段只使用本机资源，先做规模小、可追溯、可解释的实验；出现可重复且有理论意义的现象后，再考虑加大投入。主实验主体不接受自然语言训练，不获得预设的符号词典。研究从已有有限感知和学习能力、没有共享通信约定的状态出发。

此前的环境、分工、形成与维持、代际和人机比较等想法仍保留。今后它们应服务于上述能力问题，不再并列为首期必须完成的研究目标。2026年9月14日的综合调研报告保留为文献资料；当前主问题以本文件为准。

以下模型规模、任务及记忆操纵是建议的先导方案，尚未运行，也不等于已经确认其创新性。第一轮的目标是建立可靠实验和识别值得继续研究的效应，不以完整论文结果为完成标准。

## 1. 本机条件与实施边界

2026年9月14日只读检查结果：

| 项目 | 实际情况 |
| --- | --- |
| 芯片 | Apple M5 Max，arm64 |
| 统一内存 | 48 GiB |
| GPU | 40核，系统报告支持Metal |
| 系统 | macOS 26.6.2 |
| 工作目录所在磁盘剩余空间 | 约1.1 TiB，随使用变化 |
| 已检查的Python | 系统3.9.6；Codex附带3.12.14 |
| 已检查环境中的训练框架 | 未发现PyTorch或MLX |
| GPU训练与速度 | 尚未验证，不能报告实测吞吐或训练时长 |

这台机器足以作为小网络实验的硬件起点。具体可用性仍需在项目独立虚拟环境中安装并验证训练框架。建议采用PyTorch，先完成CPU上的极小训练检查，再测完整循环在MPS上的表现；不从GPU核心数推算训练速度。官方MPS文档提供运行时可用性检查：[PyTorch MPS](https://docs.pytorch.org/docs/2.14/notes/mps.html)。

这里的“本地小模型”指在交互中学习的小型神经网络。每个agent包含视觉输入、行动选择和信号收发部分；不要求先下载一个已有语言能力的聊天模型。阶段1也不需要复现完整DINO或Dreamer训练。

建议规模：32×32像素输入，每个agent约5万至30万参数，最初2个agent，后续公共约定检查再扩为4个。参数量是设计预算，需实现后统计。单进程内存目标不超过4–8GB、首批结果文件控制在1GB以内，均是资源上限建议而非实测占用。

## 2. “足以支持”需要怎样的证据

先报告一个明确的能力组合在具体条件下能够建立共同约定。例如：当前对象辨别、从结果反馈中更新行为、选择行动，以及区分若干信号。成功可以支持这个组合在给定任务中的充分性实例，不能推出它是人类语言的最小能力集合。

从某个条件移除一项能力后实验失败，也不能立即证明该能力必要。失败可能来自学习机制、探索、预算或感知困难。尤其不要把物体隐藏后，再用无法记住物体的主体失败来证明“记忆是语言起源的必要条件”。

应分别测量三个层次：

- **通信前能力**：主体是否能在没有符号帮助时区分物体、完成基础行动，或保持过去经验。
- **形成过程**：是否出现有效信号、约定多久形成、错误和映射变化怎样发展。
- **形成结果**：表达是否稳定，是否依赖某个方向或伙伴，是否适用于新外观；扩展任务后才研究结构复用。

## 3. 第一项能力：显式短时交互记忆

建议第一轮只操纵能否在作出决定时读取最近若干次自身经历。选择它的理由是：容易在同一模型、同一任务中改变，不必同时增加世界模型、规划和工具使用训练；并且可以让当前任务在没有显式历史时仍然可解。

这里的能力是一般的经历保持和使用。记忆以非语言的观察、信号编号、行动和回报记录存在，不提供自然语言总结，不自动生成或维护语义词典。

| 条件 | 当前决策可用的输入 | 保留的学习能力 |
| --- | --- | --- |
| H0：没有显式历史输入 | 当前观察、当前收到的信号；历史槽位统一遮蔽 | 正常更新参数，长期经验仍能进入权重 |
| H1：可读取真实历史 | 与H0相同的当前输入，加最近4次自身完整交互记录 | 与H0相同的参数更新机制 |

两组使用相同网络结构、输入槽位、参数量、交互预算和反馈规则。相同重复编号的两条件使用配对的初始化与环境序列；同一群体内部的agent保持独立参数和私人经历。

**H0不能称为“没有记忆”**：模型权重也保存学习结果。此处只检验显式短时历史能否提供额外作用。H1对历史的再次访问本身是增加了可用信息；即使产生效应，也不能直接解释为某一种人类工作记忆机制。

若初步效应稳定，可加一个低成本诊断：保留各次完整历史记录，但打乱它们的时间顺序。这检验时间顺序是否有额外价值。不要把信号与结果错配后产生的损害，简单当作“无记忆”的效果。

记忆和说听角色已经被语言演化研究提出为重要候选因素；这一操纵本身不是创新声明。[Galke等，2022](https://arxiv.org/abs/2204.10590)是方法性论述，LLM记忆研究也是需要比较的近邻：[Talebirad等，2026](https://arxiv.org/abs/2607.00233)。首轮先判断是否存在值得进一步解释的具体效应。

## 4. 极小任务：从可分辨物体到双人约定

环境生成四类容易分辨的简单物体。输入只有图像，模型看不到类别名称、文字、目标编号或研究日志。位置、候选排列和无关外观变化独立随机化。研究者知道环境类别，用于生成任务及事后分析；这意味着第一阶段研究的是已有可分辨类别上的符号形成，不是概念本身从零出现。

先做不含通信的个体视觉匹配练习：同一agent能看见目标以及四个候选，通过选择和反馈学会找出对应物体。目标和正确候选采用独立渲染，避免只匹配完全相同的像素。各agent独立获得相同预算的练习，没有符号消息，没有任何预置名称。

这个准备过程提供很有限的世界经验：对象辨别与选择。它不等于已经理解物理因果、他者意图或生存任务。视觉预训练会塑造后来的表达，所以必须记录和控制；已有直接证据见[Ohmer等，2022](https://doi.org/10.1371/journal.pcbi.1010658)。

通信阶段的每轮过程：

1. A私下看到一个目标物体，B看到四个候选，其中包括对应物体；候选位置随机变化，A看不到排列。
2. A可以发出一个离散信号，也可以保持沉默。B收到该信号后选择候选。
3. 选择正确时双方获得相同正回报，否则得到失败回报。默认不向B揭示真实目标，不展示正确答案标签。
4. 下一轮重新采样。A、B随机轮换发送者和接收者角色，两条件采用同一日程。

给定三个主动信号加沉默，共四个可区分消息状态；每轮恰好一个固定消息时段。沉默也可能获得约定意义，因此它计入通道容量，不再暗中增加一个第五符号。首轮不设额外通信成本。

训练与测试的目标均逐轮独立、均匀、有放回采样，并与位置独立；不采用类别轮换或小块严格配平，以免历史帮助预测下一轮。没有有效消息时，B的最优期望成功率为25%。这个上限来自本任务的对称构造，需在实现中通过数据检查和无通信实验确认。

所有显示字符只是信号编号的可视化。两agent只依据各自允许的观察、信号和反馈更新；没有跨agent隐状态共享、预置词典或穿过消息通道传递的梯度。通信阶段不得把隐藏的正确候选索引用于接收者的交叉熵或辅助监督；索引只供环境计算回报及离线分析。完整世界日志仅用于实验分析。

## 5. 校准与必要对照

**完整信息校准。**在不含通信的副本上，让接收者直接看见目标图像，检验各条件的当前视觉与选择能力。若这一步都做不好，优先处理感知和任务学习问题。校准用独立模型副本，不把可能获得的目标协议带回主实验。

**固定信号来源对照。**用一个一致的随机映射产生信号，让接收者学习它。它用于区分一般关联学习的难度与双方同时改变策略的难度，不代表自发语言形成。若H1在固定来源和共同学习中同样提速，应优先考虑一般学习解释。

**无通信条件。**消息恒定，并固定发送时刻；候选位置等额外路径不携带目标信息。除直接检验25%基线外，还检查数据顺序、随机数和日志有没有暴露答案。

**消息干预。**对训练快照替换消息，观察B的选择是否按已估计的对应关系变化。采用本来就出现过的消息和匹配情境，避免只凭陌生输入造成的成绩下降认定语义作用。

**私人历史检查。**记忆只能包含该agent实际看见或做过的内容。失败后未揭示的目标不能进入B的历史。成功选择让B间接知道目标，属于反馈自然提供的信息，需要如实说明。

## 6. 第一轮记录与判断标准

主要记录：独立群体的成功率轨迹、首次达到稳定标准的交互数、消息使用及更替、条件化的消息—对象关系，以及消息干预结果。保存随机种子、输入权限、实际反馈、配置、少量检查点和可回放的交互日志。

先导可暂用这样的稳定标准：连续三个预定检查点，在新渲染和新排列的测试中成功率达到90%，且消息具有可复现的行为作用。每个检查点可用200个独立均匀采样试次，并另外报告各类别表现。阈值是工程及先导操作定义，不是“语言已经诞生”的普遍判据；正式检验前再依据先导方差制定样本与统计计划。

测试使用训练快照的副本，关闭参数更新。主要测已形成的协议：一项测试固定保留训练末的真实历史，另一项将历史清空并持续遮蔽；两者测试中都不新增历史、不提供用于适应的反馈，也不写回训练群体。若另设允许历史滚动的测试，应单列为短时适应能力，不能与固定协议测试混合。若高分只存在于最近经历可用时，应称为依赖历史的交流策略，不能直接说固定词汇已稳定。

消息映射的稳定性须在同一运行内持续追踪，不能每个时间窗口重新重排符号而掩盖漂移。还须区分两个方向各自使用一套代码，与双方共用同一套映射。双人协议只支持双人层面的结论；之后扩为4人并改变配对，才能进一步研究公共一致性。

这一步只有四种消息状态、四类对象，不支持关于句法或组合性的主张。可观察的“最终特点”主要是映射稳定、方向依赖、历史依赖和对外观变化的泛化。频率分布、TopSim或字符串复杂度不作为第一轮主要指标。

## 7. 竞争结果及其含义

| 观察结果 | 可支持的解释 | 尚不能支持的解释 |
| --- | --- | --- |
| H0、H1都形成约定 | 基础感知、反馈学习和行动选择在该任务中已经足够；显式历史不是这里的必需条件 | 不需要任何记忆；已经找到人类语言的最小心智 |
| H1更快或更稳定 | 显式历史改变了给定条件下的形成过程 | 记忆是所有语言的必要条件；效应一定来自社会认知 |
| 只有H1在预算内成功 | 在所用学习机制与预算下，访问历史有帮助 | H0永远无法形成；缺少该能力就不可能出现语言 |
| 两组都失败、校准成功 | 需要检查探索、共同学习和信号利用机制 | 当前非语言主体原则上不能创造符号 |
| 历史只提高固定来源学习 | 一般关联学习或样本利用可以解释主要收益 | 已识别出共同约定独有的形成机制 |

先导每条件只做少量独立重复，用于发现故障与估计方差。不能把同一群体的数千轮互动当成数千个独立样本。未在预算内形成的运行保留为未完成事件，不能从平均形成时间中删除。

## 8. 控制第一批投入

先完成一个可回放的极小循环，检查数据权限、奖励、训练方向和消息干预。随后用最多5分钟的短跑测量CPU与MPS上的实际交互速度、内存及数值行为；测试完整流程，不只测矩阵乘法。

首批建议总训练墙钟预算30–60分钟，优先H0/H1各3个独立群体；固定来源及无通信先用更小规模校准。具体每组交互数由短跑速度确定，在看到组间效果前固定。所有主条件保持相同交互预算；若触及运行上限，应一起缩短本批比较范围，并报告截断。这里没有承诺在一小时内一定出现可解释结果。

初期只保留起始、形成附近和终点等少量检查点。图像由种子重建，不把每一帧单独存盘。记录GPU或CPU实际后端及算子回退，不把静默回退当作MPS性能。

如果校准失败，先修正基础环节；如果形成不稳定，先检查学习与探索；如果形成稳定但记忆无明显作用，也保留该阴性结果，随后选择另一个有理论依据的能力，而不是任意扩大参数搜索。

## 9. 何时进入下一步

第一轮的直接产物应是：本机可重复运行的实验、真实资源消耗记录、若干完整形成轨迹，以及至少一次能够解释的消息干预。单次成功或者四个暗号本身不作为值得扩资的创新证据。

值得继续投入的信号是：跨独立重复的能力效应；它不只是基础感知或固定映射学习的差异；并能提出下一项可反证预测。例如，显式历史对多人伙伴变化尤其重要，或它促进快速适应却降低长期公共一致性。这些是待检验假设，不是预期必然结果。

后续能力按问题逐项加入：

| 后续能力 | 适合追问的内容 | 需要避免的混淆 |
| --- | --- | --- |
| 视觉抽象与非语言经验 | 符号是否跨外观表达同类对象，经验怎样影响类别边界 | 只是对象更容易分辨或训练样本更多 |
| 动作结果预测 | 语言是否更早表达用途、功能或行动后果 | 把新增预测任务、额外训练直接称为因果理解 |
| 伙伴行为适应 | 能否根据伙伴经历调整表达和修复误解 | 直接提供伙伴隐状态或“他者意图”标签 |
| 事件顺序保持 | 是否出现顺序与关系表达 | 没有记忆的主体连基础动作任务都无法完成 |

形成与维持条件的比较、代际替换和人类对照，都可以在能力效应明确后加入。环境和任务变化用于检验这些能力为什么有作用。

## 10. 下一次实施的具体交付

建立项目独立环境；实现像素匹配准备任务与双人通信任务；运行校准及H0/H1短跑；输出学习轨迹、消息—对象关系、干预结果和本机资源记录。先导配置的每次修改均保留版本，避免根据结果反复调整“形成”的定义。

目前完成的是硬件与环境核查，以及上述实验设计；尚未安装训练框架、下载模型权重或运行训练。下一步实施可以直接按本文件推进，具体模型和轮数由小规模校准落实。
