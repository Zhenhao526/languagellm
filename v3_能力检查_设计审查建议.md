# v3能力检查：设计审查建议

审查时间：2026-09-15 15:13，Asia/Shanghai。只读旧实现、版本记录和本机资源；未运行模型，未修改v2。以下是v3冻结前的设计建议，不是已验证的v3实现或能力结果。

**取消加工前置步骤和道路事件，保留物品属性、双人搭档及交付地点，适合作为下一轮能力诊断。建议先用两个固定场景检查完整信息能力，再检查局部观察下的自然语言合作；四场全部实际成功后，才进入符号试跑。通过只表示满足这个最低执行门槛，不保证稳定能力或语言形成。**

## 场景与门槛

若v3沿用v2的variant公式和`seed=6101+47×variant`，两场景如下；最终应以v3生成的世界清单重新核对，不能只锁定variant编号。

| 场景 | 木材目标 | 纤维目标 | 初始位置 |
|---|---|---|---|
| variant0，seed6101 | 干长木1件，从林地送营地 | 干长纤维1件，从河滩送营地 | A营地、B河滩、C林地 |
| variant22，seed7135 | 湿长木1件，从林地送河滩 | 干短纤维2件，从河滩送林地 | B营地、A河滩、C林地 |

两场目标木材都必须由两人搬运，且目标地点不同于其起始地点。因此满分不能仅靠单人纤维运输达到，应同时核对实际成功的双人搬运和交付记录。成功判据为截止前所有目标单位被环境接收、score=1及success=true；模型自述完成不算。失败的共同动作按伙伴、实体、动作种类和目的地检查，不能仅据编号可选认定可成功。

variant22同时改变木材干湿、纤维长短、纤维数量、目的地和初始角色位置。这是覆盖不同需求的能力检查，不是属性或目的地的单因素实验，也不能将两场成绩差异归因于某一个变化。若只想隔离属性／目的地，应在冻结前另行固定相同纤维数量；不要看到结果后再简化第二场。

冻结前以无模型的执行器检查确认：加工和道路事件已从规则、动作、观察及状态转移中一致移除；目标长木确实需要双人；两个具体场景存在预算内解；可见属性及物品数量没有直接暴露隐藏目标；同一物体的私有句柄在结算时正确归并。场景清单、源码哈希、模型版本、生成预算、成功判据及停止规则应一起保存。

## 最小运行安排

推荐保留12步上限和原来每步四个广播窗口、每决定两次生成，以免同时改变时限及计算机会。按固定顺序运行：

1. 完整信息：场景0、场景22。两场均成功才进入下一阶段。
2. 局部自然语言：同样两场。两场均成功才进入符号试跑。

每场三份history从空列表开始，不导入v2经历、前一场目标、能力控制分析或其他条件计划。不同条件可使用相同的固定模型权重，但必须使用独立输入历史和新推理缓存。fresh histories用于检查各场独立的任务能力；它不测跨场语言学习，也不删除模型预训练知识。

为保留最小的跨场景诊断，建议同一阶段即使第一场失败，也完成预先选定的第二场；该阶段任一失败后停止后续阶段，不运行符号、不追加重复或继续调提示。若更重视比较完整／局部信息的失败类型，也可事先决定无论完整信息结果如何都完成四场，但这应在首场结果出现前定案。两种停止策略不能临时按结果切换。

最多四场×12步×3主体×5次决定×2次生成=**1440次模型调用**；完整信息阶段失败时，分阶段方案最多720次。依据v2两个完整规则自然语言控制约34分钟／场的实测，上限可暂按约2.3小时准备，第一阶段约1.1小时；简化任务及提前成功可能缩短，不能承诺具体耗时。只有4个场景—条件组合，没有条件内独立重复，这是可承受的筛查规模，不足以估计稳定成功率。

任务错误和程序错误需区分：能力失败按预定规则停止并保留全部记录；若出现可证实的执行器故障，也保留本轮并终止，修复后用新版本、新目录重新开始，不把修复前后结果合成一次成功的运行。

## 解释边界与本机条件

这轮同时删除加工和道路事件。即使v3成功、v2失败，也不能单独认定是哪一个删减起作用；提示长度和行动依赖也会随之变化。完整信息与局部自然语言又可能同时改变可见地点和交付进度，区别应明确列出。全场通过不证明符号消息有意义，更不证明语言已经产生；后续仍需独立的伙伴理解、跨情境使用和消息干预证据。

截至审查时，本机显示AC供电、电池100%；物理内存48GiB，`memory_pressure -Q`报告系统空闲比例71%，可用磁盘约1.1TiB。未发现命令行含qwen、mlx_lm、Ollama、llama-server或vLLM的模型进程；这是当时的进程名检查，不保证之后不会启动其他负载。

可用模型目录为`/Users/xia/Documents/ChatGPT/语言/qwen_collect_pilot/models/Qwen3.5-9B-8bit`，虚拟环境Python存在；模型清单revision为`16daa4818c54ce5f5436f929d52542eb65bbed9d`。v2实测MLX峰值约10.93GB，说明此配置此前能运行；不保证v3全部资源需求相同。本次未重新计算大权重哈希，也未试载模型。建议继续单模型顺序执行，在启动前重新确认供电，避免同时加载多个实例。
