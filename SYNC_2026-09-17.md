# GitHub 同步与本地清理记录 — 2026-09-17

本次同步目标仓库为 [`Zhenhao526/languagellm`](https://github.com/Zhenhao526/languagellmllm)。提交内容以源代码、冻结方案、紧凑结果、审计收据和研究报告为主；完整训练日志、checkpoint 和合并结果文件已在远端提交验证后从本机删除。

## 已同步的研究记录

- `research_program/tabular_signaling_study/`：无语言先验的 sender/worker 正对照；`results/formal_20260917/` 中保留 144-run 的紧凑轨迹、training-support/held-out 汇总、冻结 JSON、源数据哈希和三份独立审计。
- `research_program/temporal_scarcity_study/`：128-run 的 neural temporal-scarcity 矩阵，保留分析、审计和结果报告。
- `research_program/compositional_signaling_study/`：8 seeds × 10 conditions = 80 个 300-update 筛查 run，并在同一冻结设计下完成 80 个 3000-update 完整 run，比较 `mono4` 与 `dual2` 消息形式，包含 live/silent、permuted 和槽位重组诊断；完整批次的紧凑汇总、审计、冻结配置和收据保留在 `results/formal_20260917/`。
- `research_program/action_dependent_signaling_study/`：32 seeds × 8 conditions = 256 个 3000-update run，比较消息同时到达与按子任务分阶段到达；`results/formal_20260917/` 保留紧凑汇总、seed-level equilibrium 分析、冻结配置和独立审计。
- `research_program/generation_transmission_study/`：8 个 parent run、48 个 child run，替换 sender 或 worker，比较 live/silent/scrambled 的新成员恢复；`results/formal_20260917/` 保留紧凑汇总、学习曲线指标、冻结配置和独立审计。
- `research_program/generation_compositional_transmission_study/`：32 个 staged dual2 parent、192 个 child run，分别替换 worker 或 sender，分层记录组合 parent 的协议传递；`results/formal_20260917/` 保留紧凑汇总、分层分析、冻结配置和独立审计。
- `research_program/heldout_composition_study/`：32 seeds × full/leave-one-out × live/silent = 128 个 child run，留出一个目标组合检验零样本恢复；`results/formal_20260917/` 保留紧凑汇总、parent 分层、冻结配置和独立审计。
- `research_program/receiver_representation_study/`：32 seeds × 2 表示 × full/leave-one-out × live/silent = 256 个 child run，加入 full-support 对照并按 parent 可组合性分层；`results/formal_20260917/` 保留紧凑汇总、parent 重建哈希、冻结配置和独立审计。
- `research_program/population_signaling_study/`：fixed/rotating partner、hidden/visible sender identity、scarce/abundant capacity、live/silent 的 128-run 正式矩阵；`results/formal_20260917/` 中保留紧凑轨迹、最终汇总、codebook readout、冻结方案和独立审计。
- `研究报告_共同符号形成机制_2026-09-17.md`：把 tabular 正对照、neural 负边界和 population-level common-code 结果放入同一机制框架，并明确当前仍不是人类语言起源的完整模型。
- 三份 population 失效收据：记录地点观测不匹配、随机流未配对、visible 初始化混淆；这些运行不进入任何统计。

## 正式结果冻结

- tabular：8 seeds，144 个执行 run；独立审计的三部分分别为 64、64、16 个 run，最大回放误差均为 0。
- temporal：8 seeds × 16 conditions = 128 runs，256,000 条训练日志，512 个 checkpoint，最大回放误差为 0。
- population：8 seeds × 16 conditions = 128 runs，384,000 条训练日志，128 个终点 checkpoint，960,000 对 live/silent 轨迹，最大回放误差为 0。
- population rotating-hidden＋abundant：held-out natural `0.800`，from-scratch silent `0.299`，paired live−silent `+50.05 pp`，sender token agreement `1.000`，semantic success `1.000`。
- population rotating-visible＋abundant：paired live−silent `+45.60 pp`，sender token agreement `0.516`，semantic success `1.000`。
- compositional：80 runs，240,000 条训练日志，80 个终点 checkpoint，最大回放误差为 0；完整训练后 `dual2` 仍没有稳定槽位重组。
- action-dependent：256 runs，768,000 条训练日志，256 个终点 checkpoint，最大回放误差为 0；staged 可组合平衡为 factorized `9/32`、entangled `10/32`，simultaneous 分别为 `3/32`、`0/32`。
- generation transmission：8 个 parent、48 个 child，168,000 条训练日志，56 个终点 checkpoint，最大回放误差为 0；新 worker 的 live−silent 为 `+59.8 pp`，新 sender 为 `+48.4 pp`。
- generation compositional transmission：192 个 child，576,000 条训练日志，192 个终点 checkpoint，384,000 行配对通道轨迹，最大回放误差为 0；9 个可组合 parent 中新 worker 和新 sender 均为 9/9 可组合，非可组合 parent 为 0/23。
- held-out composition：128 个 child，384,000 条训练日志，512 个终点 checkpoint，192,000 行配对通道轨迹，最大回放误差为 0；可组合 parent 分层的 leave-one-out live 留出表现为 `0.263`，0/9 达到 `0.60`，总体 full−leave-one-out 差值为 `+0.293`。
- receiver representation：256 个 child，768,000 条训练日志，1,024 个终点 checkpoint，384,000 行配对通道轨迹，最大回放误差为 0；在可组合 parent 的 leave-one-out 分层中，joint-history 为 `0/9`、slot-local 为 `9/9` 达到 `0.60`，非可组合 parent 两种表示均为 `0/23`。

远端验证提交：`7fdfa67cdd0e4f7e590633ed60848b2d095de4ce`。本地清理收据见 `LOCAL_CLEANUP_RECEIPT_2026-09-17.json`。

## 大文件策略

远端不保存每个 run 的完整 `training.jsonl`、`checkpoint_*.npz` 或 329 MB 的合并 `execution/results.json`。这些文件只用于本轮审计和结果生成；远端提交通过独立校验后，已从本机删除。删除后本地保留源代码、冻结方案、紧凑结果、哈希、审计、失效收据和报告，今后可按冻结方案重新生成原始过程文件。
