# GitHub 同步与本地清理记录 — 2026-09-17

本次同步目标仓库为 [`Zhenhao526/languagellm`](https://github.com/Zhenhao526/languagellm)。提交内容以源代码、冻结方案、紧凑结果、审计收据和研究报告为主；完整训练日志、checkpoint 和合并结果文件已在远端提交验证后从本机删除。

## 已同步的研究记录

- `research_program/tabular_signaling_study/`：无语言先验的 sender/worker 正对照；`results/formal_20260917/` 中保留 144-run 的紧凑轨迹、training-support/held-out 汇总、冻结 JSON、源数据哈希和三份独立审计。
- `research_program/temporal_scarcity_study/`：128-run 的 neural temporal-scarcity 矩阵，保留分析、审计和结果报告。
- `research_program/compositional_signaling_study/`：8 seeds × 10 conditions = 80 个筛查性 run，比较 `mono4` 与 `dual2` 消息形式，包含 live/silent、permuted 和槽位重组诊断；`results/formal_20260917/` 保留紧凑汇总、审计、冻结配置和收据。该批次使用 300 次更新，是设计默认 3000 次更新前的筛查性预实验。
- `research_program/population_signaling_study/`：fixed/rotating partner、hidden/visible sender identity、scarce/abundant capacity、live/silent 的 128-run 正式矩阵；`results/formal_20260917/` 中保留紧凑轨迹、最终汇总、codebook readout、冻结方案和独立审计。
- `研究报告_共同符号形成机制_2026-09-17.md`：把 tabular 正对照、neural 负边界和 population-level common-code 结果放入同一机制框架，并明确当前仍不是人类语言起源的完整模型。
- 三份 population 失效收据：记录地点观测不匹配、随机流未配对、visible 初始化混淆；这些运行不进入任何统计。

## 正式结果冻结

- tabular：8 seeds，144 个执行 run；独立审计的三部分分别为 64、64、16 个 run，最大回放误差均为 0。
- temporal：8 seeds × 16 conditions = 128 runs，256,000 条训练日志，512 个 checkpoint，最大回放误差为 0。
- population：8 seeds × 16 conditions = 128 runs，384,000 条训练日志，128 个终点 checkpoint，960,000 对 live/silent 轨迹，最大回放误差为 0。
- population rotating-hidden＋abundant：held-out natural `0.800`，from-scratch silent `0.299`，paired live−silent `+50.05 pp`，sender token agreement `1.000`，semantic success `1.000`。
- population rotating-visible＋abundant：paired live−silent `+45.60 pp`，sender token agreement `0.516`，semantic success `1.000`。
- compositional：80 runs，24,000 条训练日志，80 个终点 checkpoint，最大回放误差为 0；轮换隐藏 factorized 条件下 `dual2` 的 natural−silent 为 `+6.17 pp`（95% t 区间约 `[-0.02,+12.36]`），槽位重组相对 natural 为 `−4.84 pp`，暂未观察到可迁移的组合性收益。

远端验证提交：`281611ab61921e1da81b8848fb6d6edf94145555`。本地清理收据见 `LOCAL_CLEANUP_RECEIPT_2026-09-17.json`。

## 大文件策略

远端不保存每个 run 的完整 `training.jsonl`、`checkpoint_*.npz` 或 329 MB 的合并 `execution/results.json`。这些文件只用于本轮审计和结果生成；远端提交通过独立校验后，已从本机删除。删除后本地保留源代码、冻结方案、紧凑结果、哈希、审计、失效收据和报告，今后可按冻结方案重新生成原始过程文件。
