# GitHub 同步与本地清理记录 — 2026-09-17

本轮同步目标仓库为 [`Zhenhao526/language`](https://github.com/Zhenhao526/language)。远端保留源代码、冻结方案、紧凑结果、审计收据和研究报告；完整训练日志、逐 run checkpoint、合并执行结果和临时环境只在审计期间使用。远端提交核验完成后，原始过程文件从本机删除。

## 本轮新增归档

- `research_program/multi_generation_transmission_study/`：9 个已组合 parent、两种替换顺序、两代 live/silent 链，共 108 个阶段；检验已形成的组合协议能否连续传给新 worker 和新 sender。
- `research_program/multi_generation_chain_study/`：32 个 parent、三代替换事件、`joint_history`/`slot_local` 两种接收表示，以及每代严格 leave-one-out 目标组合，共 128 条 chain、384 个 event；检验组合泛化与代际传递的联合稳定性。
- `research_program/noise_repair_study/`：9 个可组合 parent、0–40% 二值 slot 翻转、`worker_only`/`coadapt` 两种适应方式和两种接收表示，共 144 个 run；区分旧协议维持、sender–receiver 修复和性能下降。
- `research_program/ternary_composition_study/`：三值对象和属性、等容量 `mono9`/`tri3`、factorized/entangled 目标、simultaneous/staged 时序，共 144 个 run；检验消息容量与状态空间扩展对组合协议的影响。

每个新增包的 `results/formal_20260917/` 只保留 `compact_results.json`、汇总、冻结 JSON、独立审计和哈希收据。新增正式批次均通过回放审计，最大误差为 0：

| 归档 | runs/chains | 训练日志行数 | 配对轨迹行数 | 关键收据哈希 |
|---|---:|---:|---:|---|
| two-generation transmission | 108 stages | 324,000 | 108,000 | `receipt.json` |
| three-generation chain | 128 chains / 384 events | 1,152,000 | 576,000 | `receipt.json` |
| noise repair | 144 | 432,000 | 432,000 | `receipt.json` |
| ternary composition | 144 | 432,000 | 216,000 | `receipt.json` |

## 报告同步

`研究报告_共同符号形成机制_2026-09-17.md` 已加入三代严格留出链、信道噪声和三值容量扩展的设计、结果与限制。报告保留因果解释边界：当前主体是无语言先验的离散 tabular agent，结果说明任务价值、行动窗口、接收记忆和伙伴结构如何影响共同符号协议；它们还不是人类语言起源的完整模型。

已有的 tabular、时间稀缺、组合通信、行动依赖、代际传递、留出组合、接收者表示和 population 包继续保留。远端历史目录不做覆盖或删除。

## 大文件与本地保留策略

远端不提交逐 run `training.jsonl`、`checkpoint_*.npz`、模型权重、原始视觉数据、文献 PDF 或运行环境。它们不属于可审阅的最小复现实验包；紧凑结果中保留终点指标、稀疏轨迹、随机流/源文件哈希、冻结配置和审计收据。远端提交并通过完整性检查后，本机删除本轮和前几轮遗留的原始执行树与临时准备目录；工作区仅保留源代码、冻结方案、紧凑结果、审计记录、报告和本地 NumPy 运行环境。

清理详情见 `LOCAL_CLEANUP_RECEIPT_2026-09-17.json` 和 `STORAGE_MANIFEST.json`。这两份文件在远端提交核验后写入实际 commit、删除路径、字节数和保留路径清单。
