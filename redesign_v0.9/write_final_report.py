"""Assemble the completed adaptation report without changing primary result files."""
from pathlib import Path
import json
import numpy as np

ROOT=Path(__file__).resolve().parent
OUT=ROOT/'results/adaptation_001'
a=json.loads((OUT/'adaptation_analysis.json').read_text())
p=json.loads((OUT/'protocol_analysis.json').read_text())
audit=json.loads((OUT/'audit_execution.json').read_text())
replay=json.loads((OUT/'protocol_execution_audit.json').read_text())
assert a['status']=='complete' and p['complete'] and audit['passed'] and replay['passed']
arms=['sender_only','receiver_only','both']
names={'baseline':'适应前','sender_only':'仅发送端学习','receiver_only':'仅接收端学习','both':'双方学习'}
pct=lambda v:f'{100*v:.2f}%'
pp=lambda v:f'{100*v:+.2f}'
metrics={arm:a['arms'][arm]['mean_metrics'] for arm in arms}
s=p['summary']['aggregate']
lines=['# 新资源组合加入后，共同符号约定如何扩展','',
       'v0.9 本地机制实验完整报告 · 2026年9月15日','',
       '在已有24种资源组合的基础上加入其余6种组合，双方继续学习取得了更好的适应：新增组合的同消息双目标成功率从7.19%升至58.86%，仅发送端学习为38.70%，仅接收端学习为10.79%。双方学习在四个种子的终点和过程指标上均优于两种单端学习，旧组合的宏平均成绩约保持在93%。',
       '', '协议枚举进一步表明，发送端学习能利用已有但未自然用好的完整码，双方学习还能扩展接收者可正确解读的组合范围。任务适应与片段复用的改善并不相同。本轮研究已有约定的扩展；新增组合已经参加训练，尚不能据此宣称零样本组合语言或人类语言起源得到了模拟。','',
       '## 研究问题和固定设计','',
       '同一条两个整数符号的消息，需要支持食物和水两次独立资源选择。两次选择没有中间反馈。场景包含6个固定地点，每张地图中两种资源位于不同地点，共30种组合。发送者看到照片构成的资源地图；接收者只有实际消息、自身资源需求与合法动作菜单。训练仅依据共同标量资源回报，没有目标消息、协议表或地图标签监督。',
       '', '本轮保留官方DINOv2 ViT-L/14冻结视觉特征、独立个人投影及原行动/通信接口，沿用旧44张训练照片和16张测试照片。视觉主干本身没有执行通信；社会行为来自在其表示之上学习的接口，因此这里也不检验完全没有任何先验能力的主体。',
       '', '完整使用v0.8混合收益的四个来源种子27101–27104、三个平衡划分，共12个双主体来源。每个来源复制为三臂，36次适应运行，无挑选成功者。新旧30图从第一步起均匀训练，期望旧图80%、新图20%；每臂600次更新×512个世界，两个方向各占一半。各臂同源权重、同外生世界和抽样随机流，均重置Adam、学习率0.0007、前500步熵0.02、后100步为0，并按发送/接收模块分别裁剪梯度范数至2。',
       '', '发送端包含视觉槽编码、现场状态编码、消息生成及其价值函数，每人198,234个可学习参数；接收端包含消息嵌入、需求动作选择及其价值函数，11,741个参数；双方为209,975。不同臂的可训练参数量不同。冻结参数的政策在训练时仍抽样，主评价取贪心动作。两端干预都不能解释为只改变一个纯语言认知模块。',
       '', '[训练前固定方案](../../固定执行方案.md)、[适应设计审查](../../适应实验审查.md)、[协议追踪方案](../../协议追踪方案.md)。初始容量与通道候选未执行。','',
       '## 适应成绩与过程','',
       '主评价每次9600世界，地图、查询顺序和两个方向均衡；0、25、50、100、200、400、500、600检查点使用同一组世界。AUC是这些检查点的梯形面积除以600。先在每个种子内平均三个划分与两个方向，再等权汇总四个继承种子；36次运行不等于36个独立训练种子。','',
       '| 学习范围 | 新组合起点 | 新组合终点 | 新组合AUC | 旧组合终点 | 旧组合变化 |',
       '| --- | ---: | ---: | ---: | ---: | ---: |']
for arm in arms:
    r=metrics[arm]
    lines.append(f"| {names[arm]} | {pct(r['new_initial_j'])} | {pct(r['new_final_j'])} | {pct(r['new_auc_j'])} | {pct(r['old_final_j'])} | {pp(r['old_change_j'])}个百分点 |")
lines+=['','双方学习相对仅发送端，新组合终点提高20.16个百分点，四种子差依次为+22.22、+19.44、+27.78、+11.22；相对仅接收端提高48.07个百分点，四种子均提高。过程AUC的对应差也均为正。该一致方向是四个来源上的探索性证据，不作大样本显著性或总体精度推断。',
        '', '旧组合宏平均基本保持不意味着每个案例都未遗忘。例如种子27104的双方学习旧组合下降0.09个百分点，仅发送端下降0.35个百分点，完整负变化均保留。',
        '', f"![新旧组合的学习曲线]({OUT/'figures/01_learning_curves.png'})",'',
        '| 学习范围 | 新组合正常消息 | 打乱完整消息 | 常量消息 |',
        '| --- | ---: | ---: | ---: |']
for arm in arms:
    r=metrics[arm];lines.append(f"| {names[arm]} | {pct(r['new_final_j'])} | {pct(r['new_shuffle_j'])} | {pct(r['new_blank_j'])} |")
lines+=['', '打乱或替换消息后成绩大幅下降，说明新任务成绩依赖场景相关的消息。该干预本身不证明消息内部具有组合结构。随机政策、清空现场表示、旧图和全图的全部控制结果见[逐种子分析](适应实验分析草稿.md)和[机器可读汇总](adaptation_analysis.json)。','',
        '## 接收覆盖与新旧消息冲突','',
        '适应前的事后诊断先枚举49条完整消息，检查每张地图是否存在能让固定接收者完成两个目标的消息，得到接收可达率C_R。另固定自然发送函数，允许分析者任意重新解释完整码，得到最优解码上界C_S；该上界使用评价地图真值，不是实际训练过程。',
        '', '原协议的新图C_R为43.75%，自然双成功仅6.25%。新图单独C_S虽为98.61%，全30图C_S却只有77.71%；若要求旧图正确总数不下降，新图最优重赋义上界只有16.58%。新旧地图使用相同完整码，会限制只改接收端时的兼容空间。保持正确总数允许个别旧例互相替换，不等于逐例无遗忘。',
        '', '以下采用固定16对验证照片的协议枚举，区别于上文9600世界抽样，不跨口径直接计算变化。','',
        '| 阶段 | 新图自然双成功 | 新图接收可达C_R | 可达但未自然完成 | 全图发送最优解码C_S |',
        '| --- | ---: | ---: | ---: | ---: |']
for arm in ['baseline']+arms:
    r=s[arm];lines.append(f"| {names[arm]} | {pct(r['new_natural_j'])} | {pct(r['new_receiver_coverage'])} | {pct(r['new_receiver_coverage']-r['new_natural_j'])} | {pct(r['all_sender_optimal_decoder'])} |")
lines+=['', '仅发送端学习保持接收表不变，将新图自然成绩提高至38.89%，接近其43.75%的既有覆盖上界。仅接收端学习扩展了接收覆盖，但原发送函数保持不变，自然成绩仍较低；不能将其直接解释为接收者不会理解或学习。双方学习同时提高覆盖和对可达码的自然利用，终点61.81%的接收覆盖对应59.03%的自然完成。',
        '', '这些结果支持当前接口和训练程序中存在两类约束：接收者没有对应正确完整码，以及发送者没有自然选择现成的正确码。它们是固定函数下的可达性与行为分解，不是语言诞生根因或认知能力必要性的证明。','',
        '## 片段复用与自然产出要分开','',
        '严格片段替换只改变一种资源位置，替换一位消息，要求对应资源行动正确迁移，同时保持另一种资源原有的正确行动。人工拼接从旧24图所有合法消息供体取片段，合成新6图消息；供体由分析者选取。每个端点重新读取主体在旧图上当前生成的消息，固定的是供体地图集合，并没有冻结适应前的消息片段。每个端点仅在旧图校准照片上选择两位置与资源的对应，随后在验证照片评价。','',
        '| 阶段 | 全30图严格片段替换 | 减去整体重编码均值 | 旧供体拼接新图 | 减去整体重编码均值 |',
        '| --- | ---: | ---: | ---: | ---: |']
for arm in ['baseline']+arms:
    r=s[arm];lines.append(f"| {names[arm]} | {pct(r['strict'])} | {100*r['strict_excess']:.2f}个百分点 | {pct(r['stitch'])} | {100*r['stitch_excess']:.2f}个百分点 |")
conditional={}
for arm in ['baseline']+arms:
    values=[d['fragments']['strict_transfer_when_both_maps_both_goals_correct']['rate'] for r in p['runs'] if r['arm']==arm for d in r['directions']]
    assert len(values)==24 and all(v is not None for v in values)
    conditional[arm]=float(np.mean(values))
lines+=['', '仅发送端学习使自然新图成功大幅提高，人工拼接却几乎没有变化；仅接收端学习使人工拼接提高，自然新图成绩仍很低。两种分离说明，能由实验者拼出有效消息，与主体自己产生合适消息是不同要求。',
        '', f"双方学习的总体片段替换率从{pct(s['baseline']['strict'])}升到{pct(s['both']['strict'])}。这一指标也要求原场景行动正确，因而可能随基本通信完成度提高。结果后的辅助解读检查显示，仅在原图与供体图本来都自然双成功的子集内，已保存的条件片段率从{pct(conditional['baseline'])}到{pct(conditional['both'])}，变化很小；其他两臂分别为{pct(conditional['sender_only'])}和{pct(conditional['receiver_only'])}。筛选子集随条件变化，这不是一个独立因果对照，但足以提醒我们不要把总体提高全部称为语法增强。",
        '', '每方向端点100次可逆完整码重编码保持所有完整消息行为，用来区分局部片段特性与单纯整码成功。初末及三臂故意共享同一来源方向的参考置换，共24个独特随机种子、9600个条件化参考评分；这些不是独立主体重复。参考不构成重训练对照。',
        '', '[完整协议报告](协议结构报告.md)、[全部协议枚举与重编码记录](protocol_analysis.json)、[固定地图的实际消息](消息实例.md)。固定实例完整展示首个来源方向的六图，没有按成功筛选；该方向在双方学习后仅1/6新图成功，而反方向6/6成功，方向不对称仍然存在。','',
        '## 执行与核查','',
        f"36组全部完成，共21,600次更新、11,059,200个训练世界。独立审计{audit['total_checks']:,}项检查、0失败，复核1,728,000个终点世界及3,456,000次动作；288个检查点冻结分区、来源权重、空初始Adam、更新步数、配对世界和回报均通过。",
        '', f"另以独立协议枚举重放{replay['counts']['decoder_world_rows_replayed']:,}个确定性接收世界及{replay['counts']['sender_world_rows_replayed']:,}个匹配照片的发信世界，全部与终点轨迹一致。随机政策轨迹不纳入贪心重放。流程预检590项检查通过，短预检不计入正式结果。",
        '', '[正式审计](audit_execution.json)、[协议与轨迹交叉核查](protocol_execution_audit.json)、[预检](../../preflight_qa.json)。所有权重、逐步日志、实际消息、照片ID及来源哈希位于各运行目录。协议计算源和方案在正式训练前归档；原v0.8数据未改。','',
        '## 研究价值、边界与下一步','',
        '本轮最有用的发现是：已有共同符号扩展到新情境时，接收覆盖与旧码占用会形成不同约束。只调整一端可能改善其中一部分，双方可塑性在这里取得更快、更高的适应，同时宏平均旧成绩基本保持。自然产出、完整码覆盖和人工片段复用可以分别测量，并出现不同变化。',
        '', '证据仍限于四个继承来源、小型静态地图、固定二人伙伴、已有两种资源需求和预训练视觉表示。没有证明人类语言起源的最小能力集合、分工必要性、一般句法、长期社会生态或代际传递。新组合终点仍只有约59%，少数方向仍很弱。实验是根据旧结果选择混合收益后的机制探索，不能作为全新独立确认或宣称研究首创。',
        '', '下一步应保留一批始终不进入社会训练的第三组组合：例如平衡的18图起步、逐步引入6图、余下6图始终封存。检验学习新约定是否改善尚未经历组合的自主表达，区分“学会更多约定”和“获得更可推广的组合规律”。这项下一阶段方案尚未执行。','',
        '[全部逐种子结果与另外两张图](适应实验分析草稿.md)；[本轮代码和复现入口](../../README.md)。']
(OUT/'新组合通信适应研究报告.md').write_text('\n'.join(lines)+'\n')
(OUT/'conditional_structure_note.json').write_text(json.dumps(dict(timing='Post-result supplementary interpretation of conditional metrics already saved by the protocol probe',
    condition='Both donor and original map already naturally correct for both goals; eligible sets differ across endpoints',
    means=conditional),ensure_ascii=False,indent=2))
print('Wrote completed integrated report')
